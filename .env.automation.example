# =============================================================================
# SSi Automation Server - Local Configuration
# =============================================================================
# Copy this file to .env.automation and customize for your machine
# This allows multiple developers to use the same automation_server.cjs
# with different local paths and settings.
#
# IMPORTANT: .env.automation is gitignored - it's your personal config
# =============================================================================

# -----------------------------------------------------------------------------
# Server Configuration
# -----------------------------------------------------------------------------
PORT=3456

# -----------------------------------------------------------------------------
# VFS Root Path (CUSTOMIZE THIS FOR YOUR MACHINE)
# -----------------------------------------------------------------------------
# Tom's Mac: /Users/tomcassidy/SSi/SSi_Course_Production
# Kai's Mac: /Users/kai/Projects/SSi_Course_Production
#
# This should point to your LOCAL course production directory containing:
# - public/vfs/courses/
# - scripts/
# - docs/
#
VFS_ROOT=/Users/tomcassidy/SSi/SSi_Course_Production

# -----------------------------------------------------------------------------
# Dashboard URL
# -----------------------------------------------------------------------------
# Production: https://ssi-dashboard-v7.vercel.app
# Local dev: http://localhost:5173
TRAINING_URL=https://ssi-dashboard-v7.vercel.app

# -----------------------------------------------------------------------------
# Checkpoint Mode
# -----------------------------------------------------------------------------
# Controls phase automation behavior:
# - 'manual': Pause between phases, require user approval (safest for testing)
# - 'gated': Auto-run phases but pause if validators fail (recommended)
# - 'full': Full automation, no stops (for production after validation)
CHECKPOINT_MODE=manual

# -----------------------------------------------------------------------------
# Orchestrator Configuration
# -----------------------------------------------------------------------------
# Phase 5 orchestrator settings
# USE_ORCHESTRATOR_V2: true = 7 orchestrators × 10 agents (git branches)
#                      false = legacy file-based system
USE_ORCHESTRATOR_V2=true

# Agent spawn delay (milliseconds between spawning browser tabs)
AGENT_SPAWN_DELAY=5000

# Git auto-merge enabled (automatically merge claude/* branches to main)
GIT_AUTO_MERGE=true

# -----------------------------------------------------------------------------
# API Keys (Optional - for audio generation, etc.)
# -----------------------------------------------------------------------------
# Anthropic API (for LEGO regeneration)
ANTHROPIC_API_KEY=your_key_here

# ElevenLabs (for Phase 8 audio generation)
ELEVENLABS_API_KEY=your_key_here

# AWS S3 (for audio storage)
AWS_ACCESS_KEY_ID=your_key_here
AWS_SECRET_ACCESS_KEY=your_secret_here
AWS_REGION=eu-west-1
S3_BUCKET=your-bucket-name

# -----------------------------------------------------------------------------
# Advanced Settings
# -----------------------------------------------------------------------------
# VFS monitor interval (milliseconds)
VFS_MONITOR_INTERVAL=2000

# Validation thresholds for gated mode
VALIDATION_THRESHOLD_PHASE3_ERROR_RATE=0.05
VALIDATION_THRESHOLD_PHASE5_GATE_VIOLATIONS=0.02
VALIDATION_THRESHOLD_PHASE5_QUALITY_SCORE=0.95
