{
  "$schema": "./course-modes.schema.json",
  "version": "2.1.0",
  "description": "Course generation modes - DERIVED FROM APML REGISTRY",
  "canonicalSource": "apml/core/ssi-variable-registry.apml → CourseGenerationMode entity",
  "lastUpdated": "2025-12-16",
  "note": "Mode IDs (quick_test, mvp_course, full_course) MUST match registry. Dashboard fetches from /api/modes which reads this file.",

  "modes": {
    "quick_test": {
      "name": "Quick Test",
      "description": "Fast validation - fewer browsers, more agents per browser",
      "seeds": 10,
      "pattern": {
        "browsers": 2,
        "agents_per_browser": 5,
        "seeds_per_agent": 1
      },
      "capacity": 10,
      "estimated_time_minutes": 2,
      "use_case": "Validation, fast feedback loop",
      "rationale": "2 browsers with 5 agents each is more robust than 5 browsers with 2 agents"
    },

    "mvp_course": {
      "name": "MVP Course",
      "description": "Big 10 language pairs - 2 seeds per agent for granular tracking",
      "seeds": 250,
      "pattern": {
        "browsers": 5,
        "agents_per_browser": 25,
        "seeds_per_agent": 2
      },
      "capacity": 250,
      "estimated_time_minutes": 15,
      "use_case": "Production courses for Big 10 language pairs",
      "rationale": "2 seeds per agent = granular failures. 25 agents per browser keeps tab count low."
    },

    "full_course": {
      "name": "Full Course",
      "description": "Complete legacy-scale course - 2 seeds per agent for granular tracking",
      "seeds": 668,
      "pattern": {
        "browsers": 14,
        "agents_per_browser": 25,
        "seeds_per_agent": 2
      },
      "capacity": 700,
      "estimated_time_minutes": 25,
      "use_case": "Complete courses (legacy Spanish for English scale)",
      "rationale": "2 seeds per agent = granular failures. 25 agents per browser keeps tab count low."
    }
  },

  "defaults": {
    "browser_spawn_delay_ms": 8000,
    "agent_timeout_ms": 300000,
    "max_retries": 3,
    "retry_backoff_ms": 5000
  },

  "resume": {
    "comment": "Resume mode uses 1 seed per agent for maximum granularity",
    "seeds_per_agent": 1,
    "agents_per_browser": 25,
    "rationale": "1 seed per agent = if worker fails, only 1 seed lost. Easy retry."
  },

  "phase_overrides": {
    "phase1_translation": {
      "comment": "Phase 1 can use same patterns - translation is lightweight"
    },
    "phase2_conflict_resolution": {
      "comment": "Phase 2 uses same patterns - LEGO extraction is medium weight"
    },
    "phase3_basket_generation": {
      "comment": "Phase 3 is token-heavy - may need reduced seeds_per_agent",
      "seeds_per_agent_multiplier": 1.0
    }
  },

  "deprecated": {
    "30_seed_mode": {
      "reason": "Replaced by quick_test (10) and mvp_course (250)",
      "migration": "Use quick_test for validation, mvp_course for production"
    }
  }
}
