# Morning brief — 2026-06-16 (overnight autonomous run)

*What I did, the corrected model, and prioritised next actions. Text/analysis/contract/code only — NO TTS, NO learner-facing content mass-mutated. All on `main` via `methodology-encoding`.*

## The rule I got wrong (twice), now corrected in code
**There is NO inflection allowance.** A known form is usable **only if it was introduced as a LEGO or a COMPONENT of an M-LEGO — exact form.** No stemming, no "introduce the tense class once then it's free", no morphology exemption. `tried`/`wants`/`going` each need their own LEGO/component debut; an introduced `try`/`want`/`go` does **not** license them.

- **Fixed in the gate**: `stemKnownGloss` is now an exact normaliser (was stripping `ing/ed/s/e/d`). Verified: with only `I`,`try` introduced, `I try` passes, `I tried`/`I tries` flag. This enforces the rule on both the vocab side and the prompt side.
- **Cleaned the contracts**: removed the vestigial `stemStrip` and my wrong `knownTenseConstructions` (the "introduce-once-then-free" model) from `zho_for_eng` + `fra_for_eng`. Genuine glue (`a/the/to`) + NPI-under-negation stay free (separately ratified; they're not inflections).

## What that means for the numbers
The earlier counts used stemming, so they **undercounted** and the "617 tense flags collapse to ~7 demos" framing was **wrong** — there is no collapse; every uncontrolled form is a real violation. Strict (exact-match) samples:
- **zho**: 100 known-side flags / 40 baskets (was ~21 stemmed).
- **French**: 131 / 40.
The old courses were written with **natural English inflection, not a strictly-controlled known language** — so the known-side debt is large and real across both. The fix is rigorous known-language control: every form a phrase uses must have been introduced (in practice, as a component of an M-LEGO at first use), or the phrase must not use it.

## French travel test (still valid, reframed)
- **The template travels**: full lint ran on `fra_for_eng` (1652 legos, old-way). The English known-side scaffold reuses verbatim across every `_for_eng` pair (known=eng). First-pass French contract committed (target layer empty, flags author it).
- **ZUT on inflected targets is REAL, not exempt** (I had this wrong): French `going to`→`vais`/`vas` is a genuine ZUT violation — the English must carry person/number/gender so each maps to one French form (`I'm going to`→vais, `you're going to`→vas), AND each French form must be its own LEGO/component. So inflected courses carry **more** real debt, not a morphology pass.
- **Construction-licensing gaps** (89% of French known-side) collapse once the French contract's `constructions` are defined — that part of "flags author the contract" stands.
- **Frame-coverage**: made script-aware (was CJK-only) so it works for space-separated targets; verified. Real, kept.

## PRIORITISED NEXT ACTIONS
1. **[methodology, decide] Known-language control policy for authoring.** The gate now enforces exact-form membership. The regeneration prompt must author **only introduced forms** — i.e. when a phrase needs `tried`, the build introduces `tried` as a component first. Confirm this is the policy (it's what M-LEGO components already do) and how strict to be on glue/NPI. *This is what makes the gate satisfiable at scale; everything else depends on it.*
2. **[zho] Bring the known language under strict control + work the fix-queue** — the S351–668 ZUT consolidations (90, learner-facing) + introduce/restrict the flagged forms. Strict known-side debt is large; needs a controlled pass, not 7 demos.
3. **[French] Ratify the French contract from the flags** — define `constructions` (going-to/`ne…pas`/question-forms/relative/clitics), make ZUT-colliding English person/number/gender-specific, re-lint to confirm collapse.
4. **[program] Re-run the full strict lints** (zho S351–668 + French) now the gate is exact, to get true totals (the earlier full numbers were stemmed undercounts).

## Open consideration (flagging, not deciding)
Strict no-inflection-allowance is demanding: a 668-seed course must introduce every form it uses, in order. That's satisfiable via M-LEGO components (cheap) but it does constrain authoring and may lengthen courses / limit early expressiveness. Worth a conscious check that the bar is right at scale vs. learner experience — but I've implemented it exactly as you stated.

## Confidence
**>90% on the program shape**: gate-and-fix existing courses + per-pair contracts (flags author them) + English known-side reused across `_for_eng` + **strict exact-form known-language control** (no inflection allowance). The French test proved it travels; your correction tightened the known-side rule to exact-match, now enforced in the gate.

## State (all on `main`)
- Gate: `stemKnownGloss` exact (no inflection allowance); frame-coverage script-aware; phrase-ZUT phrase-granular.
- Contracts: `zho_for_eng` + `fra_for_eng` cleaned (no stemStrip / no tense-class).
- Toolchain: `scripts/lint-any.cjs <course>` (strict, generic), `fra-analysis.cjs`, `reclassify-known-side.cjs`.
- Docs: this brief + `upper-half-fix-queue.md` (NB its known-side section predates the strict gate — re-run per action #4).
