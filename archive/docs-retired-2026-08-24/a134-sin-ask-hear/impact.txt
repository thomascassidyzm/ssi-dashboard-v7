[language-code-service] Loaded 496 language codes from CSV
[language-code-service] TTS configured: 84 Azure, 75 ElevenLabs, 79 Google
[language-code-service] Legacy mappings: 107 total (110 from CSV)

══════════════════════════════════════════════════════════════════════════════
  eng_for_sin  ·  course_seeds  ·  seed 380  [proposed]
══════════════════════════════════════════════════════════════════════════════
  known_text:
      was  "ඇය ඇතුළු කරන්නට ඕනේ කළේ මොකක්ද කියලා මම ඇහුවා."
      now  "ඇය ඇතුළු කරන්නට ඕනේ කළේ මොකක්ද කියලා මම ඇයගෙන් ඇහුවා."
  target_text:
      was  "I asked what she wanted to include."
      now  "I asked her what she wanted to include."

┌─ DECISION ─────────────────────────────────────────────────────────────────
│  RECONSIDER
│  Reconsider this edit as written: it damages work beyond itself. Revise it, narrow it, or accept the listed cost as a deliberate decision.
│
│  because:
│    · The new seed target does not tile from this seed's existing LEGOs, so the breakdown must be rebuilt — this is not a text-only edit.
│    · known_audio_id will be left pointing at the OLD text with no trigger and no alarm.
│    · target1_audio_id will be left pointing at the OLD text with no trigger and no alarm.
│    · target2_audio_id will be left pointing at the OLD text with no trigger and no alarm.
│    · 2 intro clip(s) embed the old text and will keep speaking it.
│    · 2 other row(s) carry the identical old text and share its clip. Editing one leaves the course inconsistent.
│
│  before this edit is finished, you must:
│    1. Produce a new LEGO breakdown and apply it through POST /api/course/:code/edit-cascade (which snapshots and rolls back on a bad breakdown) rather than by direct UPDATE.
│    2. No clip exists for the new text yet. NULL known_audio_id so audio_autolink binds the new clip when it is rendered, then queue an audio pass (tools/course-optimization/queue-audio-pass.cjs).
│    3. No clip exists for the new text yet. NULL target1_audio_id so audio_autolink binds the new clip when it is rendered, then queue an audio pass (tools/course-optimization/queue-audio-pass.cjs).
│    4. No clip exists for the new text yet. NULL target2_audio_id so audio_autolink binds the new clip when it is rendered, then queue an audio pass (tools/course-optimization/queue-audio-pass.cjs).
│    5. Re-compose 2 presentation clip(s) (POST /regenerate-presentations, which self-scopes to missing), then queue the audio pass.
│    6. Decide explicitly whether the other 2 row(s) should change too — include them in this proposal, or say why not.
│    7. If this text appears in a pod, migrate learner progress FIRST — docs/pods/pod-migration-protocol.md. Never edit a live pod in place.
│    8. ≈5 clip(s) will need rendering. TTS costs money — show a plan and get approval; end the pass by QUEUEING, never by running TTS.
└────────────────────────────────────────────────────────────────────────────

── VERDICT ──────────────────────────────────────────────────────────────────
  [DANGER] SILENT STALE AUDIO: course_seeds has no audio-nulling trigger. After this edit the learner keeps hearing the OLD text with nothing to signal it. You must repoint the link by hand, or generate the new clip and NULL the link first so audio_autolink can bind it.
  [DANGER] STALE LINK (known_audio_id): still points at d8cf428e-f614-458b-8dcc-84bdccee8313, which speaks "ඇය ඇතුළු කරන්නට ඕනේ කළේ මොකක්ද කියලා මම ඇහුවා." — the OLD text. No clip exists for the new text yet. NULL known_audio_id so audio_autolink binds the new clip when it is rendered, then queue an audio pass (tools/course-optimization/queue-audio-pass.cjs).
  [DANGER] STALE LINK (target1_audio_id): still points at a1d66785-2b8c-4e4b-b952-1dbddc777921, which speaks "I asked what she wanted to include." — the OLD text. No clip exists for the new text yet. NULL target1_audio_id so audio_autolink binds the new clip when it is rendered, then queue an audio pass (tools/course-optimization/queue-audio-pass.cjs).
  [DANGER] STALE LINK (target2_audio_id): still points at 1793eced-b673-4c40-8263-79940cdad9ae, which speaks "I asked what she wanted to include." — the OLD text. No clip exists for the new text yet. NULL target2_audio_id so audio_autolink binds the new clip when it is rendered, then queue an audio pass (tools/course-optimization/queue-audio-pass.cjs).
  [ WARN] RE-DECOMPOSITION NEEDED: seed 380's 3 LEGO(s) no longer tile its new target text.
  [ WARN] SAME TEXT ELSEWHERE: 2 other row(s) in this course carry the identical old text. Editing one and not the others leaves the course inconsistent — and they share the same clip.
  [DANGER] PRESENTATIONS STALE: 2 intro clip(s) embed the OLD text and will keep speaking it. Presentations are per-LEGO and must be re-composed; the presentation text is built at render time, so this tool reports the clips, not the replacement wording.

── TRIGGER ──────────────────────────────────────────────────────────────────
  relink trigger: NONE
  NO nulling/relink trigger on course_seeds. The audio link is left EXACTLY AS IT WAS, still pointing at the clip for the OLD text. No NULL, no orphan, no alarm — a missing-audio sweep cannot see it and audio_autolink cannot rescue it (it only fills links that are already NULL).

── AUDIO LINKS ──────────────────────────────────────────────────────────────
  known_audio_id         left-stale
      now:  d8cf428e-f614-458b-8dcc-84bdccee8313  [azure_si-LK-SameeraNeural] "ඇය ඇතුළු කරන්නට ඕනේ කළේ මොකක්ද කියලා මම ඇහුවා."
      after:d8cf428e-f614-458b-8dcc-84bdccee8313  [azure_si-LK-SameeraNeural] "ඇය ඇතුළු කරන්නට ඕනේ කළේ මොකක්ද කියලා මම ඇහුවා."
      →  No clip exists for the new text yet. NULL known_audio_id so audio_autolink binds the new clip when it is rendered, then queue an audio pass (tools/course-optimization/queue-audio-pass.cjs).
  target1_audio_id       left-stale
      now:  a1d66785-2b8c-4e4b-b952-1dbddc777921  [bedd6226] "I asked what she wanted to include."
      after:a1d66785-2b8c-4e4b-b952-1dbddc777921  [bedd6226] "I asked what she wanted to include."
      →  No clip exists for the new text yet. NULL target1_audio_id so audio_autolink binds the new clip when it is rendered, then queue an audio pass (tools/course-optimization/queue-audio-pass.cjs).
  target2_audio_id       left-stale
      now:  1793eced-b673-4c40-8263-79940cdad9ae  [xai_gfzdpspr5fdp] "I asked what she wanted to include."
      after:1793eced-b673-4c40-8263-79940cdad9ae  [xai_gfzdpspr5fdp] "I asked what she wanted to include."
      →  No clip exists for the new text yet. NULL target2_audio_id so audio_autolink binds the new clip when it is rendered, then queue an audio pass (tools/course-optimization/queue-audio-pass.cjs).

── DERIVED ──────────────────────────────────────────────────────────────────
  legos: 3
  phrases: 15
  legos:
      L1 (M)  "ඇතුළු කරන්නට" → "to include"
      L2 (M)  "ඇය ... ඕනේ කළේ" → "she wanted to"
      L3 (M)  "මොකක්ද කියලා මම ඇහුවා" → "I asked what"
  edit_case: vocab-changing (Case 2) — the new target does NOT tile from this seed's existing LEGOs; a re-decomposition is required
  note: Re-decompose this seed (POST /api/course/:code/edit-cascade with the new legos, or /v2/decompose) — the LEGOs below no longer cover the seed text.

── COURSE-WIDE ──────────────────────────────────────────────────────────────
  tiling: not at risk — No vocab unit removed — nothing can stop tiling.
  same known text elsewhere (1): phrase 380/eng_for_
  same target text elsewhere (1): phrase 380/eng_for_

── PRESENTATIONS ────────────────────────────────────────────────────────────
  2 intro clip(s) embed the old text:
      d8406136-75ef-4800-b792-cb4c381b4a62 lego=S0380L01 [azure_si-LK-SameeraNeural] "ඉංග්‍රීසිෙන්. 'ඇතුළු කරන්නට'. 'ඇය ඇතුළු කරන්නට ඕනේ කළේ ම"
      d4130d95-307d-4743-998e-cb23294d5975 lego=S0380L03 [azure_si-LK-SameeraNeural] "ඉංග්‍රීසිෙන්. 'මොකක්ද කියලා මම ඇහුවා'. 'ඇය ඇතුළු කරන්නට "

── TTS ──────────────────────────────────────────────────────────────────────
  ≈ 5 clip(s) would need rendering (3 row + 2 presentation). NOTHING WAS RENDERED.
  ESTIMATE ONLY. This tool never renders. Content passes end by QUEUEING an audio pass (tools/course-optimization/queue-audio-pass.cjs), never by running TTS.

── DOCTRINE ─────────────────────────────────────────────────────────────────
  [CHECK] pod-content-migration: eng_for_sin has 1 pod(s). If this edit touches text that a pod sentence carries, learner progress MUST be migrated — progress is filed under a slot, not the text, so an in-place edit silently credits a learner with a sentence they never heard.
         → docs/pods/pod-migration-protocol.md (adopted 2026-08-16, plate A-111)
  [ REQD] make-before-break: 3 audio link(s) change. Generate and VERIFY the replacement clip before the old one is touched — never the other way round. Deletion never precedes a verified replacement.
         → docs/architecture/AUDIO_PIPELINE_ARCHITECTURE.md §6b
  [ info] learner-cache-invalidated: course_seeds carries touch_course_content_stamp, so this edit bumps courses.content_stamp (currently Mon Aug 17 2026 12:37:35 GMT+0000 (Coordinated Universal Time)). Learner script caches for eng_for_sin invalidate on the next read.
  [ info] learner-reach: courses.new_app_status = "beta". This course is reachable by learners — the blast radius is live.


══════════════════════════════════════════════════════════════════════════════
  eng_for_sin  ·  course_legos  ·  lego 380:3  [proposed]
══════════════════════════════════════════════════════════════════════════════
  known_text:
      was  "මොකක්ද කියලා මම ඇහුවා"
      now  "මොකක්ද කියලා මම ඇයගෙන් ඇහුවා"
  target_text:
      was  "I asked what"
      now  "I asked her what"

┌─ DECISION ─────────────────────────────────────────────────────────────────
│  RECONSIDER
│  Reconsider this edit as written: it damages work beyond itself. Revise it, narrow it, or accept the listed cost as a deliberate decision.
│
│  because:
│    · 143 phrase(s) across 48 seed(s) stop tiling once this edit removes "i asked what". Those phrases are other people's finished work.
│    · 8 of this LEGO's own build/use phrases no longer contain its target text, so the LEGO stops being what those phrases teach.
│    · known_audio_id becomes NULL — a silent slot until new audio is rendered.
│    · target1_audio_id becomes NULL — a silent slot until new audio is rendered.
│    · target2_audio_id becomes NULL — a silent slot until new audio is rendered.
│    · presentation_audio_id becomes NULL — a silent slot until new audio is rendered.
│    · 2 intro clip(s) embed the old text and will keep speaking it.
│    · 2 other row(s) carry the identical old text and share its clip. Editing one leaves the course inconsistent.
│
│  before this edit is finished, you must:
│    1. Either keep the removed chunk(s) in the new text, or accept re-authoring 143 phrase(s) (seeds 380, 396, 403, 410, 412, 427, 441, 485, 489, 490, +38 more).
│    2. Re-author 8 phrase(s) for this LEGO, or choose a new text those phrases still contain.
│    3. Queue an audio pass so known_audio_id is refilled: node tools/course-optimization/queue-audio-pass.cjs eng_for_sin --reason "<pass>". Do NOT run TTS directly.
│    4. Queue an audio pass so target1_audio_id is refilled: node tools/course-optimization/queue-audio-pass.cjs eng_for_sin --reason "<pass>". Do NOT run TTS directly.
│    5. Queue an audio pass so target2_audio_id is refilled: node tools/course-optimization/queue-audio-pass.cjs eng_for_sin --reason "<pass>". Do NOT run TTS directly.
│    6. Queue an audio pass so presentation_audio_id is refilled: node tools/course-optimization/queue-audio-pass.cjs eng_for_sin --reason "<pass>". Do NOT run TTS directly.
│    7. Re-compose 2 presentation clip(s) (POST /regenerate-presentations, which self-scopes to missing), then queue the audio pass.
│    8. Decide explicitly whether the other 2 row(s) should change too — include them in this proposal, or say why not.
│    9. If this text appears in a pod, migrate learner progress FIRST — docs/pods/pod-migration-protocol.md. Never edit a live pod in place.
│    10. ≈5 clip(s) will need rendering. TTS costs money — show a plan and get approval; end the pass by QUEUEING, never by running TTS.
└────────────────────────────────────────────────────────────────────────────

── VERDICT ──────────────────────────────────────────────────────────────────
  [ WARN] SILENT SLOT (known_audio_id): no existing clip matches the new text, so the trigger sets this to NULL. The slot is silent until Generate Missing Audio runs.
  [ WARN] SILENT SLOT (target1_audio_id): no existing clip matches the new text, so the trigger sets this to NULL. The slot is silent until Generate Missing Audio runs.
  [ WARN] SILENT SLOT (target2_audio_id): no existing clip matches the new text, so the trigger sets this to NULL. The slot is silent until Generate Missing Audio runs.
  [ WARN] SILENT SLOT (presentation_audio_id): no existing clip matches the new text, so the trigger sets this to NULL. The slot is silent until Generate Missing Audio runs.
  [DANGER] CONTAINMENT BROKEN: 8 of this LEGO's own phrase(s) no longer contain its new target text.
  [DANGER] COURSE-WIDE BREAKAGE: 143 phrase(s) elsewhere in the course tile through a chunk this edit removes and will no longer validate.
  [ WARN] SAME TEXT ELSEWHERE: 2 other row(s) in this course carry the identical old text. Editing one and not the others leaves the course inconsistent — and they share the same clip.
  [DANGER] PRESENTATIONS STALE: 2 intro clip(s) embed the OLD text and will keep speaking it. Presentations are per-LEGO and must be re-composed; the presentation text is built at render time, so this tool reports the clips, not the replacement wording.

── TRIGGER ──────────────────────────────────────────────────────────────────
  relink trigger: trg_null_lego_audio_on_text_change
  trg_null_lego_audio_on_text_change fires BEFORE UPDATE and REPLACES the audio link with audio_id_for_text(course, <new text>, role) — which is NULL when nothing matches (silent slot) and an EXISTING clip when something does (silent re-point, possibly to another voice).

── AUDIO LINKS ──────────────────────────────────────────────────────────────
  known_audio_id         nulled-silent
      now:  ea266345-2f93-4ae4-b0d9-f02d2f6456b2  [azure_si-LK-SameeraNeural] "මොකක්ද කියලා මම ඇහුවා"
      after: NULL (silent)  
  target1_audio_id       nulled-silent
      now:  fa5ac1e4-04fa-48b0-ade3-be3f59b04212  [xai_bedd6226] "I asked what"
      after: NULL (silent)  
  target2_audio_id       nulled-silent
      now:  82cabbfe-b6b3-4c29-9e41-ef9ee7b4b91c  [gfzdpspr5fdp] "I asked what"
      after: NULL (silent)  
  presentation_audio_id  nulled-silent
      now:  d4130d95-307d-4743-998e-cb23294d5975  [azure_si-LK-SameeraNeural] "ඉංග්‍රීසිෙන්. 'මොකක්ද කියලා මම ඇහුවා'. 'ඇය ඇතුළු"
      after: NULL (silent)  

── DERIVED ──────────────────────────────────────────────────────────────────
  phrases: 8
  containment failures:
      build eng_for_sin:S0380L03B01  "I asked what"
      build eng_for_sin:S0380L03B02  "I asked what today"
      build eng_for_sin:S0380L03B03  "I asked what a lot"
      use eng_for_sin:S0380L03U01  "I asked what she wanted to include"
      use eng_for_sin:S0380L03U02  "I asked what she wanted to"
      use eng_for_sin:S0380L03U03  "I asked what he can't have"
      use eng_for_sin:S0380L03U04  "I asked what he wanted"
      use eng_for_sin:S0380L03U05  "I asked what they wanted"
  note: A LEGO is the teaching unit; its own build/use phrases must still contain its target text.

── COURSE-WIDE ──────────────────────────────────────────────────────────────
  tiling: 143 phrase(s) break of 4092 re-tiled  (chunks removed: i asked what)
      seed 380 L3 build: "I asked what"  ✗ "asked what"
      seed 380 L3 build: "I asked what today"  ✗ "asked what today"
      seed 380 L3 build: "I asked what a lot"  ✗ "asked what a lot"
      seed 380 L3 use: "I asked what she wanted to include"  ✗ "asked what she wanted to include"
      seed 380 L3 use: "I asked what she wanted to"  ✗ "asked what she wanted to"
      seed 380 L3 use: "I asked what he can't have"  ✗ "asked what he cant have"
      seed 380 L3 use: "I asked what he wanted"  ✗ "asked what he wanted"
      seed 380 L3 use: "I asked what they wanted"  ✗ "asked what they wanted"
      seed 396 L2 build: "we need to stand until everybody else is ready"  ✗ "stand until everybody else is ready"
      seed 396 L2 use: "I think we need to stand until everybody else is ready"  ✗ "stand until everybody else is ready"
      seed 403 L1 build: "to remain quiet for as long as possible"  ✗ "remain quiet for as long as possible"
      seed 403 L1 build: "we need to stand for as long as possible"  ✗ "stand for as long as possible"
      seed 403 L1 build: "I want to remain quiet for as long as possible"  ✗ "remain quiet for as long as possible"
      seed 403 L1 use: "he wants to remain quiet for as long as possible"  ✗ "remain quiet for as long as possible"
      seed 403 L1 use: "I don't want to remain quiet for as long as possible"  ✗ "remain quiet for as long as possible"
      … and 128 more (full list in the JSON report)
  same known text elsewhere (1): phrase 380/eng_for_
  same target text elsewhere (1): phrase 380/eng_for_

── PRESENTATIONS ────────────────────────────────────────────────────────────
  2 intro clip(s) embed the old text:
      d8406136-75ef-4800-b792-cb4c381b4a62 lego=S0380L01 [azure_si-LK-SameeraNeural] "ඉංග්‍රීසිෙන්. 'ඇතුළු කරන්නට'. 'ඇය ඇතුළු කරන්නට ඕනේ කළේ ම"
      d4130d95-307d-4743-998e-cb23294d5975 lego=S0380L03 [azure_si-LK-SameeraNeural] "ඉංග්‍රීසිෙන්. 'මොකක්ද කියලා මම ඇහුවා'. 'ඇය ඇතුළු කරන්නට "

── TTS ──────────────────────────────────────────────────────────────────────
  ≈ 5 clip(s) would need rendering (3 row + 2 presentation). NOTHING WAS RENDERED.
  ESTIMATE ONLY. This tool never renders. Content passes end by QUEUEING an audio pass (tools/course-optimization/queue-audio-pass.cjs), never by running TTS.

── DOCTRINE ─────────────────────────────────────────────────────────────────
  [CHECK] pod-content-migration: eng_for_sin has 1 pod(s). If this edit touches text that a pod sentence carries, learner progress MUST be migrated — progress is filed under a slot, not the text, so an in-place edit silently credits a learner with a sentence they never heard.
         → docs/pods/pod-migration-protocol.md (adopted 2026-08-16, plate A-111)
  [ REQD] make-before-break: 4 audio link(s) change. Generate and VERIFY the replacement clip before the old one is touched — never the other way round. Deletion never precedes a verified replacement.
         → docs/architecture/AUDIO_PIPELINE_ARCHITECTURE.md §6b
  [ info] learner-cache-invalidated: course_legos carries touch_course_content_stamp, so this edit bumps courses.content_stamp (currently Mon Aug 17 2026 12:37:35 GMT+0000 (Coordinated Universal Time)). Learner script caches for eng_for_sin invalidate on the next read.
  [ info] learner-reach: courses.new_app_status = "beta". This course is reachable by learners — the blast radius is live.


══════════════════════════════════════════════════════════════════════════════
  eng_for_sin  ·  course_practice_phrases  ·  phrase eng_for_sin:S0380L03B01  [proposed]
══════════════════════════════════════════════════════════════════════════════
  known_text:
      was  "මොකක්ද කියලා මම ඇහුවා"
      now  "මොකක්ද කියලා මම ඇයගෙන් ඇහුවා"
  target_text:
      was  "I asked what"
      now  "I asked her what"

┌─ DECISION ─────────────────────────────────────────────────────────────────
│  PROCEED-WITH-REPAIRS
│  Safe to apply, but it is not finished when the text lands — 7 follow-up action(s) are required to leave the course whole.
│
│  because:
│    · known_audio_id becomes NULL — a silent slot until new audio is rendered.
│    · target1_audio_id becomes NULL — a silent slot until new audio is rendered.
│    · target2_audio_id becomes NULL — a silent slot until new audio is rendered.
│    · 2 intro clip(s) embed the old text and will keep speaking it.
│    · 4 other row(s) carry the identical old text and share its clip. Editing one leaves the course inconsistent.
│
│  before this edit is finished, you must:
│    1. Queue an audio pass so known_audio_id is refilled: node tools/course-optimization/queue-audio-pass.cjs eng_for_sin --reason "<pass>". Do NOT run TTS directly.
│    2. Queue an audio pass so target1_audio_id is refilled: node tools/course-optimization/queue-audio-pass.cjs eng_for_sin --reason "<pass>". Do NOT run TTS directly.
│    3. Queue an audio pass so target2_audio_id is refilled: node tools/course-optimization/queue-audio-pass.cjs eng_for_sin --reason "<pass>". Do NOT run TTS directly.
│    4. Re-compose 2 presentation clip(s) (POST /regenerate-presentations, which self-scopes to missing), then queue the audio pass.
│    5. Decide explicitly whether the other 4 row(s) should change too — include them in this proposal, or say why not.
│    6. If this text appears in a pod, migrate learner progress FIRST — docs/pods/pod-migration-protocol.md. Never edit a live pod in place.
│    7. ≈5 clip(s) will need rendering. TTS costs money — show a plan and get approval; end the pass by QUEUEING, never by running TTS.
└────────────────────────────────────────────────────────────────────────────

── VERDICT ──────────────────────────────────────────────────────────────────
  [ WARN] SILENT SLOT (known_audio_id): no existing clip matches the new text, so the trigger sets this to NULL. The slot is silent until Generate Missing Audio runs.
  [ WARN] SILENT SLOT (target1_audio_id): no existing clip matches the new text, so the trigger sets this to NULL. The slot is silent until Generate Missing Audio runs.
  [ WARN] SILENT SLOT (target2_audio_id): no existing clip matches the new text, so the trigger sets this to NULL. The slot is silent until Generate Missing Audio runs.
  [ WARN] SAME TEXT ELSEWHERE: 4 other row(s) in this course carry the identical old text. Editing one and not the others leaves the course inconsistent — and they share the same clip.
  [DANGER] PRESENTATIONS STALE: 2 intro clip(s) embed the OLD text and will keep speaking it. Presentations are per-LEGO and must be re-composed; the presentation text is built at render time, so this tool reports the clips, not the replacement wording.

── TRIGGER ──────────────────────────────────────────────────────────────────
  relink trigger: trg_null_phrase_audio_on_text_change
  trg_null_phrase_audio_on_text_change fires BEFORE UPDATE and REPLACES the audio link with audio_id_for_text(course, <new text>, role) — which is NULL when nothing matches (silent slot) and an EXISTING clip when something does (silent re-point, possibly to another voice).

── AUDIO LINKS ──────────────────────────────────────────────────────────────
  known_audio_id         nulled-silent
      now:  ea266345-2f93-4ae4-b0d9-f02d2f6456b2  [azure_si-LK-SameeraNeural] "මොකක්ද කියලා මම ඇහුවා"
      after: NULL (silent)  
  target1_audio_id       nulled-silent
      now:  fa5ac1e4-04fa-48b0-ade3-be3f59b04212  [xai_bedd6226] "I asked what"
      after: NULL (silent)  
  target2_audio_id       nulled-silent
      now:  82cabbfe-b6b3-4c29-9e41-ef9ee7b4b91c  [gfzdpspr5fdp] "I asked what"
      after: NULL (silent)  

── DERIVED ──────────────────────────────────────────────────────────────────
  note: A phrase has no derived rows; its blast radius is its own audio plus the course-wide checks below.

── COURSE-WIDE ──────────────────────────────────────────────────────────────
  tiling: not at risk — No vocab unit removed — nothing can stop tiling.
  same known text elsewhere (2): lego 380:3, phrase 380/eng_for_
  same target text elsewhere (2): lego 380:3, phrase 380/eng_for_

── PRESENTATIONS ────────────────────────────────────────────────────────────
  2 intro clip(s) embed the old text:
      d8406136-75ef-4800-b792-cb4c381b4a62 lego=S0380L01 [azure_si-LK-SameeraNeural] "ඉංග්‍රීසිෙන්. 'ඇතුළු කරන්නට'. 'ඇය ඇතුළු කරන්නට ඕනේ කළේ ම"
      d4130d95-307d-4743-998e-cb23294d5975 lego=S0380L03 [azure_si-LK-SameeraNeural] "ඉංග්‍රීසිෙන්. 'මොකක්ද කියලා මම ඇහුවා'. 'ඇය ඇතුළු කරන්නට "

── TTS ──────────────────────────────────────────────────────────────────────
  ≈ 5 clip(s) would need rendering (3 row + 2 presentation). NOTHING WAS RENDERED.
  ESTIMATE ONLY. This tool never renders. Content passes end by QUEUEING an audio pass (tools/course-optimization/queue-audio-pass.cjs), never by running TTS.

── DOCTRINE ─────────────────────────────────────────────────────────────────
  [CHECK] pod-content-migration: eng_for_sin has 1 pod(s). If this edit touches text that a pod sentence carries, learner progress MUST be migrated — progress is filed under a slot, not the text, so an in-place edit silently credits a learner with a sentence they never heard.
         → docs/pods/pod-migration-protocol.md (adopted 2026-08-16, plate A-111)
  [ REQD] make-before-break: 3 audio link(s) change. Generate and VERIFY the replacement clip before the old one is touched — never the other way round. Deletion never precedes a verified replacement.
         → docs/architecture/AUDIO_PIPELINE_ARCHITECTURE.md §6b
  [ info] learner-cache-invalidated: course_practice_phrases carries touch_course_content_stamp, so this edit bumps courses.content_stamp (currently Mon Aug 17 2026 12:37:35 GMT+0000 (Coordinated Universal Time)). Learner script caches for eng_for_sin invalidate on the next read.
  [ info] learner-reach: courses.new_app_status = "beta". This course is reachable by learners — the blast radius is live.


══════════════════════════════════════════════════════════════════════════════
  eng_for_sin  ·  course_practice_phrases  ·  phrase eng_for_sin:S0380L03B02  [proposed]
══════════════════════════════════════════════════════════════════════════════
  known_text:
      was  "මොකක්ද කියලා මම ඇහුවා අද"
      now  "මොකක්ද කියලා මම ඇයගෙන් ඇහුවා අද"
  target_text:
      was  "I asked what today"
      now  "I asked her what today"

┌─ DECISION ─────────────────────────────────────────────────────────────────
│  PROCEED-WITH-REPAIRS
│  Safe to apply, but it is not finished when the text lands — 6 follow-up action(s) are required to leave the course whole.
│
│  because:
│    · known_audio_id becomes NULL — a silent slot until new audio is rendered.
│    · target1_audio_id becomes NULL — a silent slot until new audio is rendered.
│    · target2_audio_id becomes NULL — a silent slot until new audio is rendered.
│    · 2 other row(s) carry the identical old text and share its clip. Editing one leaves the course inconsistent.
│
│  before this edit is finished, you must:
│    1. Queue an audio pass so known_audio_id is refilled: node tools/course-optimization/queue-audio-pass.cjs eng_for_sin --reason "<pass>". Do NOT run TTS directly.
│    2. Queue an audio pass so target1_audio_id is refilled: node tools/course-optimization/queue-audio-pass.cjs eng_for_sin --reason "<pass>". Do NOT run TTS directly.
│    3. Queue an audio pass so target2_audio_id is refilled: node tools/course-optimization/queue-audio-pass.cjs eng_for_sin --reason "<pass>". Do NOT run TTS directly.
│    4. Decide explicitly whether the other 2 row(s) should change too — include them in this proposal, or say why not.
│    5. If this text appears in a pod, migrate learner progress FIRST — docs/pods/pod-migration-protocol.md. Never edit a live pod in place.
│    6. ≈3 clip(s) will need rendering. TTS costs money — show a plan and get approval; end the pass by QUEUEING, never by running TTS.
└────────────────────────────────────────────────────────────────────────────

── VERDICT ──────────────────────────────────────────────────────────────────
  [ WARN] SILENT SLOT (known_audio_id): no existing clip matches the new text, so the trigger sets this to NULL. The slot is silent until Generate Missing Audio runs.
  [ WARN] SILENT SLOT (target1_audio_id): no existing clip matches the new text, so the trigger sets this to NULL. The slot is silent until Generate Missing Audio runs.
  [ WARN] SILENT SLOT (target2_audio_id): no existing clip matches the new text, so the trigger sets this to NULL. The slot is silent until Generate Missing Audio runs.
  [ WARN] SAME TEXT ELSEWHERE: 2 other row(s) in this course carry the identical old text. Editing one and not the others leaves the course inconsistent — and they share the same clip.

── TRIGGER ──────────────────────────────────────────────────────────────────
  relink trigger: trg_null_phrase_audio_on_text_change
  trg_null_phrase_audio_on_text_change fires BEFORE UPDATE and REPLACES the audio link with audio_id_for_text(course, <new text>, role) — which is NULL when nothing matches (silent slot) and an EXISTING clip when something does (silent re-point, possibly to another voice).

── AUDIO LINKS ──────────────────────────────────────────────────────────────
  known_audio_id         nulled-silent
      now:  08d83dac-7fdd-4221-92f9-d578d8bf8c9e  [azure_si-LK-SameeraNeural] "මොකක්ද කියලා මම ඇහුවා අද"
      after: NULL (silent)  
  target1_audio_id       nulled-silent
      now:  ca71956e-5e7d-45c8-b15f-ac2fdb6fe03c  [xai_bedd6226] "I asked what today"
      after: NULL (silent)  
  target2_audio_id       nulled-silent
      now:  b03830ce-b84c-4a85-b7ad-2bfccdd26f87  [xai_gfzdpspr5fdp] "I asked what today"
      after: NULL (silent)  

── DERIVED ──────────────────────────────────────────────────────────────────
  note: A phrase has no derived rows; its blast radius is its own audio plus the course-wide checks below.

── COURSE-WIDE ──────────────────────────────────────────────────────────────
  tiling: not at risk — No vocab unit removed — nothing can stop tiling.
  same known text elsewhere (1): phrase 380/eng_for_
  same target text elsewhere (1): phrase 380/eng_for_

── TTS ──────────────────────────────────────────────────────────────────────
  ≈ 3 clip(s) would need rendering (3 row + 0 presentation). NOTHING WAS RENDERED.
  ESTIMATE ONLY. This tool never renders. Content passes end by QUEUEING an audio pass (tools/course-optimization/queue-audio-pass.cjs), never by running TTS.

── DOCTRINE ─────────────────────────────────────────────────────────────────
  [CHECK] pod-content-migration: eng_for_sin has 1 pod(s). If this edit touches text that a pod sentence carries, learner progress MUST be migrated — progress is filed under a slot, not the text, so an in-place edit silently credits a learner with a sentence they never heard.
         → docs/pods/pod-migration-protocol.md (adopted 2026-08-16, plate A-111)
  [ REQD] make-before-break: 3 audio link(s) change. Generate and VERIFY the replacement clip before the old one is touched — never the other way round. Deletion never precedes a verified replacement.
         → docs/architecture/AUDIO_PIPELINE_ARCHITECTURE.md §6b
  [ info] learner-cache-invalidated: course_practice_phrases carries touch_course_content_stamp, so this edit bumps courses.content_stamp (currently Mon Aug 17 2026 12:37:35 GMT+0000 (Coordinated Universal Time)). Learner script caches for eng_for_sin invalidate on the next read.
  [ info] learner-reach: courses.new_app_status = "beta". This course is reachable by learners — the blast radius is live.


══════════════════════════════════════════════════════════════════════════════
  eng_for_sin  ·  course_practice_phrases  ·  phrase eng_for_sin:S0380L03B03  [proposed]
══════════════════════════════════════════════════════════════════════════════
  known_text:
      was  "මොකක්ද කියලා මම ඇහුවා ගොඩ"
      now  "මොකක්ද කියලා මම ඇයගෙන් ඇහුවා ගොඩ"
  target_text:
      was  "I asked what a lot"
      now  "I asked her what a lot"

┌─ DECISION ─────────────────────────────────────────────────────────────────
│  PROCEED-WITH-REPAIRS
│  Safe to apply, but it is not finished when the text lands — 6 follow-up action(s) are required to leave the course whole.
│
│  because:
│    · known_audio_id becomes NULL — a silent slot until new audio is rendered.
│    · target1_audio_id becomes NULL — a silent slot until new audio is rendered.
│    · target2_audio_id becomes NULL — a silent slot until new audio is rendered.
│    · 2 other row(s) carry the identical old text and share its clip. Editing one leaves the course inconsistent.
│
│  before this edit is finished, you must:
│    1. Queue an audio pass so known_audio_id is refilled: node tools/course-optimization/queue-audio-pass.cjs eng_for_sin --reason "<pass>". Do NOT run TTS directly.
│    2. Queue an audio pass so target1_audio_id is refilled: node tools/course-optimization/queue-audio-pass.cjs eng_for_sin --reason "<pass>". Do NOT run TTS directly.
│    3. Queue an audio pass so target2_audio_id is refilled: node tools/course-optimization/queue-audio-pass.cjs eng_for_sin --reason "<pass>". Do NOT run TTS directly.
│    4. Decide explicitly whether the other 2 row(s) should change too — include them in this proposal, or say why not.
│    5. If this text appears in a pod, migrate learner progress FIRST — docs/pods/pod-migration-protocol.md. Never edit a live pod in place.
│    6. ≈3 clip(s) will need rendering. TTS costs money — show a plan and get approval; end the pass by QUEUEING, never by running TTS.
└────────────────────────────────────────────────────────────────────────────

── VERDICT ──────────────────────────────────────────────────────────────────
  [ WARN] SILENT SLOT (known_audio_id): no existing clip matches the new text, so the trigger sets this to NULL. The slot is silent until Generate Missing Audio runs.
  [ WARN] SILENT SLOT (target1_audio_id): no existing clip matches the new text, so the trigger sets this to NULL. The slot is silent until Generate Missing Audio runs.
  [ WARN] SILENT SLOT (target2_audio_id): no existing clip matches the new text, so the trigger sets this to NULL. The slot is silent until Generate Missing Audio runs.
  [ WARN] SAME TEXT ELSEWHERE: 2 other row(s) in this course carry the identical old text. Editing one and not the others leaves the course inconsistent — and they share the same clip.

── TRIGGER ──────────────────────────────────────────────────────────────────
  relink trigger: trg_null_phrase_audio_on_text_change
  trg_null_phrase_audio_on_text_change fires BEFORE UPDATE and REPLACES the audio link with audio_id_for_text(course, <new text>, role) — which is NULL when nothing matches (silent slot) and an EXISTING clip when something does (silent re-point, possibly to another voice).

── AUDIO LINKS ──────────────────────────────────────────────────────────────
  known_audio_id         nulled-silent
      now:  d64c3a26-5764-468f-ab9b-9e92b0455f8f  [azure_si-LK-SameeraNeural] "මොකක්ද කියලා මම ඇහුවා ගොඩ"
      after: NULL (silent)  
  target1_audio_id       nulled-silent
      now:  94b2e075-2a1d-45c1-a3b2-53a1448aac9e  [xai_bedd6226] "I asked what a lot"
      after: NULL (silent)  
  target2_audio_id       nulled-silent
      now:  a3002c81-0005-4c21-906d-2d3b88089eea  [xai_gfzdpspr5fdp] "I asked what a lot"
      after: NULL (silent)  

── DERIVED ──────────────────────────────────────────────────────────────────
  note: A phrase has no derived rows; its blast radius is its own audio plus the course-wide checks below.

── COURSE-WIDE ──────────────────────────────────────────────────────────────
  tiling: not at risk — No vocab unit removed — nothing can stop tiling.
  same known text elsewhere (1): phrase 380/eng_for_
  same target text elsewhere (1): phrase 380/eng_for_

── TTS ──────────────────────────────────────────────────────────────────────
  ≈ 3 clip(s) would need rendering (3 row + 0 presentation). NOTHING WAS RENDERED.
  ESTIMATE ONLY. This tool never renders. Content passes end by QUEUEING an audio pass (tools/course-optimization/queue-audio-pass.cjs), never by running TTS.

── DOCTRINE ─────────────────────────────────────────────────────────────────
  [CHECK] pod-content-migration: eng_for_sin has 1 pod(s). If this edit touches text that a pod sentence carries, learner progress MUST be migrated — progress is filed under a slot, not the text, so an in-place edit silently credits a learner with a sentence they never heard.
         → docs/pods/pod-migration-protocol.md (adopted 2026-08-16, plate A-111)
  [ REQD] make-before-break: 3 audio link(s) change. Generate and VERIFY the replacement clip before the old one is touched — never the other way round. Deletion never precedes a verified replacement.
         → docs/architecture/AUDIO_PIPELINE_ARCHITECTURE.md §6b
  [ info] learner-cache-invalidated: course_practice_phrases carries touch_course_content_stamp, so this edit bumps courses.content_stamp (currently Mon Aug 17 2026 12:37:35 GMT+0000 (Coordinated Universal Time)). Learner script caches for eng_for_sin invalidate on the next read.
  [ info] learner-reach: courses.new_app_status = "beta". This course is reachable by learners — the blast radius is live.


══════════════════════════════════════════════════════════════════════════════
  eng_for_sin  ·  course_practice_phrases  ·  phrase eng_for_sin:S0380L03U01  [proposed]
══════════════════════════════════════════════════════════════════════════════
  known_text:
      was  "ඇය ඇතුළු කරන්නට ඕනේ කළේ මොකක්ද කියලා මම ඇහුවා"
      now  "ඇය ඇතුළු කරන්නට ඕනේ කළේ මොකක්ද කියලා මම ඇයගෙන් ඇහුවා"
  target_text:
      was  "I asked what she wanted to include"
      now  "I asked her what she wanted to include"

┌─ DECISION ─────────────────────────────────────────────────────────────────
│  PROCEED-WITH-REPAIRS
│  Safe to apply, but it is not finished when the text lands — 7 follow-up action(s) are required to leave the course whole.
│
│  because:
│    · known_audio_id becomes NULL — a silent slot until new audio is rendered.
│    · target1_audio_id becomes NULL — a silent slot until new audio is rendered.
│    · target2_audio_id becomes NULL — a silent slot until new audio is rendered.
│    · 2 intro clip(s) embed the old text and will keep speaking it.
│    · 4 other row(s) carry the identical old text and share its clip. Editing one leaves the course inconsistent.
│
│  before this edit is finished, you must:
│    1. Queue an audio pass so known_audio_id is refilled: node tools/course-optimization/queue-audio-pass.cjs eng_for_sin --reason "<pass>". Do NOT run TTS directly.
│    2. Queue an audio pass so target1_audio_id is refilled: node tools/course-optimization/queue-audio-pass.cjs eng_for_sin --reason "<pass>". Do NOT run TTS directly.
│    3. Queue an audio pass so target2_audio_id is refilled: node tools/course-optimization/queue-audio-pass.cjs eng_for_sin --reason "<pass>". Do NOT run TTS directly.
│    4. Re-compose 2 presentation clip(s) (POST /regenerate-presentations, which self-scopes to missing), then queue the audio pass.
│    5. Decide explicitly whether the other 4 row(s) should change too — include them in this proposal, or say why not.
│    6. If this text appears in a pod, migrate learner progress FIRST — docs/pods/pod-migration-protocol.md. Never edit a live pod in place.
│    7. ≈5 clip(s) will need rendering. TTS costs money — show a plan and get approval; end the pass by QUEUEING, never by running TTS.
└────────────────────────────────────────────────────────────────────────────

── VERDICT ──────────────────────────────────────────────────────────────────
  [ WARN] SILENT SLOT (known_audio_id): no existing clip matches the new text, so the trigger sets this to NULL. The slot is silent until Generate Missing Audio runs.
  [ WARN] SILENT SLOT (target1_audio_id): no existing clip matches the new text, so the trigger sets this to NULL. The slot is silent until Generate Missing Audio runs.
  [ WARN] SILENT SLOT (target2_audio_id): no existing clip matches the new text, so the trigger sets this to NULL. The slot is silent until Generate Missing Audio runs.
  [ WARN] SAME TEXT ELSEWHERE: 4 other row(s) in this course carry the identical old text. Editing one and not the others leaves the course inconsistent — and they share the same clip.
  [DANGER] PRESENTATIONS STALE: 2 intro clip(s) embed the OLD text and will keep speaking it. Presentations are per-LEGO and must be re-composed; the presentation text is built at render time, so this tool reports the clips, not the replacement wording.

── TRIGGER ──────────────────────────────────────────────────────────────────
  relink trigger: trg_null_phrase_audio_on_text_change
  trg_null_phrase_audio_on_text_change fires BEFORE UPDATE and REPLACES the audio link with audio_id_for_text(course, <new text>, role) — which is NULL when nothing matches (silent slot) and an EXISTING clip when something does (silent re-point, possibly to another voice).

── AUDIO LINKS ──────────────────────────────────────────────────────────────
  known_audio_id         nulled-silent
      now:  d8cf428e-f614-458b-8dcc-84bdccee8313  [azure_si-LK-SameeraNeural] "ඇය ඇතුළු කරන්නට ඕනේ කළේ මොකක්ද කියලා මම ඇහුවා."
      after: NULL (silent)  
  target1_audio_id       nulled-silent
      now:  a1d66785-2b8c-4e4b-b952-1dbddc777921  [bedd6226] "I asked what she wanted to include."
      after: NULL (silent)  
  target2_audio_id       nulled-silent
      now:  1793eced-b673-4c40-8263-79940cdad9ae  [xai_gfzdpspr5fdp] "I asked what she wanted to include."
      after: NULL (silent)  

── DERIVED ──────────────────────────────────────────────────────────────────
  note: A phrase has no derived rows; its blast radius is its own audio plus the course-wide checks below.

── COURSE-WIDE ──────────────────────────────────────────────────────────────
  tiling: not at risk — No vocab unit removed — nothing can stop tiling.
  same known text elsewhere (2): seed 380, phrase 380/eng_for_
  same target text elsewhere (2): seed 380, phrase 380/eng_for_

── PRESENTATIONS ────────────────────────────────────────────────────────────
  2 intro clip(s) embed the old text:
      d8406136-75ef-4800-b792-cb4c381b4a62 lego=S0380L01 [azure_si-LK-SameeraNeural] "ඉංග්‍රීසිෙන්. 'ඇතුළු කරන්නට'. 'ඇය ඇතුළු කරන්නට ඕනේ කළේ ම"
      d4130d95-307d-4743-998e-cb23294d5975 lego=S0380L03 [azure_si-LK-SameeraNeural] "ඉංග්‍රීසිෙන්. 'මොකක්ද කියලා මම ඇහුවා'. 'ඇය ඇතුළු කරන්නට "

── TTS ──────────────────────────────────────────────────────────────────────
  ≈ 5 clip(s) would need rendering (3 row + 2 presentation). NOTHING WAS RENDERED.
  ESTIMATE ONLY. This tool never renders. Content passes end by QUEUEING an audio pass (tools/course-optimization/queue-audio-pass.cjs), never by running TTS.

── DOCTRINE ─────────────────────────────────────────────────────────────────
  [CHECK] pod-content-migration: eng_for_sin has 1 pod(s). If this edit touches text that a pod sentence carries, learner progress MUST be migrated — progress is filed under a slot, not the text, so an in-place edit silently credits a learner with a sentence they never heard.
         → docs/pods/pod-migration-protocol.md (adopted 2026-08-16, plate A-111)
  [ REQD] make-before-break: 3 audio link(s) change. Generate and VERIFY the replacement clip before the old one is touched — never the other way round. Deletion never precedes a verified replacement.
         → docs/architecture/AUDIO_PIPELINE_ARCHITECTURE.md §6b
  [ info] learner-cache-invalidated: course_practice_phrases carries touch_course_content_stamp, so this edit bumps courses.content_stamp (currently Mon Aug 17 2026 12:37:35 GMT+0000 (Coordinated Universal Time)). Learner script caches for eng_for_sin invalidate on the next read.
  [ info] learner-reach: courses.new_app_status = "beta". This course is reachable by learners — the blast radius is live.


══════════════════════════════════════════════════════════════════════════════
  eng_for_sin  ·  course_practice_phrases  ·  phrase eng_for_sin:S0380L03U02  [proposed]
══════════════════════════════════════════════════════════════════════════════
  known_text:
      was  "ඇය ඕනේ කළේ මොකක්ද කියලා මම ඇහුවා"
      now  "ඇය ඕනේ කළේ මොකක්ද කියලා මම ඇයගෙන් ඇහුවා"
  target_text:
      was  "I asked what she wanted to"
      now  "I asked her what she wanted to"

┌─ DECISION ─────────────────────────────────────────────────────────────────
│  PROCEED-WITH-REPAIRS
│  Safe to apply, but it is not finished when the text lands — 6 follow-up action(s) are required to leave the course whole.
│
│  because:
│    · known_audio_id becomes NULL — a silent slot until new audio is rendered.
│    · target1_audio_id becomes NULL — a silent slot until new audio is rendered.
│    · target2_audio_id becomes NULL — a silent slot until new audio is rendered.
│    · 2 other row(s) carry the identical old text and share its clip. Editing one leaves the course inconsistent.
│
│  before this edit is finished, you must:
│    1. Queue an audio pass so known_audio_id is refilled: node tools/course-optimization/queue-audio-pass.cjs eng_for_sin --reason "<pass>". Do NOT run TTS directly.
│    2. Queue an audio pass so target1_audio_id is refilled: node tools/course-optimization/queue-audio-pass.cjs eng_for_sin --reason "<pass>". Do NOT run TTS directly.
│    3. Queue an audio pass so target2_audio_id is refilled: node tools/course-optimization/queue-audio-pass.cjs eng_for_sin --reason "<pass>". Do NOT run TTS directly.
│    4. Decide explicitly whether the other 2 row(s) should change too — include them in this proposal, or say why not.
│    5. If this text appears in a pod, migrate learner progress FIRST — docs/pods/pod-migration-protocol.md. Never edit a live pod in place.
│    6. ≈3 clip(s) will need rendering. TTS costs money — show a plan and get approval; end the pass by QUEUEING, never by running TTS.
└────────────────────────────────────────────────────────────────────────────

── VERDICT ──────────────────────────────────────────────────────────────────
  [ WARN] SILENT SLOT (known_audio_id): no existing clip matches the new text, so the trigger sets this to NULL. The slot is silent until Generate Missing Audio runs.
  [ WARN] SILENT SLOT (target1_audio_id): no existing clip matches the new text, so the trigger sets this to NULL. The slot is silent until Generate Missing Audio runs.
  [ WARN] SILENT SLOT (target2_audio_id): no existing clip matches the new text, so the trigger sets this to NULL. The slot is silent until Generate Missing Audio runs.
  [ WARN] SAME TEXT ELSEWHERE: 2 other row(s) in this course carry the identical old text. Editing one and not the others leaves the course inconsistent — and they share the same clip.

── TRIGGER ──────────────────────────────────────────────────────────────────
  relink trigger: trg_null_phrase_audio_on_text_change
  trg_null_phrase_audio_on_text_change fires BEFORE UPDATE and REPLACES the audio link with audio_id_for_text(course, <new text>, role) — which is NULL when nothing matches (silent slot) and an EXISTING clip when something does (silent re-point, possibly to another voice).

── AUDIO LINKS ──────────────────────────────────────────────────────────────
  known_audio_id         nulled-silent
      now:  cb2963a8-ebf4-4d36-b5f1-3a75fcfaf724  [azure_si-LK-SameeraNeural] "ඇය ඕනේ කළේ මොකක්ද කියලා මම ඇහුවා"
      after: NULL (silent)  
  target1_audio_id       nulled-silent
      now:  db5efcf3-2d2f-4681-9e9b-9809ae733987  [xai_bedd6226] "I asked what she wanted to"
      after: NULL (silent)  
  target2_audio_id       nulled-silent
      now:  bb52e5c8-4c1d-4a69-b63b-bca6a33a6b80  [xai_gfzdpspr5fdp] "I asked what she wanted to"
      after: NULL (silent)  

── DERIVED ──────────────────────────────────────────────────────────────────
  note: A phrase has no derived rows; its blast radius is its own audio plus the course-wide checks below.

── COURSE-WIDE ──────────────────────────────────────────────────────────────
  tiling: not at risk — No vocab unit removed — nothing can stop tiling.
  same known text elsewhere (1): phrase 380/eng_for_
  same target text elsewhere (1): phrase 380/eng_for_

── TTS ──────────────────────────────────────────────────────────────────────
  ≈ 3 clip(s) would need rendering (3 row + 0 presentation). NOTHING WAS RENDERED.
  ESTIMATE ONLY. This tool never renders. Content passes end by QUEUEING an audio pass (tools/course-optimization/queue-audio-pass.cjs), never by running TTS.

── DOCTRINE ─────────────────────────────────────────────────────────────────
  [CHECK] pod-content-migration: eng_for_sin has 1 pod(s). If this edit touches text that a pod sentence carries, learner progress MUST be migrated — progress is filed under a slot, not the text, so an in-place edit silently credits a learner with a sentence they never heard.
         → docs/pods/pod-migration-protocol.md (adopted 2026-08-16, plate A-111)
  [ REQD] make-before-break: 3 audio link(s) change. Generate and VERIFY the replacement clip before the old one is touched — never the other way round. Deletion never precedes a verified replacement.
         → docs/architecture/AUDIO_PIPELINE_ARCHITECTURE.md §6b
  [ info] learner-cache-invalidated: course_practice_phrases carries touch_course_content_stamp, so this edit bumps courses.content_stamp (currently Mon Aug 17 2026 12:37:35 GMT+0000 (Coordinated Universal Time)). Learner script caches for eng_for_sin invalidate on the next read.
  [ info] learner-reach: courses.new_app_status = "beta". This course is reachable by learners — the blast radius is live.


══════════════════════════════════════════════════════════════════════════════
  eng_for_sin  ·  course_practice_phrases  ·  phrase eng_for_sin:S0380L03U03  [proposed]
══════════════════════════════════════════════════════════════════════════════
  known_text:
      was  "ඔහුට ලැබෙන්නේ නෑ මොකක්ද කියලා මම ඇහුවා"
      now  "ඔහුට ලැබෙන්නේ නෑ මොකක්ද කියලා මම ඇයගෙන් ඇහුවා"
  target_text:
      was  "I asked what he can't have"
      now  "I asked her what he can't have"

┌─ DECISION ─────────────────────────────────────────────────────────────────
│  PROCEED-WITH-REPAIRS
│  Safe to apply, but it is not finished when the text lands — 6 follow-up action(s) are required to leave the course whole.
│
│  because:
│    · known_audio_id becomes NULL — a silent slot until new audio is rendered.
│    · target1_audio_id becomes NULL — a silent slot until new audio is rendered.
│    · target2_audio_id becomes NULL — a silent slot until new audio is rendered.
│    · 2 other row(s) carry the identical old text and share its clip. Editing one leaves the course inconsistent.
│
│  before this edit is finished, you must:
│    1. Queue an audio pass so known_audio_id is refilled: node tools/course-optimization/queue-audio-pass.cjs eng_for_sin --reason "<pass>". Do NOT run TTS directly.
│    2. Queue an audio pass so target1_audio_id is refilled: node tools/course-optimization/queue-audio-pass.cjs eng_for_sin --reason "<pass>". Do NOT run TTS directly.
│    3. Queue an audio pass so target2_audio_id is refilled: node tools/course-optimization/queue-audio-pass.cjs eng_for_sin --reason "<pass>". Do NOT run TTS directly.
│    4. Decide explicitly whether the other 2 row(s) should change too — include them in this proposal, or say why not.
│    5. If this text appears in a pod, migrate learner progress FIRST — docs/pods/pod-migration-protocol.md. Never edit a live pod in place.
│    6. ≈3 clip(s) will need rendering. TTS costs money — show a plan and get approval; end the pass by QUEUEING, never by running TTS.
└────────────────────────────────────────────────────────────────────────────

── VERDICT ──────────────────────────────────────────────────────────────────
  [ WARN] SILENT SLOT (known_audio_id): no existing clip matches the new text, so the trigger sets this to NULL. The slot is silent until Generate Missing Audio runs.
  [ WARN] SILENT SLOT (target1_audio_id): no existing clip matches the new text, so the trigger sets this to NULL. The slot is silent until Generate Missing Audio runs.
  [ WARN] SILENT SLOT (target2_audio_id): no existing clip matches the new text, so the trigger sets this to NULL. The slot is silent until Generate Missing Audio runs.
  [ WARN] SAME TEXT ELSEWHERE: 2 other row(s) in this course carry the identical old text. Editing one and not the others leaves the course inconsistent — and they share the same clip.

── TRIGGER ──────────────────────────────────────────────────────────────────
  relink trigger: trg_null_phrase_audio_on_text_change
  trg_null_phrase_audio_on_text_change fires BEFORE UPDATE and REPLACES the audio link with audio_id_for_text(course, <new text>, role) — which is NULL when nothing matches (silent slot) and an EXISTING clip when something does (silent re-point, possibly to another voice).

── AUDIO LINKS ──────────────────────────────────────────────────────────────
  known_audio_id         nulled-silent
      now:  59dfec39-d76f-4d94-868a-f4cfedf96bd9  [azure_si-LK-SameeraNeural] "ඔහුට ලැබෙන්නේ නෑ මොකක්ද කියලා මම ඇහුවා"
      after: NULL (silent)  
  target1_audio_id       nulled-silent
      now:  cdbe9518-9c37-428f-92a5-b22b9ebd4ff3  [xai_bedd6226] "I asked what he can't have"
      after: NULL (silent)  
  target2_audio_id       nulled-silent
      now:  8e769b25-cfce-476c-b661-e70a799e2fdf  [xai_gfzdpspr5fdp] "I asked what he can't have"
      after: NULL (silent)  

── DERIVED ──────────────────────────────────────────────────────────────────
  note: A phrase has no derived rows; its blast radius is its own audio plus the course-wide checks below.

── COURSE-WIDE ──────────────────────────────────────────────────────────────
  tiling: not at risk — No vocab unit removed — nothing can stop tiling.
  same known text elsewhere (1): phrase 380/eng_for_
  same target text elsewhere (1): phrase 380/eng_for_

── TTS ──────────────────────────────────────────────────────────────────────
  ≈ 3 clip(s) would need rendering (3 row + 0 presentation). NOTHING WAS RENDERED.
  ESTIMATE ONLY. This tool never renders. Content passes end by QUEUEING an audio pass (tools/course-optimization/queue-audio-pass.cjs), never by running TTS.

── DOCTRINE ─────────────────────────────────────────────────────────────────
  [CHECK] pod-content-migration: eng_for_sin has 1 pod(s). If this edit touches text that a pod sentence carries, learner progress MUST be migrated — progress is filed under a slot, not the text, so an in-place edit silently credits a learner with a sentence they never heard.
         → docs/pods/pod-migration-protocol.md (adopted 2026-08-16, plate A-111)
  [ REQD] make-before-break: 3 audio link(s) change. Generate and VERIFY the replacement clip before the old one is touched — never the other way round. Deletion never precedes a verified replacement.
         → docs/architecture/AUDIO_PIPELINE_ARCHITECTURE.md §6b
  [ info] learner-cache-invalidated: course_practice_phrases carries touch_course_content_stamp, so this edit bumps courses.content_stamp (currently Mon Aug 17 2026 12:37:35 GMT+0000 (Coordinated Universal Time)). Learner script caches for eng_for_sin invalidate on the next read.
  [ info] learner-reach: courses.new_app_status = "beta". This course is reachable by learners — the blast radius is live.


══════════════════════════════════════════════════════════════════════════════
  eng_for_sin  ·  course_practice_phrases  ·  phrase eng_for_sin:S0380L03U04  [proposed]
══════════════════════════════════════════════════════════════════════════════
  known_text:
      was  "ඔහු ඕනේ කළේ මොකක්ද කියලා මම ඇහුවා"
      now  "ඔහු ඕනේ කළේ මොකක්ද කියලා මම ඇයගෙන් ඇහුවා"
  target_text:
      was  "I asked what he wanted"
      now  "I asked her what he wanted"

┌─ DECISION ─────────────────────────────────────────────────────────────────
│  PROCEED-WITH-REPAIRS
│  Safe to apply, but it is not finished when the text lands — 6 follow-up action(s) are required to leave the course whole.
│
│  because:
│    · known_audio_id becomes NULL — a silent slot until new audio is rendered.
│    · target1_audio_id becomes NULL — a silent slot until new audio is rendered.
│    · target2_audio_id becomes NULL — a silent slot until new audio is rendered.
│    · 2 other row(s) carry the identical old text and share its clip. Editing one leaves the course inconsistent.
│
│  before this edit is finished, you must:
│    1. Queue an audio pass so known_audio_id is refilled: node tools/course-optimization/queue-audio-pass.cjs eng_for_sin --reason "<pass>". Do NOT run TTS directly.
│    2. Queue an audio pass so target1_audio_id is refilled: node tools/course-optimization/queue-audio-pass.cjs eng_for_sin --reason "<pass>". Do NOT run TTS directly.
│    3. Queue an audio pass so target2_audio_id is refilled: node tools/course-optimization/queue-audio-pass.cjs eng_for_sin --reason "<pass>". Do NOT run TTS directly.
│    4. Decide explicitly whether the other 2 row(s) should change too — include them in this proposal, or say why not.
│    5. If this text appears in a pod, migrate learner progress FIRST — docs/pods/pod-migration-protocol.md. Never edit a live pod in place.
│    6. ≈3 clip(s) will need rendering. TTS costs money — show a plan and get approval; end the pass by QUEUEING, never by running TTS.
└────────────────────────────────────────────────────────────────────────────

── VERDICT ──────────────────────────────────────────────────────────────────
  [ WARN] SILENT SLOT (known_audio_id): no existing clip matches the new text, so the trigger sets this to NULL. The slot is silent until Generate Missing Audio runs.
  [ WARN] SILENT SLOT (target1_audio_id): no existing clip matches the new text, so the trigger sets this to NULL. The slot is silent until Generate Missing Audio runs.
  [ WARN] SILENT SLOT (target2_audio_id): no existing clip matches the new text, so the trigger sets this to NULL. The slot is silent until Generate Missing Audio runs.
  [ WARN] SAME TEXT ELSEWHERE: 2 other row(s) in this course carry the identical old text. Editing one and not the others leaves the course inconsistent — and they share the same clip.

── TRIGGER ──────────────────────────────────────────────────────────────────
  relink trigger: trg_null_phrase_audio_on_text_change
  trg_null_phrase_audio_on_text_change fires BEFORE UPDATE and REPLACES the audio link with audio_id_for_text(course, <new text>, role) — which is NULL when nothing matches (silent slot) and an EXISTING clip when something does (silent re-point, possibly to another voice).

── AUDIO LINKS ──────────────────────────────────────────────────────────────
  known_audio_id         nulled-silent
      now:  5bd00449-b6e3-4466-81a8-6c4157330b13  [azure_si-LK-SameeraNeural] "ඔහු ඕනේ කළේ මොකක්ද කියලා මම ඇහුවා"
      after: NULL (silent)  
  target1_audio_id       nulled-silent
      now:  9443f2f3-cded-4e7b-88b5-e301948b869e  [xai_bedd6226] "I asked what he wanted"
      after: NULL (silent)  
  target2_audio_id       nulled-silent
      now:  d3b83590-5cf5-4439-b443-a64302e1edf5  [xai_gfzdpspr5fdp] "I asked what he wanted"
      after: NULL (silent)  

── DERIVED ──────────────────────────────────────────────────────────────────
  note: A phrase has no derived rows; its blast radius is its own audio plus the course-wide checks below.

── COURSE-WIDE ──────────────────────────────────────────────────────────────
  tiling: not at risk — No vocab unit removed — nothing can stop tiling.
  same known text elsewhere (1): phrase 380/eng_for_
  same target text elsewhere (1): phrase 380/eng_for_

── TTS ──────────────────────────────────────────────────────────────────────
  ≈ 3 clip(s) would need rendering (3 row + 0 presentation). NOTHING WAS RENDERED.
  ESTIMATE ONLY. This tool never renders. Content passes end by QUEUEING an audio pass (tools/course-optimization/queue-audio-pass.cjs), never by running TTS.

── DOCTRINE ─────────────────────────────────────────────────────────────────
  [CHECK] pod-content-migration: eng_for_sin has 1 pod(s). If this edit touches text that a pod sentence carries, learner progress MUST be migrated — progress is filed under a slot, not the text, so an in-place edit silently credits a learner with a sentence they never heard.
         → docs/pods/pod-migration-protocol.md (adopted 2026-08-16, plate A-111)
  [ REQD] make-before-break: 3 audio link(s) change. Generate and VERIFY the replacement clip before the old one is touched — never the other way round. Deletion never precedes a verified replacement.
         → docs/architecture/AUDIO_PIPELINE_ARCHITECTURE.md §6b
  [ info] learner-cache-invalidated: course_practice_phrases carries touch_course_content_stamp, so this edit bumps courses.content_stamp (currently Mon Aug 17 2026 12:37:35 GMT+0000 (Coordinated Universal Time)). Learner script caches for eng_for_sin invalidate on the next read.
  [ info] learner-reach: courses.new_app_status = "beta". This course is reachable by learners — the blast radius is live.


══════════════════════════════════════════════════════════════════════════════
  eng_for_sin  ·  course_practice_phrases  ·  phrase eng_for_sin:S0380L03U05  [proposed]
══════════════════════════════════════════════════════════════════════════════
  known_text:
      was  "ඒ අය ඕනේ කළේ මොකක්ද කියලා මම ඇහුවා"
      now  "ඒ අය ඕනේ කළේ මොකක්ද කියලා මම ඇයගෙන් ඇහුවා"
  target_text:
      was  "I asked what they wanted"
      now  "I asked her what they wanted"

┌─ DECISION ─────────────────────────────────────────────────────────────────
│  PROCEED-WITH-REPAIRS
│  Safe to apply, but it is not finished when the text lands — 6 follow-up action(s) are required to leave the course whole.
│
│  because:
│    · known_audio_id becomes NULL — a silent slot until new audio is rendered.
│    · target1_audio_id becomes NULL — a silent slot until new audio is rendered.
│    · target2_audio_id becomes NULL — a silent slot until new audio is rendered.
│    · 2 other row(s) carry the identical old text and share its clip. Editing one leaves the course inconsistent.
│
│  before this edit is finished, you must:
│    1. Queue an audio pass so known_audio_id is refilled: node tools/course-optimization/queue-audio-pass.cjs eng_for_sin --reason "<pass>". Do NOT run TTS directly.
│    2. Queue an audio pass so target1_audio_id is refilled: node tools/course-optimization/queue-audio-pass.cjs eng_for_sin --reason "<pass>". Do NOT run TTS directly.
│    3. Queue an audio pass so target2_audio_id is refilled: node tools/course-optimization/queue-audio-pass.cjs eng_for_sin --reason "<pass>". Do NOT run TTS directly.
│    4. Decide explicitly whether the other 2 row(s) should change too — include them in this proposal, or say why not.
│    5. If this text appears in a pod, migrate learner progress FIRST — docs/pods/pod-migration-protocol.md. Never edit a live pod in place.
│    6. ≈3 clip(s) will need rendering. TTS costs money — show a plan and get approval; end the pass by QUEUEING, never by running TTS.
└────────────────────────────────────────────────────────────────────────────

── VERDICT ──────────────────────────────────────────────────────────────────
  [ WARN] SILENT SLOT (known_audio_id): no existing clip matches the new text, so the trigger sets this to NULL. The slot is silent until Generate Missing Audio runs.
  [ WARN] SILENT SLOT (target1_audio_id): no existing clip matches the new text, so the trigger sets this to NULL. The slot is silent until Generate Missing Audio runs.
  [ WARN] SILENT SLOT (target2_audio_id): no existing clip matches the new text, so the trigger sets this to NULL. The slot is silent until Generate Missing Audio runs.
  [ WARN] SAME TEXT ELSEWHERE: 2 other row(s) in this course carry the identical old text. Editing one and not the others leaves the course inconsistent — and they share the same clip.

── TRIGGER ──────────────────────────────────────────────────────────────────
  relink trigger: trg_null_phrase_audio_on_text_change
  trg_null_phrase_audio_on_text_change fires BEFORE UPDATE and REPLACES the audio link with audio_id_for_text(course, <new text>, role) — which is NULL when nothing matches (silent slot) and an EXISTING clip when something does (silent re-point, possibly to another voice).

── AUDIO LINKS ──────────────────────────────────────────────────────────────
  known_audio_id         nulled-silent
      now:  ae7ad5ac-e345-4b8d-b45b-1ce03977bf9b  [azure_si-LK-SameeraNeural] "ඒ අය ඕනේ කළේ මොකක්ද කියලා මම ඇහුවා"
      after: NULL (silent)  
  target1_audio_id       nulled-silent
      now:  bc18af3e-c8db-4364-8b30-b1a8859f3107  [xai_bedd6226] "I asked what they wanted"
      after: NULL (silent)  
  target2_audio_id       nulled-silent
      now:  b0eea334-a95d-4263-9cf2-5cc2ad8eb8ef  [xai_gfzdpspr5fdp] "I asked what they wanted"
      after: NULL (silent)  

── DERIVED ──────────────────────────────────────────────────────────────────
  note: A phrase has no derived rows; its blast radius is its own audio plus the course-wide checks below.

── COURSE-WIDE ──────────────────────────────────────────────────────────────
  tiling: not at risk — No vocab unit removed — nothing can stop tiling.
  same known text elsewhere (1): phrase 380/eng_for_
  same target text elsewhere (1): phrase 380/eng_for_

── TTS ──────────────────────────────────────────────────────────────────────
  ≈ 3 clip(s) would need rendering (3 row + 0 presentation). NOTHING WAS RENDERED.
  ESTIMATE ONLY. This tool never renders. Content passes end by QUEUEING an audio pass (tools/course-optimization/queue-audio-pass.cjs), never by running TTS.

── DOCTRINE ─────────────────────────────────────────────────────────────────
  [CHECK] pod-content-migration: eng_for_sin has 1 pod(s). If this edit touches text that a pod sentence carries, learner progress MUST be migrated — progress is filed under a slot, not the text, so an in-place edit silently credits a learner with a sentence they never heard.
         → docs/pods/pod-migration-protocol.md (adopted 2026-08-16, plate A-111)
  [ REQD] make-before-break: 3 audio link(s) change. Generate and VERIFY the replacement clip before the old one is touched — never the other way round. Deletion never precedes a verified replacement.
         → docs/architecture/AUDIO_PIPELINE_ARCHITECTURE.md §6b
  [ info] learner-cache-invalidated: course_practice_phrases carries touch_course_content_stamp, so this edit bumps courses.content_stamp (currently Mon Aug 17 2026 12:37:35 GMT+0000 (Coordinated Universal Time)). Learner script caches for eng_for_sin invalidate on the next read.
  [ info] learner-reach: courses.new_app_status = "beta". This course is reachable by learners — the blast radius is live.


══════════════════════════════════════════════════════════════════════════════
  eng_for_sin  ·  course_seeds  ·  seed 381  [proposed]
══════════════════════════════════════════════════════════════════════════════
  known_text:
      was  "ඔහු අපිව ගෙන ආවොත් ඕනේ කළාද කියලා මම ඇහුවේ නෑ."
      now  "ඔහු අපිව ගෙන ආවොත් ඕනේ කළාද කියලා මම ඔහුගෙන් ඇහුවේ නෑ."
  target_text:
      was  "I didn't ask if he wanted to follow us."
      now  "I didn't ask him if he wanted to follow us."

┌─ DECISION ─────────────────────────────────────────────────────────────────
│  RECONSIDER
│  Reconsider this edit as written: it damages work beyond itself. Revise it, narrow it, or accept the listed cost as a deliberate decision.
│
│  because:
│    · The new seed target does not tile from this seed's existing LEGOs, so the breakdown must be rebuilt — this is not a text-only edit.
│    · known_audio_id will be left pointing at the OLD text with no trigger and no alarm.
│    · target1_audio_id will be left pointing at the OLD text with no trigger and no alarm.
│    · target2_audio_id will be left pointing at the OLD text with no trigger and no alarm.
│    · 2 intro clip(s) embed the old text and will keep speaking it.
│    · 1 other row(s) carry the identical old text and share its clip. Editing one leaves the course inconsistent.
│
│  before this edit is finished, you must:
│    1. Produce a new LEGO breakdown and apply it through POST /api/course/:code/edit-cascade (which snapshots and rolls back on a bad breakdown) rather than by direct UPDATE.
│    2. No clip exists for the new text yet. NULL known_audio_id so audio_autolink binds the new clip when it is rendered, then queue an audio pass (tools/course-optimization/queue-audio-pass.cjs).
│    3. No clip exists for the new text yet. NULL target1_audio_id so audio_autolink binds the new clip when it is rendered, then queue an audio pass (tools/course-optimization/queue-audio-pass.cjs).
│    4. No clip exists for the new text yet. NULL target2_audio_id so audio_autolink binds the new clip when it is rendered, then queue an audio pass (tools/course-optimization/queue-audio-pass.cjs).
│    5. Re-compose 2 presentation clip(s) (POST /regenerate-presentations, which self-scopes to missing), then queue the audio pass.
│    6. Decide explicitly whether the other 1 row(s) should change too — include them in this proposal, or say why not.
│    7. If this text appears in a pod, migrate learner progress FIRST — docs/pods/pod-migration-protocol.md. Never edit a live pod in place.
│    8. ≈5 clip(s) will need rendering. TTS costs money — show a plan and get approval; end the pass by QUEUEING, never by running TTS.
└────────────────────────────────────────────────────────────────────────────

── VERDICT ──────────────────────────────────────────────────────────────────
  [DANGER] SILENT STALE AUDIO: course_seeds has no audio-nulling trigger. After this edit the learner keeps hearing the OLD text with nothing to signal it. You must repoint the link by hand, or generate the new clip and NULL the link first so audio_autolink can bind it.
  [DANGER] STALE LINK (known_audio_id): still points at d81a3055-300a-439e-999e-b37a8b4b556d, which speaks "ඔහු අපිව ගෙන ආවොත් ඕනේ කළාද කියලා මම ඇහුවේ නෑ." — the OLD text. No clip exists for the new text yet. NULL known_audio_id so audio_autolink binds the new clip when it is rendered, then queue an audio pass (tools/course-optimization/queue-audio-pass.cjs).
  [DANGER] STALE LINK (target1_audio_id): still points at a872bf67-177c-4a94-8b90-4d0af0cbf5d5, which speaks "I didn't ask if he wanted to follow us." — the OLD text. No clip exists for the new text yet. NULL target1_audio_id so audio_autolink binds the new clip when it is rendered, then queue an audio pass (tools/course-optimization/queue-audio-pass.cjs).
  [DANGER] STALE LINK (target2_audio_id): still points at d1268275-fe71-4cd9-bc68-f075af2044e9, which speaks "I didn't ask if he wanted to follow us." — the OLD text. No clip exists for the new text yet. NULL target2_audio_id so audio_autolink binds the new clip when it is rendered, then queue an audio pass (tools/course-optimization/queue-audio-pass.cjs).
  [ WARN] RE-DECOMPOSITION NEEDED: seed 381's 3 LEGO(s) no longer tile its new target text.
  [ WARN] SAME TEXT ELSEWHERE: 1 other row(s) in this course carry the identical old text. Editing one and not the others leaves the course inconsistent — and they share the same clip.
  [DANGER] PRESENTATIONS STALE: 2 intro clip(s) embed the OLD text and will keep speaking it. Presentations are per-LEGO and must be re-composed; the presentation text is built at render time, so this tool reports the clips, not the replacement wording.

── TRIGGER ──────────────────────────────────────────────────────────────────
  relink trigger: NONE
  NO nulling/relink trigger on course_seeds. The audio link is left EXACTLY AS IT WAS, still pointing at the clip for the OLD text. No NULL, no orphan, no alarm — a missing-audio sweep cannot see it and audio_autolink cannot rescue it (it only fills links that are already NULL).

── AUDIO LINKS ──────────────────────────────────────────────────────────────
  known_audio_id         left-stale
      now:  d81a3055-300a-439e-999e-b37a8b4b556d  [azure_si-LK-SameeraNeural] "ඔහු අපිව ගෙන ආවොත් ඕනේ කළාද කියලා මම ඇහුවේ නෑ."
      after:d81a3055-300a-439e-999e-b37a8b4b556d  [azure_si-LK-SameeraNeural] "ඔහු අපිව ගෙන ආවොත් ඕනේ කළාද කියලා මම ඇහුවේ නෑ."
      →  No clip exists for the new text yet. NULL known_audio_id so audio_autolink binds the new clip when it is rendered, then queue an audio pass (tools/course-optimization/queue-audio-pass.cjs).
  target1_audio_id       left-stale
      now:  a872bf67-177c-4a94-8b90-4d0af0cbf5d5  [bedd6226] "I didn't ask if he wanted to follow us."
      after:a872bf67-177c-4a94-8b90-4d0af0cbf5d5  [bedd6226] "I didn't ask if he wanted to follow us."
      →  No clip exists for the new text yet. NULL target1_audio_id so audio_autolink binds the new clip when it is rendered, then queue an audio pass (tools/course-optimization/queue-audio-pass.cjs).
  target2_audio_id       left-stale
      now:  d1268275-fe71-4cd9-bc68-f075af2044e9  [gfzdpspr5fdp] "I didn't ask if he wanted to follow us."
      after:d1268275-fe71-4cd9-bc68-f075af2044e9  [gfzdpspr5fdp] "I didn't ask if he wanted to follow us."
      →  No clip exists for the new text yet. NULL target2_audio_id so audio_autolink binds the new clip when it is rendered, then queue an audio pass (tools/course-optimization/queue-audio-pass.cjs).

── DERIVED ──────────────────────────────────────────────────────────────────
  legos: 3
  phrases: 29
  legos:
      L1 (M)  "අපිව ගෙන ආවොත්" → "to follow us"
      L2 (M)  "ඔහු ... ඕනේ කළාද" → "he wanted"
      L3 (M)  "කියලා මම ඇහුවේ නෑ" → "I didn't hear if"
  edit_case: vocab-changing (Case 2) — the new target does NOT tile from this seed's existing LEGOs; a re-decomposition is required
  note: Re-decompose this seed (POST /api/course/:code/edit-cascade with the new legos, or /v2/decompose) — the LEGOs below no longer cover the seed text.

── COURSE-WIDE ──────────────────────────────────────────────────────────────
  tiling: not at risk — No vocab unit removed — nothing can stop tiling.
  same known text elsewhere (1): phrase 381/eng_for_

── PRESENTATIONS ────────────────────────────────────────────────────────────
  2 intro clip(s) embed the old text:
      48668c58-545b-4516-9b4b-9f396f720c40 lego=S0381L01 [azure_si-LK-SameeraNeural] "ඉංග්‍රීසිෙන්. 'අපිව ගෙන ආවොත්'. 'ඔහු අපිව ගෙන ආවොත් ඕනේ "
      6e5ae983-b283-4ffc-9cd7-4ce7b6dc141d lego=S0381L03 [azure_si-LK-SameeraNeural] "ඉංග්‍රීසිෙන්. 'කියලා මම ඇහුවේ නෑ'. 'ඔහු අපිව ගෙන ආවොත් ඕ"

── TTS ──────────────────────────────────────────────────────────────────────
  ≈ 5 clip(s) would need rendering (3 row + 2 presentation). NOTHING WAS RENDERED.
  ESTIMATE ONLY. This tool never renders. Content passes end by QUEUEING an audio pass (tools/course-optimization/queue-audio-pass.cjs), never by running TTS.

── DOCTRINE ─────────────────────────────────────────────────────────────────
  [CHECK] pod-content-migration: eng_for_sin has 1 pod(s). If this edit touches text that a pod sentence carries, learner progress MUST be migrated — progress is filed under a slot, not the text, so an in-place edit silently credits a learner with a sentence they never heard.
         → docs/pods/pod-migration-protocol.md (adopted 2026-08-16, plate A-111)
  [ REQD] make-before-break: 3 audio link(s) change. Generate and VERIFY the replacement clip before the old one is touched — never the other way round. Deletion never precedes a verified replacement.
         → docs/architecture/AUDIO_PIPELINE_ARCHITECTURE.md §6b
  [ info] learner-cache-invalidated: course_seeds carries touch_course_content_stamp, so this edit bumps courses.content_stamp (currently Mon Aug 17 2026 12:37:35 GMT+0000 (Coordinated Universal Time)). Learner script caches for eng_for_sin invalidate on the next read.
  [ info] learner-reach: courses.new_app_status = "beta". This course is reachable by learners — the blast radius is live.


══════════════════════════════════════════════════════════════════════════════
  eng_for_sin  ·  course_legos  ·  lego 381:3  [proposed]
══════════════════════════════════════════════════════════════════════════════
  known_text:
      was  "කියලා මම ඇහුවේ නෑ"
      now  "කියලා මම ඔහුගෙන් ඇහුවේ නෑ"
  target_text:
      was  "I didn't hear if"
      now  "I didn't ask him if"

┌─ DECISION ─────────────────────────────────────────────────────────────────
│  RECONSIDER
│  Reconsider this edit as written: it damages work beyond itself. Revise it, narrow it, or accept the listed cost as a deliberate decision.
│
│  because:
│    · 135 phrase(s) across 47 seed(s) stop tiling once this edit removes "i didn't hear if". Those phrases are other people's finished work.
│    · 8 of this LEGO's own build/use phrases no longer contain its target text, so the LEGO stops being what those phrases teach.
│    · known_audio_id becomes NULL — a silent slot until new audio is rendered.
│    · target1_audio_id becomes NULL — a silent slot until new audio is rendered.
│    · target2_audio_id becomes NULL — a silent slot until new audio is rendered.
│    · presentation_audio_id becomes NULL — a silent slot until new audio is rendered.
│    · 2 intro clip(s) embed the old text and will keep speaking it.
│    · 2 other row(s) carry the identical old text and share its clip. Editing one leaves the course inconsistent.
│
│  before this edit is finished, you must:
│    1. Either keep the removed chunk(s) in the new text, or accept re-authoring 135 phrase(s) (seeds 396, 403, 410, 412, 427, 441, 485, 489, 490, 492, +37 more).
│    2. Re-author 8 phrase(s) for this LEGO, or choose a new text those phrases still contain.
│    3. Queue an audio pass so known_audio_id is refilled: node tools/course-optimization/queue-audio-pass.cjs eng_for_sin --reason "<pass>". Do NOT run TTS directly.
│    4. Queue an audio pass so target1_audio_id is refilled: node tools/course-optimization/queue-audio-pass.cjs eng_for_sin --reason "<pass>". Do NOT run TTS directly.
│    5. Queue an audio pass so target2_audio_id is refilled: node tools/course-optimization/queue-audio-pass.cjs eng_for_sin --reason "<pass>". Do NOT run TTS directly.
│    6. Queue an audio pass so presentation_audio_id is refilled: node tools/course-optimization/queue-audio-pass.cjs eng_for_sin --reason "<pass>". Do NOT run TTS directly.
│    7. Re-compose 2 presentation clip(s) (POST /regenerate-presentations, which self-scopes to missing), then queue the audio pass.
│    8. Decide explicitly whether the other 2 row(s) should change too — include them in this proposal, or say why not.
│    9. If this text appears in a pod, migrate learner progress FIRST — docs/pods/pod-migration-protocol.md. Never edit a live pod in place.
│    10. ≈5 clip(s) will need rendering. TTS costs money — show a plan and get approval; end the pass by QUEUEING, never by running TTS.
└────────────────────────────────────────────────────────────────────────────

── VERDICT ──────────────────────────────────────────────────────────────────
  [ WARN] SILENT SLOT (known_audio_id): no existing clip matches the new text, so the trigger sets this to NULL. The slot is silent until Generate Missing Audio runs.
  [ WARN] SILENT SLOT (target1_audio_id): no existing clip matches the new text, so the trigger sets this to NULL. The slot is silent until Generate Missing Audio runs.
  [ WARN] SILENT SLOT (target2_audio_id): no existing clip matches the new text, so the trigger sets this to NULL. The slot is silent until Generate Missing Audio runs.
  [ WARN] SILENT SLOT (presentation_audio_id): no existing clip matches the new text, so the trigger sets this to NULL. The slot is silent until Generate Missing Audio runs.
  [DANGER] CONTAINMENT BROKEN: 8 of this LEGO's own phrase(s) no longer contain its new target text.
  [DANGER] COURSE-WIDE BREAKAGE: 135 phrase(s) elsewhere in the course tile through a chunk this edit removes and will no longer validate.
  [ WARN] SAME TEXT ELSEWHERE: 2 other row(s) in this course carry the identical old text. Editing one and not the others leaves the course inconsistent — and they share the same clip.
  [DANGER] PRESENTATIONS STALE: 2 intro clip(s) embed the OLD text and will keep speaking it. Presentations are per-LEGO and must be re-composed; the presentation text is built at render time, so this tool reports the clips, not the replacement wording.

── TRIGGER ──────────────────────────────────────────────────────────────────
  relink trigger: trg_null_lego_audio_on_text_change
  trg_null_lego_audio_on_text_change fires BEFORE UPDATE and REPLACES the audio link with audio_id_for_text(course, <new text>, role) — which is NULL when nothing matches (silent slot) and an EXISTING clip when something does (silent re-point, possibly to another voice).

── AUDIO LINKS ──────────────────────────────────────────────────────────────
  known_audio_id         nulled-silent
      now:  325026a9-0921-4b3d-9c67-54a112d8eaa4  [azure_si-LK-SameeraNeural] "කියලා මම ඇහුවේ නෑ"
      after: NULL (silent)  
  target1_audio_id       nulled-silent
      now:  70f27f44-a586-4f2f-8f08-7714ecc9af6d  [xai_bedd6226] "I didn't hear if"
      after: NULL (silent)  
  target2_audio_id       nulled-silent
      now:  a834f27a-c1f5-409f-a060-3043ea781a66  [xai_gfzdpspr5fdp] "I didn't hear if"
      after: NULL (silent)  
  presentation_audio_id  nulled-silent
      now:  6e5ae983-b283-4ffc-9cd7-4ce7b6dc141d  [azure_si-LK-SameeraNeural] "ඉංග්‍රීසිෙන්. 'කියලා මම ඇහුවේ නෑ'. 'ඔහු අපිව ගෙන"
      after: NULL (silent)  

── DERIVED ──────────────────────────────────────────────────────────────────
  phrases: 10
  containment failures:
      build eng_for_sin:S0381L03B01  "I didn't hear if"
      build eng_for_sin:S0381L03B02  "I didn't hear if a lot"
      build eng_for_sin:S0381L03B03  "I didn't hear if he wanted"
      use eng_for_sin:S0381L03U01  "I didn't hear if he wanted to follow us"
      use eng_for_sin:S0381L03U02  "I didn't hear if he can't have it"
      use eng_for_sin:S0381L03U03  "I didn't hear if she was rather quiet"
      use eng_for_sin:S0381L03U04  "I didn't hear if they had something important"
      use eng_for_sin:S0381L03U05  "I didn't hear if what she was doing"
  note: A LEGO is the teaching unit; its own build/use phrases must still contain its target text.

── COURSE-WIDE ──────────────────────────────────────────────────────────────
  tiling: 135 phrase(s) break of 4077 re-tiled  (chunks removed: i didn't hear if)
      seed 396 L2 build: "we need to stand until everybody else is ready"  ✗ "stand until everybody else is ready"
      seed 396 L2 use: "I think we need to stand until everybody else is ready"  ✗ "stand until everybody else is ready"
      seed 403 L1 build: "to remain quiet for as long as possible"  ✗ "remain quiet for as long as possible"
      seed 403 L1 build: "we need to stand for as long as possible"  ✗ "stand for as long as possible"
      seed 403 L1 build: "I want to remain quiet for as long as possible"  ✗ "remain quiet for as long as possible"
      seed 403 L1 use: "he wants to remain quiet for as long as possible"  ✗ "remain quiet for as long as possible"
      seed 403 L1 use: "I don't want to remain quiet for as long as possible"  ✗ "remain quiet for as long as possible"
      seed 403 L1 use: "they want to remain quiet for as long as possible"  ✗ "remain quiet for as long as possible"
      seed 403 L1 use: "I think we should stand for as long as possible"  ✗ "stand for as long as possible"
      seed 403 L1 use: "do we need to stand for as long as possible?"  ✗ "stand for as long as possible"
      seed 410 L1 build: "fight with each other"  ✗ "fight with each other"
      seed 410 L1 build: "they fight with each other"  ✗ "fight with each other"
      seed 410 L1 build: "he said they fight with each other"  ✗ "fight with each other"
      seed 410 L1 use: "I think they fight with each other"  ✗ "fight with each other"
      seed 410 L1 use: "she said they fight with each other"  ✗ "fight with each other"
      … and 120 more (full list in the JSON report)
  same known text elsewhere (1): phrase 381/eng_for_
  same target text elsewhere (1): phrase 381/eng_for_

── PRESENTATIONS ────────────────────────────────────────────────────────────
  2 intro clip(s) embed the old text:
      48668c58-545b-4516-9b4b-9f396f720c40 lego=S0381L01 [azure_si-LK-SameeraNeural] "ඉංග්‍රීසිෙන්. 'අපිව ගෙන ආවොත්'. 'ඔහු අපිව ගෙන ආවොත් ඕනේ "
      6e5ae983-b283-4ffc-9cd7-4ce7b6dc141d lego=S0381L03 [azure_si-LK-SameeraNeural] "ඉංග්‍රීසිෙන්. 'කියලා මම ඇහුවේ නෑ'. 'ඔහු අපිව ගෙන ආවොත් ඕ"

── TTS ──────────────────────────────────────────────────────────────────────
  ≈ 5 clip(s) would need rendering (3 row + 2 presentation). NOTHING WAS RENDERED.
  ESTIMATE ONLY. This tool never renders. Content passes end by QUEUEING an audio pass (tools/course-optimization/queue-audio-pass.cjs), never by running TTS.

── DOCTRINE ─────────────────────────────────────────────────────────────────
  [CHECK] pod-content-migration: eng_for_sin has 1 pod(s). If this edit touches text that a pod sentence carries, learner progress MUST be migrated — progress is filed under a slot, not the text, so an in-place edit silently credits a learner with a sentence they never heard.
         → docs/pods/pod-migration-protocol.md (adopted 2026-08-16, plate A-111)
  [ REQD] make-before-break: 4 audio link(s) change. Generate and VERIFY the replacement clip before the old one is touched — never the other way round. Deletion never precedes a verified replacement.
         → docs/architecture/AUDIO_PIPELINE_ARCHITECTURE.md §6b
  [ info] learner-cache-invalidated: course_legos carries touch_course_content_stamp, so this edit bumps courses.content_stamp (currently Mon Aug 17 2026 12:37:35 GMT+0000 (Coordinated Universal Time)). Learner script caches for eng_for_sin invalidate on the next read.
  [ info] learner-reach: courses.new_app_status = "beta". This course is reachable by learners — the blast radius is live.


══════════════════════════════════════════════════════════════════════════════
  eng_for_sin  ·  course_practice_phrases  ·  phrase eng_for_sin:S0381L03C02  [proposed]
══════════════════════════════════════════════════════════════════════════════
  known_text:
      was  "මම ඇහුවේ නෑ"
      now  "මම ඔහුගෙන් ඇහුවේ නෑ"
  target_text:
      was  "I didn't hear"
      now  "I didn't ask him"

┌─ DECISION ─────────────────────────────────────────────────────────────────
│  PROCEED-WITH-REPAIRS
│  Safe to apply, but it is not finished when the text lands — 7 follow-up action(s) are required to leave the course whole.
│
│  because:
│    · known_audio_id becomes NULL — a silent slot until new audio is rendered.
│    · target1_audio_id becomes NULL — a silent slot until new audio is rendered.
│    · target2_audio_id becomes NULL — a silent slot until new audio is rendered.
│    · 4 intro clip(s) embed the old text and will keep speaking it.
│    · 6 other row(s) carry the identical old text and share its clip. Editing one leaves the course inconsistent.
│
│  before this edit is finished, you must:
│    1. Queue an audio pass so known_audio_id is refilled: node tools/course-optimization/queue-audio-pass.cjs eng_for_sin --reason "<pass>". Do NOT run TTS directly.
│    2. Queue an audio pass so target1_audio_id is refilled: node tools/course-optimization/queue-audio-pass.cjs eng_for_sin --reason "<pass>". Do NOT run TTS directly.
│    3. Queue an audio pass so target2_audio_id is refilled: node tools/course-optimization/queue-audio-pass.cjs eng_for_sin --reason "<pass>". Do NOT run TTS directly.
│    4. Re-compose 4 presentation clip(s) (POST /regenerate-presentations, which self-scopes to missing), then queue the audio pass.
│    5. Decide explicitly whether the other 6 row(s) should change too — include them in this proposal, or say why not.
│    6. If this text appears in a pod, migrate learner progress FIRST — docs/pods/pod-migration-protocol.md. Never edit a live pod in place.
│    7. ≈7 clip(s) will need rendering. TTS costs money — show a plan and get approval; end the pass by QUEUEING, never by running TTS.
└────────────────────────────────────────────────────────────────────────────

── VERDICT ──────────────────────────────────────────────────────────────────
  [ WARN] SILENT SLOT (known_audio_id): no existing clip matches the new text, so the trigger sets this to NULL. The slot is silent until Generate Missing Audio runs.
  [ WARN] SILENT SLOT (target1_audio_id): no existing clip matches the new text, so the trigger sets this to NULL. The slot is silent until Generate Missing Audio runs.
  [ WARN] SILENT SLOT (target2_audio_id): no existing clip matches the new text, so the trigger sets this to NULL. The slot is silent until Generate Missing Audio runs.
  [ WARN] SAME TEXT ELSEWHERE: 6 other row(s) in this course carry the identical old text. Editing one and not the others leaves the course inconsistent — and they share the same clip.
  [DANGER] PRESENTATIONS STALE: 4 intro clip(s) embed the OLD text and will keep speaking it. Presentations are per-LEGO and must be re-composed; the presentation text is built at render time, so this tool reports the clips, not the replacement wording.

── TRIGGER ──────────────────────────────────────────────────────────────────
  relink trigger: trg_null_phrase_audio_on_text_change
  trg_null_phrase_audio_on_text_change fires BEFORE UPDATE and REPLACES the audio link with audio_id_for_text(course, <new text>, role) — which is NULL when nothing matches (silent slot) and an EXISTING clip when something does (silent re-point, possibly to another voice).

── AUDIO LINKS ──────────────────────────────────────────────────────────────
  known_audio_id         nulled-silent
      now:  e16b82a6-30c6-40cc-965d-12d2a0736668  [azure_si-LK-SameeraNeural] "මම ඇහුවේ නෑ"
      after: NULL (silent)  
  target1_audio_id       nulled-silent
      now:  e03e724f-afd1-40a2-b4a4-2d9ec1657ff4  [xai_bedd6226] "I didn't hear"
      after: NULL (silent)  
  target2_audio_id       nulled-silent
      now:  44e36bce-d56d-46bf-b261-7f5c01e21cf6  [xai_gfzdpspr5fdp] "I didn't hear"
      after: NULL (silent)  

── DERIVED ──────────────────────────────────────────────────────────────────
  note: A phrase has no derived rows; its blast radius is its own audio plus the course-wide checks below.

── COURSE-WIDE ──────────────────────────────────────────────────────────────
  tiling: not at risk — No vocab unit removed — nothing can stop tiling.
  same known text elsewhere (3): lego 365:2, phrase 365/eng_for_, phrase 381/eng_for_
  same target text elsewhere (3): lego 365:2, phrase 365/eng_for_, phrase 381/eng_for_

── PRESENTATIONS ────────────────────────────────────────────────────────────
  4 intro clip(s) embed the old text:
      d4de90a6-a633-4a74-a4c0-30f83526343f lego=S0365L01 [azure_si-LK-SameeraNeural] "ඉංග්‍රීසිෙන්. 'ඇය ඔහු ලවා කිව්ව දේ'. 'ඇය ඔහු ලවා කිව්ව ද"
      48668c58-545b-4516-9b4b-9f396f720c40 lego=S0381L01 [azure_si-LK-SameeraNeural] "ඉංග්‍රීසිෙන්. 'අපිව ගෙන ආවොත්'. 'ඔහු අපිව ගෙන ආවොත් ඕනේ "
      6e5ae983-b283-4ffc-9cd7-4ce7b6dc141d lego=S0381L03 [azure_si-LK-SameeraNeural] "ඉංග්‍රීසිෙන්. 'කියලා මම ඇහුවේ නෑ'. 'ඔහු අපිව ගෙන ආවොත් ඕ"
      11e5c0d4-41b2-4764-a3f4-8ef7e4481e14 (component) [azure_si-LK-SameeraNeural] "ඉංග්‍රීසිෙන්. 'මම ඇහුවේ නෑ'. '' ඉතින්. :"

── TTS ──────────────────────────────────────────────────────────────────────
  ≈ 7 clip(s) would need rendering (3 row + 4 presentation). NOTHING WAS RENDERED.
  ESTIMATE ONLY. This tool never renders. Content passes end by QUEUEING an audio pass (tools/course-optimization/queue-audio-pass.cjs), never by running TTS.

── DOCTRINE ─────────────────────────────────────────────────────────────────
  [CHECK] pod-content-migration: eng_for_sin has 1 pod(s). If this edit touches text that a pod sentence carries, learner progress MUST be migrated — progress is filed under a slot, not the text, so an in-place edit silently credits a learner with a sentence they never heard.
         → docs/pods/pod-migration-protocol.md (adopted 2026-08-16, plate A-111)
  [ REQD] make-before-break: 3 audio link(s) change. Generate and VERIFY the replacement clip before the old one is touched — never the other way round. Deletion never precedes a verified replacement.
         → docs/architecture/AUDIO_PIPELINE_ARCHITECTURE.md §6b
  [ info] learner-cache-invalidated: course_practice_phrases carries touch_course_content_stamp, so this edit bumps courses.content_stamp (currently Mon Aug 17 2026 12:37:35 GMT+0000 (Coordinated Universal Time)). Learner script caches for eng_for_sin invalidate on the next read.
  [ info] learner-reach: courses.new_app_status = "beta". This course is reachable by learners — the blast radius is live.


══════════════════════════════════════════════════════════════════════════════
  eng_for_sin  ·  course_practice_phrases  ·  phrase eng_for_sin:S0381L03B01  [proposed]
══════════════════════════════════════════════════════════════════════════════
  known_text:
      was  "කියලා මම ඇහුවේ නෑ"
      now  "කියලා මම ඔහුගෙන් ඇහුවේ නෑ"
  target_text:
      was  "I didn't hear if"
      now  "I didn't ask him if"

┌─ DECISION ─────────────────────────────────────────────────────────────────
│  PROCEED-WITH-REPAIRS
│  Safe to apply, but it is not finished when the text lands — 7 follow-up action(s) are required to leave the course whole.
│
│  because:
│    · known_audio_id becomes NULL — a silent slot until new audio is rendered.
│    · target1_audio_id becomes NULL — a silent slot until new audio is rendered.
│    · target2_audio_id becomes NULL — a silent slot until new audio is rendered.
│    · 2 intro clip(s) embed the old text and will keep speaking it.
│    · 4 other row(s) carry the identical old text and share its clip. Editing one leaves the course inconsistent.
│
│  before this edit is finished, you must:
│    1. Queue an audio pass so known_audio_id is refilled: node tools/course-optimization/queue-audio-pass.cjs eng_for_sin --reason "<pass>". Do NOT run TTS directly.
│    2. Queue an audio pass so target1_audio_id is refilled: node tools/course-optimization/queue-audio-pass.cjs eng_for_sin --reason "<pass>". Do NOT run TTS directly.
│    3. Queue an audio pass so target2_audio_id is refilled: node tools/course-optimization/queue-audio-pass.cjs eng_for_sin --reason "<pass>". Do NOT run TTS directly.
│    4. Re-compose 2 presentation clip(s) (POST /regenerate-presentations, which self-scopes to missing), then queue the audio pass.
│    5. Decide explicitly whether the other 4 row(s) should change too — include them in this proposal, or say why not.
│    6. If this text appears in a pod, migrate learner progress FIRST — docs/pods/pod-migration-protocol.md. Never edit a live pod in place.
│    7. ≈5 clip(s) will need rendering. TTS costs money — show a plan and get approval; end the pass by QUEUEING, never by running TTS.
└────────────────────────────────────────────────────────────────────────────

── VERDICT ──────────────────────────────────────────────────────────────────
  [ WARN] SILENT SLOT (known_audio_id): no existing clip matches the new text, so the trigger sets this to NULL. The slot is silent until Generate Missing Audio runs.
  [ WARN] SILENT SLOT (target1_audio_id): no existing clip matches the new text, so the trigger sets this to NULL. The slot is silent until Generate Missing Audio runs.
  [ WARN] SILENT SLOT (target2_audio_id): no existing clip matches the new text, so the trigger sets this to NULL. The slot is silent until Generate Missing Audio runs.
  [ WARN] SAME TEXT ELSEWHERE: 4 other row(s) in this course carry the identical old text. Editing one and not the others leaves the course inconsistent — and they share the same clip.
  [DANGER] PRESENTATIONS STALE: 2 intro clip(s) embed the OLD text and will keep speaking it. Presentations are per-LEGO and must be re-composed; the presentation text is built at render time, so this tool reports the clips, not the replacement wording.

── TRIGGER ──────────────────────────────────────────────────────────────────
  relink trigger: trg_null_phrase_audio_on_text_change
  trg_null_phrase_audio_on_text_change fires BEFORE UPDATE and REPLACES the audio link with audio_id_for_text(course, <new text>, role) — which is NULL when nothing matches (silent slot) and an EXISTING clip when something does (silent re-point, possibly to another voice).

── AUDIO LINKS ──────────────────────────────────────────────────────────────
  known_audio_id         nulled-silent
      now:  325026a9-0921-4b3d-9c67-54a112d8eaa4  [azure_si-LK-SameeraNeural] "කියලා මම ඇහුවේ නෑ"
      after: NULL (silent)  
  target1_audio_id       nulled-silent
      now:  70f27f44-a586-4f2f-8f08-7714ecc9af6d  [xai_bedd6226] "I didn't hear if"
      after: NULL (silent)  
  target2_audio_id       nulled-silent
      now:  a834f27a-c1f5-409f-a060-3043ea781a66  [xai_gfzdpspr5fdp] "I didn't hear if"
      after: NULL (silent)  

── DERIVED ──────────────────────────────────────────────────────────────────
  note: A phrase has no derived rows; its blast radius is its own audio plus the course-wide checks below.

── COURSE-WIDE ──────────────────────────────────────────────────────────────
  tiling: not at risk — No vocab unit removed — nothing can stop tiling.
  same known text elsewhere (2): lego 381:3, phrase 381/eng_for_
  same target text elsewhere (2): lego 381:3, phrase 381/eng_for_

── PRESENTATIONS ────────────────────────────────────────────────────────────
  2 intro clip(s) embed the old text:
      48668c58-545b-4516-9b4b-9f396f720c40 lego=S0381L01 [azure_si-LK-SameeraNeural] "ඉංග්‍රීසිෙන්. 'අපිව ගෙන ආවොත්'. 'ඔහු අපිව ගෙන ආවොත් ඕනේ "
      6e5ae983-b283-4ffc-9cd7-4ce7b6dc141d lego=S0381L03 [azure_si-LK-SameeraNeural] "ඉංග්‍රීසිෙන්. 'කියලා මම ඇහුවේ නෑ'. 'ඔහු අපිව ගෙන ආවොත් ඕ"

── TTS ──────────────────────────────────────────────────────────────────────
  ≈ 5 clip(s) would need rendering (3 row + 2 presentation). NOTHING WAS RENDERED.
  ESTIMATE ONLY. This tool never renders. Content passes end by QUEUEING an audio pass (tools/course-optimization/queue-audio-pass.cjs), never by running TTS.

── DOCTRINE ─────────────────────────────────────────────────────────────────
  [CHECK] pod-content-migration: eng_for_sin has 1 pod(s). If this edit touches text that a pod sentence carries, learner progress MUST be migrated — progress is filed under a slot, not the text, so an in-place edit silently credits a learner with a sentence they never heard.
         → docs/pods/pod-migration-protocol.md (adopted 2026-08-16, plate A-111)
  [ REQD] make-before-break: 3 audio link(s) change. Generate and VERIFY the replacement clip before the old one is touched — never the other way round. Deletion never precedes a verified replacement.
         → docs/architecture/AUDIO_PIPELINE_ARCHITECTURE.md §6b
  [ info] learner-cache-invalidated: course_practice_phrases carries touch_course_content_stamp, so this edit bumps courses.content_stamp (currently Mon Aug 17 2026 12:37:35 GMT+0000 (Coordinated Universal Time)). Learner script caches for eng_for_sin invalidate on the next read.
  [ info] learner-reach: courses.new_app_status = "beta". This course is reachable by learners — the blast radius is live.


══════════════════════════════════════════════════════════════════════════════
  eng_for_sin  ·  course_practice_phrases  ·  phrase eng_for_sin:S0381L03B02  [proposed]
══════════════════════════════════════════════════════════════════════════════
  known_text:
      was  "මම ඇහුවේ නෑ කියලා ගොඩ"
      now  "මම ඔහුගෙන් ඇහුවේ නෑ කියලා ගොඩ"
  target_text:
      was  "I didn't hear if a lot"
      now  "I didn't ask him if a lot"

┌─ DECISION ─────────────────────────────────────────────────────────────────
│  PROCEED-WITH-REPAIRS
│  Safe to apply, but it is not finished when the text lands — 6 follow-up action(s) are required to leave the course whole.
│
│  because:
│    · known_audio_id becomes NULL — a silent slot until new audio is rendered.
│    · target1_audio_id becomes NULL — a silent slot until new audio is rendered.
│    · target2_audio_id becomes NULL — a silent slot until new audio is rendered.
│    · 2 other row(s) carry the identical old text and share its clip. Editing one leaves the course inconsistent.
│
│  before this edit is finished, you must:
│    1. Queue an audio pass so known_audio_id is refilled: node tools/course-optimization/queue-audio-pass.cjs eng_for_sin --reason "<pass>". Do NOT run TTS directly.
│    2. Queue an audio pass so target1_audio_id is refilled: node tools/course-optimization/queue-audio-pass.cjs eng_for_sin --reason "<pass>". Do NOT run TTS directly.
│    3. Queue an audio pass so target2_audio_id is refilled: node tools/course-optimization/queue-audio-pass.cjs eng_for_sin --reason "<pass>". Do NOT run TTS directly.
│    4. Decide explicitly whether the other 2 row(s) should change too — include them in this proposal, or say why not.
│    5. If this text appears in a pod, migrate learner progress FIRST — docs/pods/pod-migration-protocol.md. Never edit a live pod in place.
│    6. ≈3 clip(s) will need rendering. TTS costs money — show a plan and get approval; end the pass by QUEUEING, never by running TTS.
└────────────────────────────────────────────────────────────────────────────

── VERDICT ──────────────────────────────────────────────────────────────────
  [ WARN] SILENT SLOT (known_audio_id): no existing clip matches the new text, so the trigger sets this to NULL. The slot is silent until Generate Missing Audio runs.
  [ WARN] SILENT SLOT (target1_audio_id): no existing clip matches the new text, so the trigger sets this to NULL. The slot is silent until Generate Missing Audio runs.
  [ WARN] SILENT SLOT (target2_audio_id): no existing clip matches the new text, so the trigger sets this to NULL. The slot is silent until Generate Missing Audio runs.
  [ WARN] SAME TEXT ELSEWHERE: 2 other row(s) in this course carry the identical old text. Editing one and not the others leaves the course inconsistent — and they share the same clip.

── TRIGGER ──────────────────────────────────────────────────────────────────
  relink trigger: trg_null_phrase_audio_on_text_change
  trg_null_phrase_audio_on_text_change fires BEFORE UPDATE and REPLACES the audio link with audio_id_for_text(course, <new text>, role) — which is NULL when nothing matches (silent slot) and an EXISTING clip when something does (silent re-point, possibly to another voice).

── AUDIO LINKS ──────────────────────────────────────────────────────────────
  known_audio_id         nulled-silent
      now:  bfe67b35-04dd-4209-be85-16c8bc3f8487  [azure_si-LK-SameeraNeural] "මම ඇහුවේ නෑ කියලා ගොඩ"
      after: NULL (silent)  
  target1_audio_id       nulled-silent
      now:  4254dd46-f67d-4a53-85bb-a584f68b5a6b  [xai_bedd6226] "I didn't hear if a lot"
      after: NULL (silent)  
  target2_audio_id       nulled-silent
      now:  f67b4f6b-8e57-424e-8655-eaf2f6392c3c  [xai_gfzdpspr5fdp] "I didn't hear if a lot"
      after: NULL (silent)  

── DERIVED ──────────────────────────────────────────────────────────────────
  note: A phrase has no derived rows; its blast radius is its own audio plus the course-wide checks below.

── COURSE-WIDE ──────────────────────────────────────────────────────────────
  tiling: not at risk — No vocab unit removed — nothing can stop tiling.
  same known text elsewhere (1): phrase 381/eng_for_
  same target text elsewhere (1): phrase 381/eng_for_

── TTS ──────────────────────────────────────────────────────────────────────
  ≈ 3 clip(s) would need rendering (3 row + 0 presentation). NOTHING WAS RENDERED.
  ESTIMATE ONLY. This tool never renders. Content passes end by QUEUEING an audio pass (tools/course-optimization/queue-audio-pass.cjs), never by running TTS.

── DOCTRINE ─────────────────────────────────────────────────────────────────
  [CHECK] pod-content-migration: eng_for_sin has 1 pod(s). If this edit touches text that a pod sentence carries, learner progress MUST be migrated — progress is filed under a slot, not the text, so an in-place edit silently credits a learner with a sentence they never heard.
         → docs/pods/pod-migration-protocol.md (adopted 2026-08-16, plate A-111)
  [ REQD] make-before-break: 3 audio link(s) change. Generate and VERIFY the replacement clip before the old one is touched — never the other way round. Deletion never precedes a verified replacement.
         → docs/architecture/AUDIO_PIPELINE_ARCHITECTURE.md §6b
  [ info] learner-cache-invalidated: course_practice_phrases carries touch_course_content_stamp, so this edit bumps courses.content_stamp (currently Mon Aug 17 2026 12:37:35 GMT+0000 (Coordinated Universal Time)). Learner script caches for eng_for_sin invalidate on the next read.
  [ info] learner-reach: courses.new_app_status = "beta". This course is reachable by learners — the blast radius is live.


══════════════════════════════════════════════════════════════════════════════
  eng_for_sin  ·  course_practice_phrases  ·  phrase eng_for_sin:S0381L03B03  [proposed]
══════════════════════════════════════════════════════════════════════════════
  known_text:
      was  "ඔහු ඕනේ කළාද කියලා මම ඇහුවේ නෑ"
      now  "ඔහු ඕනේ කළාද කියලා මම ඔහුගෙන් ඇහුවේ නෑ"
  target_text:
      was  "I didn't hear if he wanted"
      now  "I didn't ask him if he wanted"

┌─ DECISION ─────────────────────────────────────────────────────────────────
│  PROCEED-WITH-REPAIRS
│  Safe to apply, but it is not finished when the text lands — 6 follow-up action(s) are required to leave the course whole.
│
│  because:
│    · known_audio_id becomes NULL — a silent slot until new audio is rendered.
│    · target1_audio_id becomes NULL — a silent slot until new audio is rendered.
│    · target2_audio_id becomes NULL — a silent slot until new audio is rendered.
│    · 2 other row(s) carry the identical old text and share its clip. Editing one leaves the course inconsistent.
│
│  before this edit is finished, you must:
│    1. Queue an audio pass so known_audio_id is refilled: node tools/course-optimization/queue-audio-pass.cjs eng_for_sin --reason "<pass>". Do NOT run TTS directly.
│    2. Queue an audio pass so target1_audio_id is refilled: node tools/course-optimization/queue-audio-pass.cjs eng_for_sin --reason "<pass>". Do NOT run TTS directly.
│    3. Queue an audio pass so target2_audio_id is refilled: node tools/course-optimization/queue-audio-pass.cjs eng_for_sin --reason "<pass>". Do NOT run TTS directly.
│    4. Decide explicitly whether the other 2 row(s) should change too — include them in this proposal, or say why not.
│    5. If this text appears in a pod, migrate learner progress FIRST — docs/pods/pod-migration-protocol.md. Never edit a live pod in place.
│    6. ≈3 clip(s) will need rendering. TTS costs money — show a plan and get approval; end the pass by QUEUEING, never by running TTS.
└────────────────────────────────────────────────────────────────────────────

── VERDICT ──────────────────────────────────────────────────────────────────
  [ WARN] SILENT SLOT (known_audio_id): no existing clip matches the new text, so the trigger sets this to NULL. The slot is silent until Generate Missing Audio runs.
  [ WARN] SILENT SLOT (target1_audio_id): no existing clip matches the new text, so the trigger sets this to NULL. The slot is silent until Generate Missing Audio runs.
  [ WARN] SILENT SLOT (target2_audio_id): no existing clip matches the new text, so the trigger sets this to NULL. The slot is silent until Generate Missing Audio runs.
  [ WARN] SAME TEXT ELSEWHERE: 2 other row(s) in this course carry the identical old text. Editing one and not the others leaves the course inconsistent — and they share the same clip.

── TRIGGER ──────────────────────────────────────────────────────────────────
  relink trigger: trg_null_phrase_audio_on_text_change
  trg_null_phrase_audio_on_text_change fires BEFORE UPDATE and REPLACES the audio link with audio_id_for_text(course, <new text>, role) — which is NULL when nothing matches (silent slot) and an EXISTING clip when something does (silent re-point, possibly to another voice).

── AUDIO LINKS ──────────────────────────────────────────────────────────────
  known_audio_id         nulled-silent
      now:  5be1eb84-9266-41f6-9c96-9dcb54ff4f02  [azure_si-LK-SameeraNeural] "ඔහු ඕනේ කළාද කියලා මම ඇහුවේ නෑ"
      after: NULL (silent)  
  target1_audio_id       nulled-silent
      now:  022ea085-fbc4-4228-a0e0-518827734572  [xai_bedd6226] "I didn't hear if he wanted"
      after: NULL (silent)  
  target2_audio_id       nulled-silent
      now:  5ec6239b-e400-4c45-b088-2855d80a8a7f  [xai_gfzdpspr5fdp] "I didn't hear if he wanted"
      after: NULL (silent)  

── DERIVED ──────────────────────────────────────────────────────────────────
  note: A phrase has no derived rows; its blast radius is its own audio plus the course-wide checks below.

── COURSE-WIDE ──────────────────────────────────────────────────────────────
  tiling: not at risk — No vocab unit removed — nothing can stop tiling.
  same known text elsewhere (1): phrase 381/eng_for_
  same target text elsewhere (1): phrase 381/eng_for_

── TTS ──────────────────────────────────────────────────────────────────────
  ≈ 3 clip(s) would need rendering (3 row + 0 presentation). NOTHING WAS RENDERED.
  ESTIMATE ONLY. This tool never renders. Content passes end by QUEUEING an audio pass (tools/course-optimization/queue-audio-pass.cjs), never by running TTS.

── DOCTRINE ─────────────────────────────────────────────────────────────────
  [CHECK] pod-content-migration: eng_for_sin has 1 pod(s). If this edit touches text that a pod sentence carries, learner progress MUST be migrated — progress is filed under a slot, not the text, so an in-place edit silently credits a learner with a sentence they never heard.
         → docs/pods/pod-migration-protocol.md (adopted 2026-08-16, plate A-111)
  [ REQD] make-before-break: 3 audio link(s) change. Generate and VERIFY the replacement clip before the old one is touched — never the other way round. Deletion never precedes a verified replacement.
         → docs/architecture/AUDIO_PIPELINE_ARCHITECTURE.md §6b
  [ info] learner-cache-invalidated: course_practice_phrases carries touch_course_content_stamp, so this edit bumps courses.content_stamp (currently Mon Aug 17 2026 12:37:35 GMT+0000 (Coordinated Universal Time)). Learner script caches for eng_for_sin invalidate on the next read.
  [ info] learner-reach: courses.new_app_status = "beta". This course is reachable by learners — the blast radius is live.


══════════════════════════════════════════════════════════════════════════════
  eng_for_sin  ·  course_practice_phrases  ·  phrase eng_for_sin:S0381L03U01  [proposed]
══════════════════════════════════════════════════════════════════════════════
  known_text:
      was  "ඔහු අපිව ගෙන ආවොත් ඕනේ කළාද කියලා මම ඇහුවේ නෑ"
      now  "ඔහු අපිව ගෙන ආවොත් ඕනේ කළාද කියලා මම ඔහුගෙන් ඇහුවේ නෑ"
  target_text:
      was  "I didn't hear if he wanted to follow us"
      now  "I didn't ask him if he wanted to follow us"

┌─ DECISION ─────────────────────────────────────────────────────────────────
│  PROCEED-WITH-REPAIRS
│  Safe to apply, but it is not finished when the text lands — 7 follow-up action(s) are required to leave the course whole.
│
│  because:
│    · known_audio_id becomes NULL — a silent slot until new audio is rendered.
│    · target1_audio_id becomes NULL — a silent slot until new audio is rendered.
│    · target2_audio_id becomes NULL — a silent slot until new audio is rendered.
│    · 2 intro clip(s) embed the old text and will keep speaking it.
│    · 3 other row(s) carry the identical old text and share its clip. Editing one leaves the course inconsistent.
│
│  before this edit is finished, you must:
│    1. Queue an audio pass so known_audio_id is refilled: node tools/course-optimization/queue-audio-pass.cjs eng_for_sin --reason "<pass>". Do NOT run TTS directly.
│    2. Queue an audio pass so target1_audio_id is refilled: node tools/course-optimization/queue-audio-pass.cjs eng_for_sin --reason "<pass>". Do NOT run TTS directly.
│    3. Queue an audio pass so target2_audio_id is refilled: node tools/course-optimization/queue-audio-pass.cjs eng_for_sin --reason "<pass>". Do NOT run TTS directly.
│    4. Re-compose 2 presentation clip(s) (POST /regenerate-presentations, which self-scopes to missing), then queue the audio pass.
│    5. Decide explicitly whether the other 3 row(s) should change too — include them in this proposal, or say why not.
│    6. If this text appears in a pod, migrate learner progress FIRST — docs/pods/pod-migration-protocol.md. Never edit a live pod in place.
│    7. ≈5 clip(s) will need rendering. TTS costs money — show a plan and get approval; end the pass by QUEUEING, never by running TTS.
└────────────────────────────────────────────────────────────────────────────

── VERDICT ──────────────────────────────────────────────────────────────────
  [ WARN] SILENT SLOT (known_audio_id): no existing clip matches the new text, so the trigger sets this to NULL. The slot is silent until Generate Missing Audio runs.
  [ WARN] SILENT SLOT (target1_audio_id): no existing clip matches the new text, so the trigger sets this to NULL. The slot is silent until Generate Missing Audio runs.
  [ WARN] SILENT SLOT (target2_audio_id): no existing clip matches the new text, so the trigger sets this to NULL. The slot is silent until Generate Missing Audio runs.
  [ WARN] SAME TEXT ELSEWHERE: 3 other row(s) in this course carry the identical old text. Editing one and not the others leaves the course inconsistent — and they share the same clip.
  [DANGER] PRESENTATIONS STALE: 2 intro clip(s) embed the OLD text and will keep speaking it. Presentations are per-LEGO and must be re-composed; the presentation text is built at render time, so this tool reports the clips, not the replacement wording.

── TRIGGER ──────────────────────────────────────────────────────────────────
  relink trigger: trg_null_phrase_audio_on_text_change
  trg_null_phrase_audio_on_text_change fires BEFORE UPDATE and REPLACES the audio link with audio_id_for_text(course, <new text>, role) — which is NULL when nothing matches (silent slot) and an EXISTING clip when something does (silent re-point, possibly to another voice).

── AUDIO LINKS ──────────────────────────────────────────────────────────────
  known_audio_id         nulled-silent
      now:  d81a3055-300a-439e-999e-b37a8b4b556d  [azure_si-LK-SameeraNeural] "ඔහු අපිව ගෙන ආවොත් ඕනේ කළාද කියලා මම ඇහුවේ නෑ."
      after: NULL (silent)  
  target1_audio_id       nulled-silent
      now:  c2c1c9de-c901-4792-bb6a-66ff5d138fa5  [xai_bedd6226] "I didn't hear if he wanted to follow us"
      after: NULL (silent)  
  target2_audio_id       nulled-silent
      now:  f6020e7c-a773-49e2-b064-1e276f00fc1f  [xai_gfzdpspr5fdp] "I didn't hear if he wanted to follow us"
      after: NULL (silent)  

── DERIVED ──────────────────────────────────────────────────────────────────
  note: A phrase has no derived rows; its blast radius is its own audio plus the course-wide checks below.

── COURSE-WIDE ──────────────────────────────────────────────────────────────
  tiling: not at risk — No vocab unit removed — nothing can stop tiling.
  same known text elsewhere (2): seed 381, phrase 381/eng_for_
  same target text elsewhere (1): phrase 381/eng_for_

── PRESENTATIONS ────────────────────────────────────────────────────────────
  2 intro clip(s) embed the old text:
      48668c58-545b-4516-9b4b-9f396f720c40 lego=S0381L01 [azure_si-LK-SameeraNeural] "ඉංග්‍රීසිෙන්. 'අපිව ගෙන ආවොත්'. 'ඔහු අපිව ගෙන ආවොත් ඕනේ "
      6e5ae983-b283-4ffc-9cd7-4ce7b6dc141d lego=S0381L03 [azure_si-LK-SameeraNeural] "ඉංග්‍රීසිෙන්. 'කියලා මම ඇහුවේ නෑ'. 'ඔහු අපිව ගෙන ආවොත් ඕ"

── TTS ──────────────────────────────────────────────────────────────────────
  ≈ 5 clip(s) would need rendering (3 row + 2 presentation). NOTHING WAS RENDERED.
  ESTIMATE ONLY. This tool never renders. Content passes end by QUEUEING an audio pass (tools/course-optimization/queue-audio-pass.cjs), never by running TTS.

── DOCTRINE ─────────────────────────────────────────────────────────────────
  [CHECK] pod-content-migration: eng_for_sin has 1 pod(s). If this edit touches text that a pod sentence carries, learner progress MUST be migrated — progress is filed under a slot, not the text, so an in-place edit silently credits a learner with a sentence they never heard.
         → docs/pods/pod-migration-protocol.md (adopted 2026-08-16, plate A-111)
  [ REQD] make-before-break: 3 audio link(s) change. Generate and VERIFY the replacement clip before the old one is touched — never the other way round. Deletion never precedes a verified replacement.
         → docs/architecture/AUDIO_PIPELINE_ARCHITECTURE.md §6b
  [ info] learner-cache-invalidated: course_practice_phrases carries touch_course_content_stamp, so this edit bumps courses.content_stamp (currently Mon Aug 17 2026 12:37:35 GMT+0000 (Coordinated Universal Time)). Learner script caches for eng_for_sin invalidate on the next read.
  [ info] learner-reach: courses.new_app_status = "beta". This course is reachable by learners — the blast radius is live.


══════════════════════════════════════════════════════════════════════════════
  eng_for_sin  ·  course_practice_phrases  ·  phrase eng_for_sin:S0381L03U02  [proposed]
══════════════════════════════════════════════════════════════════════════════
  known_text:
      was  "ඔහුට ලැබෙන්නේ නෑ කියලා මම ඇහුවේ නෑ"
      now  "ඔහුට ලැබෙන්නේ නෑ කියලා මම ඔහුගෙන් ඇහුවේ නෑ"
  target_text:
      was  "I didn't hear if he can't have it"
      now  "I didn't ask him if he can't have it"

┌─ DECISION ─────────────────────────────────────────────────────────────────
│  PROCEED-WITH-REPAIRS
│  Safe to apply, but it is not finished when the text lands — 6 follow-up action(s) are required to leave the course whole.
│
│  because:
│    · known_audio_id becomes NULL — a silent slot until new audio is rendered.
│    · target1_audio_id becomes NULL — a silent slot until new audio is rendered.
│    · target2_audio_id becomes NULL — a silent slot until new audio is rendered.
│    · 2 other row(s) carry the identical old text and share its clip. Editing one leaves the course inconsistent.
│
│  before this edit is finished, you must:
│    1. Queue an audio pass so known_audio_id is refilled: node tools/course-optimization/queue-audio-pass.cjs eng_for_sin --reason "<pass>". Do NOT run TTS directly.
│    2. Queue an audio pass so target1_audio_id is refilled: node tools/course-optimization/queue-audio-pass.cjs eng_for_sin --reason "<pass>". Do NOT run TTS directly.
│    3. Queue an audio pass so target2_audio_id is refilled: node tools/course-optimization/queue-audio-pass.cjs eng_for_sin --reason "<pass>". Do NOT run TTS directly.
│    4. Decide explicitly whether the other 2 row(s) should change too — include them in this proposal, or say why not.
│    5. If this text appears in a pod, migrate learner progress FIRST — docs/pods/pod-migration-protocol.md. Never edit a live pod in place.
│    6. ≈3 clip(s) will need rendering. TTS costs money — show a plan and get approval; end the pass by QUEUEING, never by running TTS.
└────────────────────────────────────────────────────────────────────────────

── VERDICT ──────────────────────────────────────────────────────────────────
  [ WARN] SILENT SLOT (known_audio_id): no existing clip matches the new text, so the trigger sets this to NULL. The slot is silent until Generate Missing Audio runs.
  [ WARN] SILENT SLOT (target1_audio_id): no existing clip matches the new text, so the trigger sets this to NULL. The slot is silent until Generate Missing Audio runs.
  [ WARN] SILENT SLOT (target2_audio_id): no existing clip matches the new text, so the trigger sets this to NULL. The slot is silent until Generate Missing Audio runs.
  [ WARN] SAME TEXT ELSEWHERE: 2 other row(s) in this course carry the identical old text. Editing one and not the others leaves the course inconsistent — and they share the same clip.

── TRIGGER ──────────────────────────────────────────────────────────────────
  relink trigger: trg_null_phrase_audio_on_text_change
  trg_null_phrase_audio_on_text_change fires BEFORE UPDATE and REPLACES the audio link with audio_id_for_text(course, <new text>, role) — which is NULL when nothing matches (silent slot) and an EXISTING clip when something does (silent re-point, possibly to another voice).

── AUDIO LINKS ──────────────────────────────────────────────────────────────
  known_audio_id         nulled-silent
      now:  c27e97c1-e5b2-453b-ae18-d596238310cb  [azure_si-LK-SameeraNeural] "ඔහුට ලැබෙන්නේ නෑ කියලා මම ඇහුවේ නෑ"
      after: NULL (silent)  
  target1_audio_id       nulled-silent
      now:  cf39dbaf-4a45-4fc9-b11f-d06c0d0c8641  [xai_bedd6226] "I didn't hear if he can't have it"
      after: NULL (silent)  
  target2_audio_id       nulled-silent
      now:  bdd482c4-b3d0-416c-a59a-d1c418226217  [xai_gfzdpspr5fdp] "I didn't hear if he can't have it"
      after: NULL (silent)  

── DERIVED ──────────────────────────────────────────────────────────────────
  note: A phrase has no derived rows; its blast radius is its own audio plus the course-wide checks below.

── COURSE-WIDE ──────────────────────────────────────────────────────────────
  tiling: not at risk — No vocab unit removed — nothing can stop tiling.
  same known text elsewhere (1): phrase 381/eng_for_
  same target text elsewhere (1): phrase 381/eng_for_

── TTS ──────────────────────────────────────────────────────────────────────
  ≈ 3 clip(s) would need rendering (3 row + 0 presentation). NOTHING WAS RENDERED.
  ESTIMATE ONLY. This tool never renders. Content passes end by QUEUEING an audio pass (tools/course-optimization/queue-audio-pass.cjs), never by running TTS.

── DOCTRINE ─────────────────────────────────────────────────────────────────
  [CHECK] pod-content-migration: eng_for_sin has 1 pod(s). If this edit touches text that a pod sentence carries, learner progress MUST be migrated — progress is filed under a slot, not the text, so an in-place edit silently credits a learner with a sentence they never heard.
         → docs/pods/pod-migration-protocol.md (adopted 2026-08-16, plate A-111)
  [ REQD] make-before-break: 3 audio link(s) change. Generate and VERIFY the replacement clip before the old one is touched — never the other way round. Deletion never precedes a verified replacement.
         → docs/architecture/AUDIO_PIPELINE_ARCHITECTURE.md §6b
  [ info] learner-cache-invalidated: course_practice_phrases carries touch_course_content_stamp, so this edit bumps courses.content_stamp (currently Mon Aug 17 2026 12:37:35 GMT+0000 (Coordinated Universal Time)). Learner script caches for eng_for_sin invalidate on the next read.
  [ info] learner-reach: courses.new_app_status = "beta". This course is reachable by learners — the blast radius is live.


══════════════════════════════════════════════════════════════════════════════
  eng_for_sin  ·  course_practice_phrases  ·  phrase eng_for_sin:S0381L03U03  [proposed]
══════════════════════════════════════════════════════════════════════════════
  known_text:
      was  "ඇය ගොඩ නිශ්ශබ්ද කියලා මම ඇහුවේ නෑ"
      now  "ඇය ගොඩ නිශ්ශබ්ද කියලා මම ඔහුගෙන් ඇහුවේ නෑ"
  target_text:
      was  "I didn't hear if she was rather quiet"
      now  "I didn't ask him if she was rather quiet"

┌─ DECISION ─────────────────────────────────────────────────────────────────
│  PROCEED-WITH-REPAIRS
│  Safe to apply, but it is not finished when the text lands — 6 follow-up action(s) are required to leave the course whole.
│
│  because:
│    · known_audio_id becomes NULL — a silent slot until new audio is rendered.
│    · target1_audio_id becomes NULL — a silent slot until new audio is rendered.
│    · target2_audio_id becomes NULL — a silent slot until new audio is rendered.
│    · 2 other row(s) carry the identical old text and share its clip. Editing one leaves the course inconsistent.
│
│  before this edit is finished, you must:
│    1. Queue an audio pass so known_audio_id is refilled: node tools/course-optimization/queue-audio-pass.cjs eng_for_sin --reason "<pass>". Do NOT run TTS directly.
│    2. Queue an audio pass so target1_audio_id is refilled: node tools/course-optimization/queue-audio-pass.cjs eng_for_sin --reason "<pass>". Do NOT run TTS directly.
│    3. Queue an audio pass so target2_audio_id is refilled: node tools/course-optimization/queue-audio-pass.cjs eng_for_sin --reason "<pass>". Do NOT run TTS directly.
│    4. Decide explicitly whether the other 2 row(s) should change too — include them in this proposal, or say why not.
│    5. If this text appears in a pod, migrate learner progress FIRST — docs/pods/pod-migration-protocol.md. Never edit a live pod in place.
│    6. ≈3 clip(s) will need rendering. TTS costs money — show a plan and get approval; end the pass by QUEUEING, never by running TTS.
└────────────────────────────────────────────────────────────────────────────

── VERDICT ──────────────────────────────────────────────────────────────────
  [ WARN] SILENT SLOT (known_audio_id): no existing clip matches the new text, so the trigger sets this to NULL. The slot is silent until Generate Missing Audio runs.
  [ WARN] SILENT SLOT (target1_audio_id): no existing clip matches the new text, so the trigger sets this to NULL. The slot is silent until Generate Missing Audio runs.
  [ WARN] SILENT SLOT (target2_audio_id): no existing clip matches the new text, so the trigger sets this to NULL. The slot is silent until Generate Missing Audio runs.
  [ WARN] SAME TEXT ELSEWHERE: 2 other row(s) in this course carry the identical old text. Editing one and not the others leaves the course inconsistent — and they share the same clip.

── TRIGGER ──────────────────────────────────────────────────────────────────
  relink trigger: trg_null_phrase_audio_on_text_change
  trg_null_phrase_audio_on_text_change fires BEFORE UPDATE and REPLACES the audio link with audio_id_for_text(course, <new text>, role) — which is NULL when nothing matches (silent slot) and an EXISTING clip when something does (silent re-point, possibly to another voice).

── AUDIO LINKS ──────────────────────────────────────────────────────────────
  known_audio_id         nulled-silent
      now:  176706a6-8f67-4469-bfe3-eb8e86070907  [azure_si-LK-SameeraNeural] "ඇය ගොඩ නිශ්ශබ්ද කියලා මම ඇහුවේ නෑ"
      after: NULL (silent)  
  target1_audio_id       nulled-silent
      now:  bdaacb78-9e9f-4263-acdf-35a493bcce64  [xai_bedd6226] "I didn't hear if she was rather quiet"
      after: NULL (silent)  
  target2_audio_id       nulled-silent
      now:  a14686f0-6885-489d-9ea6-adcbecbbe96e  [xai_gfzdpspr5fdp] "I didn't hear if she was rather quiet"
      after: NULL (silent)  

── DERIVED ──────────────────────────────────────────────────────────────────
  note: A phrase has no derived rows; its blast radius is its own audio plus the course-wide checks below.

── COURSE-WIDE ──────────────────────────────────────────────────────────────
  tiling: not at risk — No vocab unit removed — nothing can stop tiling.
  same known text elsewhere (1): phrase 381/eng_for_
  same target text elsewhere (1): phrase 381/eng_for_

── TTS ──────────────────────────────────────────────────────────────────────
  ≈ 3 clip(s) would need rendering (3 row + 0 presentation). NOTHING WAS RENDERED.
  ESTIMATE ONLY. This tool never renders. Content passes end by QUEUEING an audio pass (tools/course-optimization/queue-audio-pass.cjs), never by running TTS.

── DOCTRINE ─────────────────────────────────────────────────────────────────
  [CHECK] pod-content-migration: eng_for_sin has 1 pod(s). If this edit touches text that a pod sentence carries, learner progress MUST be migrated — progress is filed under a slot, not the text, so an in-place edit silently credits a learner with a sentence they never heard.
         → docs/pods/pod-migration-protocol.md (adopted 2026-08-16, plate A-111)
  [ REQD] make-before-break: 3 audio link(s) change. Generate and VERIFY the replacement clip before the old one is touched — never the other way round. Deletion never precedes a verified replacement.
         → docs/architecture/AUDIO_PIPELINE_ARCHITECTURE.md §6b
  [ info] learner-cache-invalidated: course_practice_phrases carries touch_course_content_stamp, so this edit bumps courses.content_stamp (currently Mon Aug 17 2026 12:37:35 GMT+0000 (Coordinated Universal Time)). Learner script caches for eng_for_sin invalidate on the next read.
  [ info] learner-reach: courses.new_app_status = "beta". This course is reachable by learners — the blast radius is live.


══════════════════════════════════════════════════════════════════════════════
  eng_for_sin  ·  course_practice_phrases  ·  phrase eng_for_sin:S0381L03U04  [proposed]
══════════════════════════════════════════════════════════════════════════════
  known_text:
      was  "ඒ අයට වැදගත් දෙයක් තිබිල කියලා මම ඇහුවේ නෑ"
      now  "ඒ අයට වැදගත් දෙයක් තිබිල කියලා මම ඔහුගෙන් ඇහුවේ නෑ"
  target_text:
      was  "I didn't hear if they had something important"
      now  "I didn't ask him if they had something important"

┌─ DECISION ─────────────────────────────────────────────────────────────────
│  PROCEED-WITH-REPAIRS
│  Safe to apply, but it is not finished when the text lands — 6 follow-up action(s) are required to leave the course whole.
│
│  because:
│    · known_audio_id becomes NULL — a silent slot until new audio is rendered.
│    · target1_audio_id becomes NULL — a silent slot until new audio is rendered.
│    · target2_audio_id becomes NULL — a silent slot until new audio is rendered.
│    · 2 other row(s) carry the identical old text and share its clip. Editing one leaves the course inconsistent.
│
│  before this edit is finished, you must:
│    1. Queue an audio pass so known_audio_id is refilled: node tools/course-optimization/queue-audio-pass.cjs eng_for_sin --reason "<pass>". Do NOT run TTS directly.
│    2. Queue an audio pass so target1_audio_id is refilled: node tools/course-optimization/queue-audio-pass.cjs eng_for_sin --reason "<pass>". Do NOT run TTS directly.
│    3. Queue an audio pass so target2_audio_id is refilled: node tools/course-optimization/queue-audio-pass.cjs eng_for_sin --reason "<pass>". Do NOT run TTS directly.
│    4. Decide explicitly whether the other 2 row(s) should change too — include them in this proposal, or say why not.
│    5. If this text appears in a pod, migrate learner progress FIRST — docs/pods/pod-migration-protocol.md. Never edit a live pod in place.
│    6. ≈3 clip(s) will need rendering. TTS costs money — show a plan and get approval; end the pass by QUEUEING, never by running TTS.
└────────────────────────────────────────────────────────────────────────────

── VERDICT ──────────────────────────────────────────────────────────────────
  [ WARN] SILENT SLOT (known_audio_id): no existing clip matches the new text, so the trigger sets this to NULL. The slot is silent until Generate Missing Audio runs.
  [ WARN] SILENT SLOT (target1_audio_id): no existing clip matches the new text, so the trigger sets this to NULL. The slot is silent until Generate Missing Audio runs.
  [ WARN] SILENT SLOT (target2_audio_id): no existing clip matches the new text, so the trigger sets this to NULL. The slot is silent until Generate Missing Audio runs.
  [ WARN] SAME TEXT ELSEWHERE: 2 other row(s) in this course carry the identical old text. Editing one and not the others leaves the course inconsistent — and they share the same clip.

── TRIGGER ──────────────────────────────────────────────────────────────────
  relink trigger: trg_null_phrase_audio_on_text_change
  trg_null_phrase_audio_on_text_change fires BEFORE UPDATE and REPLACES the audio link with audio_id_for_text(course, <new text>, role) — which is NULL when nothing matches (silent slot) and an EXISTING clip when something does (silent re-point, possibly to another voice).

── AUDIO LINKS ──────────────────────────────────────────────────────────────
  known_audio_id         nulled-silent
      now:  0799d9bd-15f0-47a2-b677-1f84a4a6c7f6  [azure_si-LK-SameeraNeural] "ඒ අයට වැදගත් දෙයක් තිබිල කියලා මම ඇහුවේ නෑ"
      after: NULL (silent)  
  target1_audio_id       nulled-silent
      now:  5c80ba84-f19c-4a09-b330-98d9edf9e25e  [xai_bedd6226] "I didn't hear if they had something important"
      after: NULL (silent)  
  target2_audio_id       nulled-silent
      now:  4789ea5e-9cc5-49ff-bccf-c21a3758edda  [xai_gfzdpspr5fdp] "I didn't hear if they had something important"
      after: NULL (silent)  

── DERIVED ──────────────────────────────────────────────────────────────────
  note: A phrase has no derived rows; its blast radius is its own audio plus the course-wide checks below.

── COURSE-WIDE ──────────────────────────────────────────────────────────────
  tiling: not at risk — No vocab unit removed — nothing can stop tiling.
  same known text elsewhere (1): phrase 381/eng_for_
  same target text elsewhere (1): phrase 381/eng_for_

── TTS ──────────────────────────────────────────────────────────────────────
  ≈ 3 clip(s) would need rendering (3 row + 0 presentation). NOTHING WAS RENDERED.
  ESTIMATE ONLY. This tool never renders. Content passes end by QUEUEING an audio pass (tools/course-optimization/queue-audio-pass.cjs), never by running TTS.

── DOCTRINE ─────────────────────────────────────────────────────────────────
  [CHECK] pod-content-migration: eng_for_sin has 1 pod(s). If this edit touches text that a pod sentence carries, learner progress MUST be migrated — progress is filed under a slot, not the text, so an in-place edit silently credits a learner with a sentence they never heard.
         → docs/pods/pod-migration-protocol.md (adopted 2026-08-16, plate A-111)
  [ REQD] make-before-break: 3 audio link(s) change. Generate and VERIFY the replacement clip before the old one is touched — never the other way round. Deletion never precedes a verified replacement.
         → docs/architecture/AUDIO_PIPELINE_ARCHITECTURE.md §6b
  [ info] learner-cache-invalidated: course_practice_phrases carries touch_course_content_stamp, so this edit bumps courses.content_stamp (currently Mon Aug 17 2026 12:37:35 GMT+0000 (Coordinated Universal Time)). Learner script caches for eng_for_sin invalidate on the next read.
  [ info] learner-reach: courses.new_app_status = "beta". This course is reachable by learners — the blast radius is live.


══════════════════════════════════════════════════════════════════════════════
  eng_for_sin  ·  course_practice_phrases  ·  phrase eng_for_sin:S0381L03U05  [proposed]
══════════════════════════════════════════════════════════════════════════════
  known_text:
      was  "ඇය කළේ මොකක්ද කියලා මම ඇහුවේ නෑ"
      now  "ඇය කළේ මොකක්ද කියලා මම ඔහුගෙන් ඇහුවේ නෑ"
  target_text:
      was  "I didn't hear if what she was doing"
      now  "I didn't ask him what she was doing"

┌─ DECISION ─────────────────────────────────────────────────────────────────
│  PROCEED-WITH-REPAIRS
│  Safe to apply, but it is not finished when the text lands — 6 follow-up action(s) are required to leave the course whole.
│
│  because:
│    · known_audio_id becomes NULL — a silent slot until new audio is rendered.
│    · target1_audio_id becomes NULL — a silent slot until new audio is rendered.
│    · target2_audio_id becomes NULL — a silent slot until new audio is rendered.
│    · 2 other row(s) carry the identical old text and share its clip. Editing one leaves the course inconsistent.
│
│  before this edit is finished, you must:
│    1. Queue an audio pass so known_audio_id is refilled: node tools/course-optimization/queue-audio-pass.cjs eng_for_sin --reason "<pass>". Do NOT run TTS directly.
│    2. Queue an audio pass so target1_audio_id is refilled: node tools/course-optimization/queue-audio-pass.cjs eng_for_sin --reason "<pass>". Do NOT run TTS directly.
│    3. Queue an audio pass so target2_audio_id is refilled: node tools/course-optimization/queue-audio-pass.cjs eng_for_sin --reason "<pass>". Do NOT run TTS directly.
│    4. Decide explicitly whether the other 2 row(s) should change too — include them in this proposal, or say why not.
│    5. If this text appears in a pod, migrate learner progress FIRST — docs/pods/pod-migration-protocol.md. Never edit a live pod in place.
│    6. ≈3 clip(s) will need rendering. TTS costs money — show a plan and get approval; end the pass by QUEUEING, never by running TTS.
└────────────────────────────────────────────────────────────────────────────

── VERDICT ──────────────────────────────────────────────────────────────────
  [ WARN] SILENT SLOT (known_audio_id): no existing clip matches the new text, so the trigger sets this to NULL. The slot is silent until Generate Missing Audio runs.
  [ WARN] SILENT SLOT (target1_audio_id): no existing clip matches the new text, so the trigger sets this to NULL. The slot is silent until Generate Missing Audio runs.
  [ WARN] SILENT SLOT (target2_audio_id): no existing clip matches the new text, so the trigger sets this to NULL. The slot is silent until Generate Missing Audio runs.
  [ WARN] SAME TEXT ELSEWHERE: 2 other row(s) in this course carry the identical old text. Editing one and not the others leaves the course inconsistent — and they share the same clip.

── TRIGGER ──────────────────────────────────────────────────────────────────
  relink trigger: trg_null_phrase_audio_on_text_change
  trg_null_phrase_audio_on_text_change fires BEFORE UPDATE and REPLACES the audio link with audio_id_for_text(course, <new text>, role) — which is NULL when nothing matches (silent slot) and an EXISTING clip when something does (silent re-point, possibly to another voice).

── AUDIO LINKS ──────────────────────────────────────────────────────────────
  known_audio_id         nulled-silent
      now:  0b2501ae-87d9-44d1-b1a2-2a379c34d0ca  [azure_si-LK-SameeraNeural] "ඇය කළේ මොකක්ද කියලා මම ඇහුවේ නෑ"
      after: NULL (silent)  
  target1_audio_id       nulled-silent
      now:  f862cc55-7308-4c90-a683-97ecb27cbe3c  [xai_bedd6226] "I didn't hear if what she was doing"
      after: NULL (silent)  
  target2_audio_id       nulled-silent
      now:  e5287e4c-d669-4df7-8d30-392fbebff412  [xai_gfzdpspr5fdp] "I didn't hear if what she was doing"
      after: NULL (silent)  

── DERIVED ──────────────────────────────────────────────────────────────────
  note: A phrase has no derived rows; its blast radius is its own audio plus the course-wide checks below.

── COURSE-WIDE ──────────────────────────────────────────────────────────────
  tiling: not at risk — No vocab unit removed — nothing can stop tiling.
  same known text elsewhere (1): phrase 381/eng_for_
  same target text elsewhere (1): phrase 381/eng_for_

── TTS ──────────────────────────────────────────────────────────────────────
  ≈ 3 clip(s) would need rendering (3 row + 0 presentation). NOTHING WAS RENDERED.
  ESTIMATE ONLY. This tool never renders. Content passes end by QUEUEING an audio pass (tools/course-optimization/queue-audio-pass.cjs), never by running TTS.

── DOCTRINE ─────────────────────────────────────────────────────────────────
  [CHECK] pod-content-migration: eng_for_sin has 1 pod(s). If this edit touches text that a pod sentence carries, learner progress MUST be migrated — progress is filed under a slot, not the text, so an in-place edit silently credits a learner with a sentence they never heard.
         → docs/pods/pod-migration-protocol.md (adopted 2026-08-16, plate A-111)
  [ REQD] make-before-break: 3 audio link(s) change. Generate and VERIFY the replacement clip before the old one is touched — never the other way round. Deletion never precedes a verified replacement.
         → docs/architecture/AUDIO_PIPELINE_ARCHITECTURE.md §6b
  [ info] learner-cache-invalidated: course_practice_phrases carries touch_course_content_stamp, so this edit bumps courses.content_stamp (currently Mon Aug 17 2026 12:37:35 GMT+0000 (Coordinated Universal Time)). Learner script caches for eng_for_sin invalidate on the next read.
  [ info] learner-reach: courses.new_app_status = "beta". This course is reachable by learners — the blast radius is live.


══════════════════════════════════════════════════════════════════════════════
  eng_for_sin  ·  course_seeds  ·  seed 382  [proposed]
══════════════════════════════════════════════════════════════════════════════
  known_text:
      was  "ඔහු ඒ දාන්නයි ඕනේ කළේ කොහෙද කියලා ඔයා ඇහුවාද?"
      now  "ඔහු ඒ දාන්නයි ඕනේ කළේ කොහෙද කියලා ඔයා ඔහුගෙන් ඇහුවාද?"
  target_text:
      was  "Did you ask where he wanted to put it?"
      now  "Did you ask him where he wanted to put it?"

┌─ DECISION ─────────────────────────────────────────────────────────────────
│  RECONSIDER
│  Reconsider this edit as written: it damages work beyond itself. Revise it, narrow it, or accept the listed cost as a deliberate decision.
│
│  because:
│    · The new seed target does not tile from this seed's existing LEGOs, so the breakdown must be rebuilt — this is not a text-only edit.
│    · known_audio_id will be left pointing at the OLD text with no trigger and no alarm.
│    · target1_audio_id will be left pointing at the OLD text with no trigger and no alarm.
│    · target2_audio_id will be left pointing at the OLD text with no trigger and no alarm.
│    · 2 intro clip(s) embed the old text and will keep speaking it.
│
│  before this edit is finished, you must:
│    1. Produce a new LEGO breakdown and apply it through POST /api/course/:code/edit-cascade (which snapshots and rolls back on a bad breakdown) rather than by direct UPDATE.
│    2. No clip exists for the new text yet. NULL known_audio_id so audio_autolink binds the new clip when it is rendered, then queue an audio pass (tools/course-optimization/queue-audio-pass.cjs).
│    3. No clip exists for the new text yet. NULL target1_audio_id so audio_autolink binds the new clip when it is rendered, then queue an audio pass (tools/course-optimization/queue-audio-pass.cjs).
│    4. No clip exists for the new text yet. NULL target2_audio_id so audio_autolink binds the new clip when it is rendered, then queue an audio pass (tools/course-optimization/queue-audio-pass.cjs).
│    5. Re-compose 2 presentation clip(s) (POST /regenerate-presentations, which self-scopes to missing), then queue the audio pass.
│    6. If this text appears in a pod, migrate learner progress FIRST — docs/pods/pod-migration-protocol.md. Never edit a live pod in place.
│    7. ≈5 clip(s) will need rendering. TTS costs money — show a plan and get approval; end the pass by QUEUEING, never by running TTS.
└────────────────────────────────────────────────────────────────────────────

── VERDICT ──────────────────────────────────────────────────────────────────
  [DANGER] SILENT STALE AUDIO: course_seeds has no audio-nulling trigger. After this edit the learner keeps hearing the OLD text with nothing to signal it. You must repoint the link by hand, or generate the new clip and NULL the link first so audio_autolink can bind it.
  [DANGER] STALE LINK (known_audio_id): still points at 5ff52ce5-638c-40f8-ad7b-7ec3317a2a04, which speaks "ඔහු ඒ දාන්නයි ඕනේ කළේ කොහෙද කියලා ඔයා ඇහුවාද?" — the OLD text. No clip exists for the new text yet. NULL known_audio_id so audio_autolink binds the new clip when it is rendered, then queue an audio pass (tools/course-optimization/queue-audio-pass.cjs).
  [DANGER] STALE LINK (target1_audio_id): still points at 77507944-d685-4396-8c5d-70e81f3b5b46, which speaks "Did you ask where he wanted to put it?" — the OLD text. No clip exists for the new text yet. NULL target1_audio_id so audio_autolink binds the new clip when it is rendered, then queue an audio pass (tools/course-optimization/queue-audio-pass.cjs).
  [DANGER] STALE LINK (target2_audio_id): still points at 13716681-7ee4-46bf-87dd-626f0b41451a, which speaks "Did you ask where he wanted to put it?" — the OLD text. No clip exists for the new text yet. NULL target2_audio_id so audio_autolink binds the new clip when it is rendered, then queue an audio pass (tools/course-optimization/queue-audio-pass.cjs).
  [ WARN] RE-DECOMPOSITION NEEDED: seed 382's 4 LEGO(s) no longer tile its new target text.
  [DANGER] PRESENTATIONS STALE: 2 intro clip(s) embed the OLD text and will keep speaking it. Presentations are per-LEGO and must be re-composed; the presentation text is built at render time, so this tool reports the clips, not the replacement wording.

── TRIGGER ──────────────────────────────────────────────────────────────────
  relink trigger: NONE
  NO nulling/relink trigger on course_seeds. The audio link is left EXACTLY AS IT WAS, still pointing at the clip for the OLD text. No NULL, no orphan, no alarm — a missing-audio sweep cannot see it and audio_autolink cannot rescue it (it only fills links that are already NULL).

── AUDIO LINKS ──────────────────────────────────────────────────────────────
  known_audio_id         left-stale
      now:  5ff52ce5-638c-40f8-ad7b-7ec3317a2a04  [azure_si-LK-SameeraNeural] "ඔහු ඒ දාන්නයි ඕනේ කළේ කොහෙද කියලා ඔයා ඇහුවාද?"
      after:5ff52ce5-638c-40f8-ad7b-7ec3317a2a04  [azure_si-LK-SameeraNeural] "ඔහු ඒ දාන්නයි ඕනේ කළේ කොහෙද කියලා ඔයා ඇහුවාද?"
      →  No clip exists for the new text yet. NULL known_audio_id so audio_autolink binds the new clip when it is rendered, then queue an audio pass (tools/course-optimization/queue-audio-pass.cjs).
  target1_audio_id       left-stale
      now:  77507944-d685-4396-8c5d-70e81f3b5b46  [bedd6226] "Did you ask where he wanted to put it?"
      after:77507944-d685-4396-8c5d-70e81f3b5b46  [bedd6226] "Did you ask where he wanted to put it?"
      →  No clip exists for the new text yet. NULL target1_audio_id so audio_autolink binds the new clip when it is rendered, then queue an audio pass (tools/course-optimization/queue-audio-pass.cjs).
  target2_audio_id       left-stale
      now:  13716681-7ee4-46bf-87dd-626f0b41451a  [xai_gfzdpspr5fdp] "Did you ask where he wanted to put it?"
      after:13716681-7ee4-46bf-87dd-626f0b41451a  [xai_gfzdpspr5fdp] "Did you ask where he wanted to put it?"
      →  No clip exists for the new text yet. NULL target2_audio_id so audio_autolink binds the new clip when it is rendered, then queue an audio pass (tools/course-optimization/queue-audio-pass.cjs).

── DERIVED ──────────────────────────────────────────────────────────────────
  legos: 4
  phrases: 17
  legos:
      L1 (M)  "ඒ දාන්නයි" → "to put that"
      L2 (M)  "ඔහු ... ඕනේ කළේ" → "he wanted to"
      L3 (A)  "කොහෙද" → "where"
      L4 (M)  "ඔයා ... ඇහුවාද" → "did you hear"
  edit_case: vocab-changing (Case 2) — the new target does NOT tile from this seed's existing LEGOs; a re-decomposition is required
  note: Re-decompose this seed (POST /api/course/:code/edit-cascade with the new legos, or /v2/decompose) — the LEGOs below no longer cover the seed text.

── COURSE-WIDE ──────────────────────────────────────────────────────────────
  tiling: not at risk — No vocab unit removed — nothing can stop tiling.

── PRESENTATIONS ────────────────────────────────────────────────────────────
  2 intro clip(s) embed the old text:
      7a4e1392-1de8-4dd7-90a2-29789d51c4cc lego=S0382L01 [azure_si-LK-SameeraNeural] "ඉංග්‍රීසිෙන්. 'ඒ දාන්නයි'. 'ඔහු ඒ දාන්නයි ඕනේ කළේ කොහෙද "
      7d16ccc1-400c-43ee-91bb-cb85da931e9b lego=S0382L03 [azure_si-LK-SameeraNeural] "ඉංග්‍රීසිෙන්. 'කොහෙද'. 'ඔහු ඒ දාන්නයි ඕනේ කළේ කොහෙද කියල"

── TTS ──────────────────────────────────────────────────────────────────────
  ≈ 5 clip(s) would need rendering (3 row + 2 presentation). NOTHING WAS RENDERED.
  ESTIMATE ONLY. This tool never renders. Content passes end by QUEUEING an audio pass (tools/course-optimization/queue-audio-pass.cjs), never by running TTS.

── DOCTRINE ─────────────────────────────────────────────────────────────────
  [CHECK] pod-content-migration: eng_for_sin has 1 pod(s). If this edit touches text that a pod sentence carries, learner progress MUST be migrated — progress is filed under a slot, not the text, so an in-place edit silently credits a learner with a sentence they never heard.
         → docs/pods/pod-migration-protocol.md (adopted 2026-08-16, plate A-111)
  [ REQD] make-before-break: 3 audio link(s) change. Generate and VERIFY the replacement clip before the old one is touched — never the other way round. Deletion never precedes a verified replacement.
         → docs/architecture/AUDIO_PIPELINE_ARCHITECTURE.md §6b
  [ info] learner-cache-invalidated: course_seeds carries touch_course_content_stamp, so this edit bumps courses.content_stamp (currently Mon Aug 17 2026 12:37:35 GMT+0000 (Coordinated Universal Time)). Learner script caches for eng_for_sin invalidate on the next read.
  [ info] learner-reach: courses.new_app_status = "beta". This course is reachable by learners — the blast radius is live.


══════════════════════════════════════════════════════════════════════════════
  eng_for_sin  ·  course_legos  ·  lego 382:4  [proposed]
══════════════════════════════════════════════════════════════════════════════
  known_text:
      was  "ඔයා ... ඇහුවාද"
      now  "ඔයා ඔහුගෙන් ඇහුවාද"
  target_text:
      was  "did you hear"
      now  "did you ask him"

┌─ DECISION ─────────────────────────────────────────────────────────────────
│  RECONSIDER
│  Reconsider this edit as written: it damages work beyond itself. Revise it, narrow it, or accept the listed cost as a deliberate decision.
│
│  because:
│    · 135 phrase(s) across 47 seed(s) stop tiling once this edit removes "did you hear". Those phrases are other people's finished work.
│    · known_audio_id becomes NULL — a silent slot until new audio is rendered.
│    · target1_audio_id becomes NULL — a silent slot until new audio is rendered.
│    · target2_audio_id becomes NULL — a silent slot until new audio is rendered.
│    · presentation_audio_id becomes NULL — a silent slot until new audio is rendered.
│    · 1 intro clip(s) embed the old text and will keep speaking it.
│    · 4 other row(s) carry the identical old text and share its clip. Editing one leaves the course inconsistent.
│
│  before this edit is finished, you must:
│    1. Either keep the removed chunk(s) in the new text, or accept re-authoring 135 phrase(s) (seeds 396, 403, 410, 412, 427, 441, 485, 489, 490, 492, +37 more).
│    2. Queue an audio pass so known_audio_id is refilled: node tools/course-optimization/queue-audio-pass.cjs eng_for_sin --reason "<pass>". Do NOT run TTS directly.
│    3. Queue an audio pass so target1_audio_id is refilled: node tools/course-optimization/queue-audio-pass.cjs eng_for_sin --reason "<pass>". Do NOT run TTS directly.
│    4. Queue an audio pass so target2_audio_id is refilled: node tools/course-optimization/queue-audio-pass.cjs eng_for_sin --reason "<pass>". Do NOT run TTS directly.
│    5. Queue an audio pass so presentation_audio_id is refilled: node tools/course-optimization/queue-audio-pass.cjs eng_for_sin --reason "<pass>". Do NOT run TTS directly.
│    6. Re-compose 1 presentation clip(s) (POST /regenerate-presentations, which self-scopes to missing), then queue the audio pass.
│    7. Decide explicitly whether the other 4 row(s) should change too — include them in this proposal, or say why not.
│    8. If this text appears in a pod, migrate learner progress FIRST — docs/pods/pod-migration-protocol.md. Never edit a live pod in place.
│    9. ≈4 clip(s) will need rendering. TTS costs money — show a plan and get approval; end the pass by QUEUEING, never by running TTS.
└────────────────────────────────────────────────────────────────────────────

── VERDICT ──────────────────────────────────────────────────────────────────
  [ WARN] SILENT SLOT (known_audio_id): no existing clip matches the new text, so the trigger sets this to NULL. The slot is silent until Generate Missing Audio runs.
  [ WARN] SILENT SLOT (target1_audio_id): no existing clip matches the new text, so the trigger sets this to NULL. The slot is silent until Generate Missing Audio runs.
  [ WARN] SILENT SLOT (target2_audio_id): no existing clip matches the new text, so the trigger sets this to NULL. The slot is silent until Generate Missing Audio runs.
  [ WARN] SILENT SLOT (presentation_audio_id): no existing clip matches the new text, so the trigger sets this to NULL. The slot is silent until Generate Missing Audio runs.
  [DANGER] COURSE-WIDE BREAKAGE: 135 phrase(s) elsewhere in the course tile through a chunk this edit removes and will no longer validate.
  [ WARN] SAME TEXT ELSEWHERE: 4 other row(s) in this course carry the identical old text. Editing one and not the others leaves the course inconsistent — and they share the same clip.
  [DANGER] PRESENTATIONS STALE: 1 intro clip(s) embed the OLD text and will keep speaking it. Presentations are per-LEGO and must be re-composed; the presentation text is built at render time, so this tool reports the clips, not the replacement wording.

── TRIGGER ──────────────────────────────────────────────────────────────────
  relink trigger: trg_null_lego_audio_on_text_change
  trg_null_lego_audio_on_text_change fires BEFORE UPDATE and REPLACES the audio link with audio_id_for_text(course, <new text>, role) — which is NULL when nothing matches (silent slot) and an EXISTING clip when something does (silent re-point, possibly to another voice).

── AUDIO LINKS ──────────────────────────────────────────────────────────────
  known_audio_id         nulled-silent
      now:  9df6163f-c6e1-4721-aad4-7d638720b6d3  [azure_si-LK-SameeraNeural] "ඔයා ... ඇහුවාද"
      after: NULL (silent)  
  target1_audio_id       nulled-silent
      now:  69332676-ddc3-4be1-8e47-fd9d61a2f02c  [xai_bedd6226] "did you hear"
      after: NULL (silent)  
  target2_audio_id       nulled-silent
      now:  c0807e7b-c384-404c-a8cb-95d2409c2686  [xai_gfzdpspr5fdp] "did you hear"
      after: NULL (silent)  
  presentation_audio_id  nulled-silent
      now:  (none)  
      after: NULL (silent)  

── DERIVED ──────────────────────────────────────────────────────────────────
  phrases: 0
  note: A LEGO is the teaching unit; its own build/use phrases must still contain its target text.

── COURSE-WIDE ──────────────────────────────────────────────────────────────
  tiling: 135 phrase(s) break of 4054 re-tiled  (chunks removed: did you hear)
      seed 396 L2 build: "we need to stand until everybody else is ready"  ✗ "stand until everybody else is ready"
      seed 396 L2 use: "I think we need to stand until everybody else is ready"  ✗ "stand until everybody else is ready"
      seed 403 L1 build: "to remain quiet for as long as possible"  ✗ "remain quiet for as long as possible"
      seed 403 L1 build: "we need to stand for as long as possible"  ✗ "stand for as long as possible"
      seed 403 L1 build: "I want to remain quiet for as long as possible"  ✗ "remain quiet for as long as possible"
      seed 403 L1 use: "he wants to remain quiet for as long as possible"  ✗ "remain quiet for as long as possible"
      seed 403 L1 use: "I don't want to remain quiet for as long as possible"  ✗ "remain quiet for as long as possible"
      seed 403 L1 use: "they want to remain quiet for as long as possible"  ✗ "remain quiet for as long as possible"
      seed 403 L1 use: "I think we should stand for as long as possible"  ✗ "stand for as long as possible"
      seed 403 L1 use: "do we need to stand for as long as possible?"  ✗ "stand for as long as possible"
      seed 410 L1 build: "fight with each other"  ✗ "fight with each other"
      seed 410 L1 build: "they fight with each other"  ✗ "fight with each other"
      seed 410 L1 build: "he said they fight with each other"  ✗ "fight with each other"
      seed 410 L1 use: "I think they fight with each other"  ✗ "fight with each other"
      seed 410 L1 use: "she said they fight with each other"  ✗ "fight with each other"
      … and 120 more (full list in the JSON report)
  same known text elsewhere (2): lego 366:3, phrase 366/eng_for_
  same target text elsewhere (2): lego 366:3, phrase 366/eng_for_

── PRESENTATIONS ────────────────────────────────────────────────────────────
  1 intro clip(s) embed the old text:
      2c803347-8754-4710-92f3-96f2f73b77ba lego=S0366L03 [azure_si-LK-SameeraNeural] "ඉංග්‍රීසිෙන්. 'ඔයා ... ඇහුවාද'. '' ඉතින්. :"

── TTS ──────────────────────────────────────────────────────────────────────
  ≈ 4 clip(s) would need rendering (3 row + 1 presentation). NOTHING WAS RENDERED.
  ESTIMATE ONLY. This tool never renders. Content passes end by QUEUEING an audio pass (tools/course-optimization/queue-audio-pass.cjs), never by running TTS.

── DOCTRINE ─────────────────────────────────────────────────────────────────
  [CHECK] pod-content-migration: eng_for_sin has 1 pod(s). If this edit touches text that a pod sentence carries, learner progress MUST be migrated — progress is filed under a slot, not the text, so an in-place edit silently credits a learner with a sentence they never heard.
         → docs/pods/pod-migration-protocol.md (adopted 2026-08-16, plate A-111)
  [ REQD] make-before-break: 4 audio link(s) change. Generate and VERIFY the replacement clip before the old one is touched — never the other way round. Deletion never precedes a verified replacement.
         → docs/architecture/AUDIO_PIPELINE_ARCHITECTURE.md §6b
  [ info] learner-cache-invalidated: course_legos carries touch_course_content_stamp, so this edit bumps courses.content_stamp (currently Mon Aug 17 2026 12:37:35 GMT+0000 (Coordinated Universal Time)). Learner script caches for eng_for_sin invalidate on the next read.
  [ info] learner-reach: courses.new_app_status = "beta". This course is reachable by learners — the blast radius is live.


══════════════════════════════════════════════════════════════════════════════
  eng_for_sin  ·  course_seeds  ·  seed 465  [proposed]
══════════════════════════════════════════════════════════════════════════════
  known_text:
      was  "ඊළඟ සැරේ මම ඇගේ නම මොකක්ද කියලා අහනවා."
      now  "ඊළඟ සැරේ මම ඇගෙන් ඇගේ නම මොකක්ද කියලා අහනවා."

┌─ DECISION ─────────────────────────────────────────────────────────────────
│  RECONSIDER
│  Reconsider this edit as written: it damages work beyond itself. Revise it, narrow it, or accept the listed cost as a deliberate decision.
│
│  because:
│    · This edit places "ඇගෙන්" at seed 465, but the course teaches it at no point at all. The methodology rail is that the learner is never given a word they haven't been taught.
│      (Matching is on exact word-forms with NO stemming, exactly like the known-side gate. In an inflecting language a flagged form may be a case/tense variant of a word that IS taught — which a gate could not tell apart, and you can. Judge it; do not assume it.)
│    · known_audio_id will be left pointing at the OLD text with no trigger and no alarm.
│    · 1 intro clip(s) embed the old text and will keep speaking it.
│
│  before this edit is finished, you must:
│    1. Use vocabulary already introduced by this point, or move the introduction earlier — deliberately, as a separate decision.
│    2. No clip exists for the new text yet. NULL known_audio_id so audio_autolink binds the new clip when it is rendered, then queue an audio pass (tools/course-optimization/queue-audio-pass.cjs).
│    3. Re-compose 1 presentation clip(s) (POST /regenerate-presentations, which self-scopes to missing), then queue the audio pass.
│    4. If this text appears in a pod, migrate learner progress FIRST — docs/pods/pod-migration-protocol.md. Never edit a live pod in place.
│    5. ≈2 clip(s) will need rendering. TTS costs money — show a plan and get approval; end the pass by QUEUEING, never by running TTS.
└────────────────────────────────────────────────────────────────────────────

── VERDICT ──────────────────────────────────────────────────────────────────
  [DANGER] SILENT STALE AUDIO: course_seeds has no audio-nulling trigger. After this edit the learner keeps hearing the OLD text with nothing to signal it. You must repoint the link by hand, or generate the new clip and NULL the link first so audio_autolink can bind it.
  [DANGER] STALE LINK (known_audio_id): still points at 8a499630-e18e-4101-b778-b52f2c13dbc4, which speaks "ඊළඟ සැරේ මම ඇගේ නම මොකක්ද කියලා අහනවා." — the OLD text. No clip exists for the new text yet. NULL known_audio_id so audio_autolink binds the new clip when it is rendered, then queue an audio pass (tools/course-optimization/queue-audio-pass.cjs).
  [ WARN] TAUGHT LATE, USED EARLY: this edit introduces 1 word-form(s) that the course uses before (or without ever) teaching them. This is the shape that generalises past the edited row.
  [DANGER] PRESENTATIONS STALE: 1 intro clip(s) embed the OLD text and will keep speaking it. Presentations are per-LEGO and must be re-composed; the presentation text is built at render time, so this tool reports the clips, not the replacement wording.

── TRIGGER ──────────────────────────────────────────────────────────────────
  relink trigger: NONE
  NO nulling/relink trigger on course_seeds. The audio link is left EXACTLY AS IT WAS, still pointing at the clip for the OLD text. No NULL, no orphan, no alarm — a missing-audio sweep cannot see it and audio_autolink cannot rescue it (it only fills links that are already NULL).

── AUDIO LINKS ──────────────────────────────────────────────────────────────
  known_audio_id         left-stale
      now:  8a499630-e18e-4101-b778-b52f2c13dbc4  [azure_si-LK-SameeraNeural] "ඊළඟ සැරේ මම ඇගේ නම මොකක්ද කියලා අහනවා."
      after:8a499630-e18e-4101-b778-b52f2c13dbc4  [azure_si-LK-SameeraNeural] "ඊළඟ සැරේ මම ඇගේ නම මොකක්ද කියලා අහනවා."
      →  No clip exists for the new text yet. NULL known_audio_id so audio_autolink binds the new clip when it is rendered, then queue an audio pass (tools/course-optimization/queue-audio-pass.cjs).

── DERIVED ──────────────────────────────────────────────────────────────────
  legos: 3
  phrases: 29
  legos:
      L1 (M)  "ඊළඟ සැරේ" → "next time"
      L2 (M)  "ඇගේ නම මොකක්ද කිව්ව" → "what her name was"
      L3 (M)  "මම ... අහනවා" → "I'll ask"
  edit_case: known-side edit — the target-side chunk set is untouched, so tiling downstream is unaffected. The blast radius is ZUT, the known-side gate, and audio.

── COURSE-WIDE ──────────────────────────────────────────────────────────────
  tiling: not at risk — No vocab unit removed — nothing can stop tiling.
  taught late / used early:
      [known] "ඇගෙන්" — taught at seed NEVER, used 2× from seed 136; used earlier at 136, 136

── PRESENTATIONS ────────────────────────────────────────────────────────────
  1 intro clip(s) embed the old text:
      28e8881b-03e1-4631-a3f0-ac51db3f1dba lego=S0465L01 [azure_si-LK-SameeraNeural] "ඉංග්‍රීසිෙන්. 'ඊළඟ සැරේ'. 'ඊළඟ සැරේ මම ඇගේ නම මොකක්ද කිය"

── TTS ──────────────────────────────────────────────────────────────────────
  ≈ 2 clip(s) would need rendering (1 row + 1 presentation). NOTHING WAS RENDERED.
  ESTIMATE ONLY. This tool never renders. Content passes end by QUEUEING an audio pass (tools/course-optimization/queue-audio-pass.cjs), never by running TTS.

── DOCTRINE ─────────────────────────────────────────────────────────────────
  [CHECK] pod-content-migration: eng_for_sin has 1 pod(s). If this edit touches text that a pod sentence carries, learner progress MUST be migrated — progress is filed under a slot, not the text, so an in-place edit silently credits a learner with a sentence they never heard.
         → docs/pods/pod-migration-protocol.md (adopted 2026-08-16, plate A-111)
  [ REQD] make-before-break: 1 audio link(s) change. Generate and VERIFY the replacement clip before the old one is touched — never the other way round. Deletion never precedes a verified replacement.
         → docs/architecture/AUDIO_PIPELINE_ARCHITECTURE.md §6b
  [ info] learner-cache-invalidated: course_seeds carries touch_course_content_stamp, so this edit bumps courses.content_stamp (currently Mon Aug 17 2026 12:37:35 GMT+0000 (Coordinated Universal Time)). Learner script caches for eng_for_sin invalidate on the next read.
  [ info] learner-reach: courses.new_app_status = "beta". This course is reachable by learners — the blast radius is live.


══════════════════════════════════════════════════════════════════════════════
  eng_for_sin  ·  course_seeds  ·  seed 415  [proposed]
══════════════════════════════════════════════════════════════════════════════
  known_text:
      was  "මාව ලවා ඇහුවොත් ඒ ප්‍රශ්නයක් නෙමෙයි."
      now  "මගෙන් ඇහුවොත් ඒ ප්‍රශ්නයක් නෙමෙයි."

┌─ DECISION ─────────────────────────────────────────────────────────────────
│  RECONSIDER
│  Reconsider this edit as written: it damages work beyond itself. Revise it, narrow it, or accept the listed cost as a deliberate decision.
│
│  because:
│    · This edit places "මගෙන්" at seed 415, but the course teaches it at no point at all. The methodology rail is that the learner is never given a word they haven't been taught.
│      (Matching is on exact word-forms with NO stemming, exactly like the known-side gate. In an inflecting language a flagged form may be a case/tense variant of a word that IS taught — which a gate could not tell apart, and you can. Judge it; do not assume it.)
│    · known_audio_id will be left pointing at the OLD text with no trigger and no alarm.
│    · 2 intro clip(s) embed the old text and will keep speaking it.
│    · 1 other row(s) carry the identical old text and share its clip. Editing one leaves the course inconsistent.
│
│  before this edit is finished, you must:
│    1. Use vocabulary already introduced by this point, or move the introduction earlier — deliberately, as a separate decision.
│    2. No clip exists for the new text yet. NULL known_audio_id so audio_autolink binds the new clip when it is rendered, then queue an audio pass (tools/course-optimization/queue-audio-pass.cjs).
│    3. Re-compose 2 presentation clip(s) (POST /regenerate-presentations, which self-scopes to missing), then queue the audio pass.
│    4. Decide explicitly whether the other 1 row(s) should change too — include them in this proposal, or say why not.
│    5. If this text appears in a pod, migrate learner progress FIRST — docs/pods/pod-migration-protocol.md. Never edit a live pod in place.
│    6. ≈3 clip(s) will need rendering. TTS costs money — show a plan and get approval; end the pass by QUEUEING, never by running TTS.
└────────────────────────────────────────────────────────────────────────────

── VERDICT ──────────────────────────────────────────────────────────────────
  [DANGER] SILENT STALE AUDIO: course_seeds has no audio-nulling trigger. After this edit the learner keeps hearing the OLD text with nothing to signal it. You must repoint the link by hand, or generate the new clip and NULL the link first so audio_autolink can bind it.
  [DANGER] STALE LINK (known_audio_id): still points at 1d4d22a8-e913-4484-8ec2-3c1c8e244aec, which speaks "මාව ලවා ඇහුවොත් ඒ ප්‍රශ්නයක් නෙමෙයි." — the OLD text. No clip exists for the new text yet. NULL known_audio_id so audio_autolink binds the new clip when it is rendered, then queue an audio pass (tools/course-optimization/queue-audio-pass.cjs).
  [ WARN] TAUGHT LATE, USED EARLY: this edit introduces 1 word-form(s) that the course uses before (or without ever) teaching them. This is the shape that generalises past the edited row.
  [ WARN] SAME TEXT ELSEWHERE: 1 other row(s) in this course carry the identical old text. Editing one and not the others leaves the course inconsistent — and they share the same clip.
  [DANGER] PRESENTATIONS STALE: 2 intro clip(s) embed the OLD text and will keep speaking it. Presentations are per-LEGO and must be re-composed; the presentation text is built at render time, so this tool reports the clips, not the replacement wording.

── TRIGGER ──────────────────────────────────────────────────────────────────
  relink trigger: NONE
  NO nulling/relink trigger on course_seeds. The audio link is left EXACTLY AS IT WAS, still pointing at the clip for the OLD text. No NULL, no orphan, no alarm — a missing-audio sweep cannot see it and audio_autolink cannot rescue it (it only fills links that are already NULL).

── AUDIO LINKS ──────────────────────────────────────────────────────────────
  known_audio_id         left-stale
      now:  1d4d22a8-e913-4484-8ec2-3c1c8e244aec  [azure_si-LK-SameeraNeural] "මාව ලවා ඇහුවොත් ඒ ප්‍රශ්නයක් නෙමෙයි."
      after:1d4d22a8-e913-4484-8ec2-3c1c8e244aec  [azure_si-LK-SameeraNeural] "මාව ලවා ඇහුවොත් ඒ ප්‍රශ්නයක් නෙමෙයි."
      →  No clip exists for the new text yet. NULL known_audio_id so audio_autolink binds the new clip when it is rendered, then queue an audio pass (tools/course-optimization/queue-audio-pass.cjs).

── DERIVED ──────────────────────────────────────────────────────────────────
  legos: 3
  phrases: 18
  legos:
      L1 (A)  "ප්‍රශ්නයක්" → "problem"
      L2 (M)  "ඒ ප්‍රශ්නයක් නෙමෙයි" → "that's not a problem"
      L3 (M)  "මාව ලවා ඇහුවොත්" → "if you ask me"
  edit_case: known-side edit — the target-side chunk set is untouched, so tiling downstream is unaffected. The blast radius is ZUT, the known-side gate, and audio.

── COURSE-WIDE ──────────────────────────────────────────────────────────────
  tiling: not at risk — No vocab unit removed — nothing can stop tiling.
  taught late / used early:
      [known] "මගෙන්" — taught at seed NEVER, used 0× from seed null
  same known text elsewhere (1): phrase 415/eng_for_

── PRESENTATIONS ────────────────────────────────────────────────────────────
  2 intro clip(s) embed the old text:
      a04fee0e-12c8-4a7f-b1dd-02b8db73ff68 lego=S0415L02 [azure_si-LK-SameeraNeural] "ඉංග්‍රීසිෙන්. 'ඒ ප්‍රශ්නයක් නෙමෙයි'. 'මාව ලවා ඇහුවොත් ඒ "
      27eca252-4af1-4219-a875-c7d74c713e3a lego=S0415L03 [azure_si-LK-SameeraNeural] "ඉංග්‍රීසිෙන්. 'මාව ලවා ඇහුවොත්'. 'මාව ලවා ඇහුවොත් ඒ ප්‍ර"

── TTS ──────────────────────────────────────────────────────────────────────
  ≈ 3 clip(s) would need rendering (1 row + 2 presentation). NOTHING WAS RENDERED.
  ESTIMATE ONLY. This tool never renders. Content passes end by QUEUEING an audio pass (tools/course-optimization/queue-audio-pass.cjs), never by running TTS.

── DOCTRINE ─────────────────────────────────────────────────────────────────
  [CHECK] pod-content-migration: eng_for_sin has 1 pod(s). If this edit touches text that a pod sentence carries, learner progress MUST be migrated — progress is filed under a slot, not the text, so an in-place edit silently credits a learner with a sentence they never heard.
         → docs/pods/pod-migration-protocol.md (adopted 2026-08-16, plate A-111)
  [ REQD] make-before-break: 1 audio link(s) change. Generate and VERIFY the replacement clip before the old one is touched — never the other way round. Deletion never precedes a verified replacement.
         → docs/architecture/AUDIO_PIPELINE_ARCHITECTURE.md §6b
  [ info] learner-cache-invalidated: course_seeds carries touch_course_content_stamp, so this edit bumps courses.content_stamp (currently Mon Aug 17 2026 12:37:35 GMT+0000 (Coordinated Universal Time)). Learner script caches for eng_for_sin invalidate on the next read.
  [ info] learner-reach: courses.new_app_status = "beta". This course is reachable by learners — the blast radius is live.


══════════════════════════════════════════════════════════════════════════════
  eng_for_sin  ·  course_legos  ·  lego 415:3  [proposed]
══════════════════════════════════════════════════════════════════════════════
  known_text:
      was  "මාව ලවා ඇහුවොත්"
      now  "මගෙන් ඇහුවොත්"

┌─ DECISION ─────────────────────────────────────────────────────────────────
│  RECONSIDER
│  Reconsider this edit as written: it damages work beyond itself. Revise it, narrow it, or accept the listed cost as a deliberate decision.
│
│  because:
│    · This edit places "මගෙන්" at seed 415, but the course teaches it at no point at all. The methodology rail is that the learner is never given a word they haven't been taught.
│      (Matching is on exact word-forms with NO stemming, exactly like the known-side gate. In an inflecting language a flagged form may be a case/tense variant of a word that IS taught — which a gate could not tell apart, and you can. Judge it; do not assume it.)
│    · known_audio_id becomes NULL — a silent slot until new audio is rendered.
│    · presentation_audio_id becomes NULL — a silent slot until new audio is rendered.
│    · 2 intro clip(s) embed the old text and will keep speaking it.
│    · 1 other row(s) carry the identical old text and share its clip. Editing one leaves the course inconsistent.
│
│  before this edit is finished, you must:
│    1. Use vocabulary already introduced by this point, or move the introduction earlier — deliberately, as a separate decision.
│    2. Queue an audio pass so known_audio_id is refilled: node tools/course-optimization/queue-audio-pass.cjs eng_for_sin --reason "<pass>". Do NOT run TTS directly.
│    3. Queue an audio pass so presentation_audio_id is refilled: node tools/course-optimization/queue-audio-pass.cjs eng_for_sin --reason "<pass>". Do NOT run TTS directly.
│    4. Re-compose 2 presentation clip(s) (POST /regenerate-presentations, which self-scopes to missing), then queue the audio pass.
│    5. Decide explicitly whether the other 1 row(s) should change too — include them in this proposal, or say why not.
│    6. If this text appears in a pod, migrate learner progress FIRST — docs/pods/pod-migration-protocol.md. Never edit a live pod in place.
│    7. ≈3 clip(s) will need rendering. TTS costs money — show a plan and get approval; end the pass by QUEUEING, never by running TTS.
└────────────────────────────────────────────────────────────────────────────

── VERDICT ──────────────────────────────────────────────────────────────────
  [ WARN] SILENT SLOT (known_audio_id): no existing clip matches the new text, so the trigger sets this to NULL. The slot is silent until Generate Missing Audio runs.
  [ WARN] SILENT SLOT (presentation_audio_id): no existing clip matches the new text, so the trigger sets this to NULL. The slot is silent until Generate Missing Audio runs.
  [ WARN] TAUGHT LATE, USED EARLY: this edit introduces 1 word-form(s) that the course uses before (or without ever) teaching them. This is the shape that generalises past the edited row.
  [ WARN] SAME TEXT ELSEWHERE: 1 other row(s) in this course carry the identical old text. Editing one and not the others leaves the course inconsistent — and they share the same clip.
  [DANGER] PRESENTATIONS STALE: 2 intro clip(s) embed the OLD text and will keep speaking it. Presentations are per-LEGO and must be re-composed; the presentation text is built at render time, so this tool reports the clips, not the replacement wording.

── TRIGGER ──────────────────────────────────────────────────────────────────
  relink trigger: trg_null_lego_audio_on_text_change
  trg_null_lego_audio_on_text_change fires BEFORE UPDATE and REPLACES the audio link with audio_id_for_text(course, <new text>, role) — which is NULL when nothing matches (silent slot) and an EXISTING clip when something does (silent re-point, possibly to another voice).

── AUDIO LINKS ──────────────────────────────────────────────────────────────
  known_audio_id         nulled-silent
      now:  ca58f18c-6681-44e0-9edd-efa4466530f3  [azure_si-LK-SameeraNeural] "මාව ලවා ඇහුවොත්"
      after: NULL (silent)  
  presentation_audio_id  nulled-silent
      now:  27eca252-4af1-4219-a875-c7d74c713e3a  [azure_si-LK-SameeraNeural] "ඉංග්‍රීසිෙන්. 'මාව ලවා ඇහුවොත්'. 'මාව ලවා ඇහුවොත"
      after: NULL (silent)  

── DERIVED ──────────────────────────────────────────────────────────────────
  phrases: 10
  note: A LEGO is the teaching unit; its own build/use phrases must still contain its target text.

── COURSE-WIDE ──────────────────────────────────────────────────────────────
  tiling: not at risk — No vocab unit removed — nothing can stop tiling.
  taught late / used early:
      [known] "මගෙන්" — taught at seed NEVER, used 0× from seed null
  same known text elsewhere (1): phrase 415/eng_for_

── PRESENTATIONS ────────────────────────────────────────────────────────────
  2 intro clip(s) embed the old text:
      a04fee0e-12c8-4a7f-b1dd-02b8db73ff68 lego=S0415L02 [azure_si-LK-SameeraNeural] "ඉංග්‍රීසිෙන්. 'ඒ ප්‍රශ්නයක් නෙමෙයි'. 'මාව ලවා ඇහුවොත් ඒ "
      27eca252-4af1-4219-a875-c7d74c713e3a lego=S0415L03 [azure_si-LK-SameeraNeural] "ඉංග්‍රීසිෙන්. 'මාව ලවා ඇහුවොත්'. 'මාව ලවා ඇහුවොත් ඒ ප්‍ර"

── TTS ──────────────────────────────────────────────────────────────────────
  ≈ 3 clip(s) would need rendering (1 row + 2 presentation). NOTHING WAS RENDERED.
  ESTIMATE ONLY. This tool never renders. Content passes end by QUEUEING an audio pass (tools/course-optimization/queue-audio-pass.cjs), never by running TTS.

── DOCTRINE ─────────────────────────────────────────────────────────────────
  [CHECK] pod-content-migration: eng_for_sin has 1 pod(s). If this edit touches text that a pod sentence carries, learner progress MUST be migrated — progress is filed under a slot, not the text, so an in-place edit silently credits a learner with a sentence they never heard.
         → docs/pods/pod-migration-protocol.md (adopted 2026-08-16, plate A-111)
  [ REQD] make-before-break: 2 audio link(s) change. Generate and VERIFY the replacement clip before the old one is touched — never the other way round. Deletion never precedes a verified replacement.
         → docs/architecture/AUDIO_PIPELINE_ARCHITECTURE.md §6b
  [ info] learner-cache-invalidated: course_legos carries touch_course_content_stamp, so this edit bumps courses.content_stamp (currently Mon Aug 17 2026 12:37:35 GMT+0000 (Coordinated Universal Time)). Learner script caches for eng_for_sin invalidate on the next read.
  [ info] learner-reach: courses.new_app_status = "beta". This course is reachable by learners — the blast radius is live.


══════════════════════════════════════════════════════════════════════════════
  eng_for_sin  ·  course_practice_phrases  ·  phrase eng_for_sin:S0415L03C01  [proposed]
══════════════════════════════════════════════════════════════════════════════
  known_text:
      was  "මාව"
      now  "මගෙන්"

┌─ DECISION ─────────────────────────────────────────────────────────────────
│  PROCEED-WITH-REPAIRS
│  Safe to apply, but it is not finished when the text lands — 5 follow-up action(s) are required to leave the course whole.
│
│  because:
│    · known_audio_id becomes NULL — a silent slot until new audio is rendered.
│    · 33 intro clip(s) embed the old text and will keep speaking it.
│    · 4 other row(s) carry the identical old text and share its clip. Editing one leaves the course inconsistent.
│
│  before this edit is finished, you must:
│    1. Queue an audio pass so known_audio_id is refilled: node tools/course-optimization/queue-audio-pass.cjs eng_for_sin --reason "<pass>". Do NOT run TTS directly.
│    2. Re-compose 33 presentation clip(s) (POST /regenerate-presentations, which self-scopes to missing), then queue the audio pass.
│    3. Decide explicitly whether the other 4 row(s) should change too — include them in this proposal, or say why not.
│    4. If this text appears in a pod, migrate learner progress FIRST — docs/pods/pod-migration-protocol.md. Never edit a live pod in place.
│    5. ≈34 clip(s) will need rendering. TTS costs money — show a plan and get approval; end the pass by QUEUEING, never by running TTS.
└────────────────────────────────────────────────────────────────────────────

── VERDICT ──────────────────────────────────────────────────────────────────
  [ WARN] SILENT SLOT (known_audio_id): no existing clip matches the new text, so the trigger sets this to NULL. The slot is silent until Generate Missing Audio runs.
  [ WARN] TAUGHT LATE, USED EARLY: this edit introduces 1 word-form(s) that the course uses before (or without ever) teaching them. This is the shape that generalises past the edited row.
  [ WARN] SAME TEXT ELSEWHERE: 4 other row(s) in this course carry the identical old text. Editing one and not the others leaves the course inconsistent — and they share the same clip.
  [DANGER] PRESENTATIONS STALE: 33 intro clip(s) embed the OLD text and will keep speaking it. Presentations are per-LEGO and must be re-composed; the presentation text is built at render time, so this tool reports the clips, not the replacement wording.

── TRIGGER ──────────────────────────────────────────────────────────────────
  relink trigger: trg_null_phrase_audio_on_text_change
  trg_null_phrase_audio_on_text_change fires BEFORE UPDATE and REPLACES the audio link with audio_id_for_text(course, <new text>, role) — which is NULL when nothing matches (silent slot) and an EXISTING clip when something does (silent re-point, possibly to another voice).

── AUDIO LINKS ──────────────────────────────────────────────────────────────
  known_audio_id         nulled-silent
      now:  cb92683d-cb02-460c-af58-a77f36a4caf0  [azure_si-LK-SameeraNeural] "මාව"
      after: NULL (silent)  

── DERIVED ──────────────────────────────────────────────────────────────────
  note: A phrase has no derived rows; its blast radius is its own audio plus the course-wide checks below.

── COURSE-WIDE ──────────────────────────────────────────────────────────────
  tiling: not at risk — No vocab unit removed — nothing can stop tiling.
  taught late / used early:
      [known] "මගෙන්" — taught at seed NEVER, used 0× from seed null
  same known text elsewhere (4): phrase 351/eng_for_, phrase 367/eng_for_, phrase 378/eng_for_, phrase 415/eng_for_

── PRESENTATIONS ────────────────────────────────────────────────────────────
  33 intro clip(s) embed the old text:
      73e2f14e-add9-47df-9d74-6cd90d8d69b2 lego=S0139L01 [azure_si-LK-SameeraNeural] "ඉංග්‍රීසිෙන්. 'සමාවෙන්න'. 'මම සමාවෙන්න ඒක අමාරු වුණා' ඉත"
      ecfb010e-c3ed-46ef-88cb-9b2ff25fa5b3 lego=S0226L01 [azure_si-LK-SameeraNeural] "ඉංග්‍රීසිෙන්. 'මිනිහා'. 'ඒ මිනිහා මාව ලවා උදව් කරන්නයි උ"
      21dea1e7-a759-4a51-8de4-0fe64f182153 lego=S0227L02 [azure_si-LK-SameeraNeural] "ඉංග්‍රීසිෙන්. 'අලුත් දෙයක්'. 'ඒ මිනිහා මාව ලවා අලුත් දෙය"
      45867101-6b6e-4dd3-ade3-b2fd013c05f3 lego=S0227L03 [azure_si-LK-SameeraNeural] "ඉංග්‍රීසිෙන්. 'ඒ මිනිහා'. 'ඒ මිනිහා මාව ලවා අලුත් දෙයක් "
      28596766-7aa9-4cec-96a5-e2e9f17a45b1 lego=S0237L01 [azure_si-LK-SameeraNeural] "ඉංග්‍රීසිෙන්. 'ඔයාට කියන්නයි මාව ලවා ඕනේ කළේ'. '' ඉතින්."
      ab4da188-efee-4eb7-af0e-af3da1fff3e0 lego=S0238L01 [azure_si-LK-SameeraNeural] "ඉංග්‍රීසිෙන්. 'ඔයා මාව ලවා කියන්නයි ඕනේ කළේ'. '' ඉතින්. "
      1934fe30-cf35-4908-8d10-8fa261f5ee11 lego=S0268L01 [azure_si-LK-SameeraNeural] "ඉංග්‍රීසිෙන්. 'ඊමේල් දෙකක් යැව්වා'. 'ඇය මාව ලවා ඊමේල් දෙ"
      a0df5933-1af2-4118-b100-b0de7b5195ed lego=S0293L01 [azure_si-LK-SameeraNeural] "ඉංග්‍රීසිෙන්. 'ඔහු මාව ලවා හමු වෙන්නේ කොහෙද'. '' ඉතින්. "
      613ec87f-14c8-4734-98d6-e520d5e29c6b lego=S0298L01 [azure_si-LK-SameeraNeural] "ඉංග්‍රීසිෙන්. 'ඉතිරි'. 'මාව ලවා ඔයාට ඉතිරි දේ නෑ' ඉතින්."
      a45c7478-948c-4321-b261-09270a5156cb lego=S0334L02 [azure_si-LK-SameeraNeural] "ඉංග්‍රීසිෙන්. 'පූසා ළමාව ගන්නයි'. '' ඉතින්. :"
      60e788fd-3131-4278-9a40-76b9768efb1c lego=S0351L02 [azure_si-LK-SameeraNeural] "ඉංග්‍රීසිෙන්. 'මාව තනියම දාන්නේ'. 'නෑ මාව තනියම දාන්නේ ඔ"
      ce65818d-c179-4992-91bc-4b0dfd9e3907 lego=S0351L03 [azure_si-LK-SameeraNeural] "ඉංග්‍රීසිෙන්. 'ඔහු ඕනෑකමින් ඕනේ නොකළා'. 'නෑ මාව තනියම දා"
      … and 21 more

── TTS ──────────────────────────────────────────────────────────────────────
  ≈ 34 clip(s) would need rendering (1 row + 33 presentation). NOTHING WAS RENDERED.
  ESTIMATE ONLY. This tool never renders. Content passes end by QUEUEING an audio pass (tools/course-optimization/queue-audio-pass.cjs), never by running TTS.

── DOCTRINE ─────────────────────────────────────────────────────────────────
  [CHECK] pod-content-migration: eng_for_sin has 1 pod(s). If this edit touches text that a pod sentence carries, learner progress MUST be migrated — progress is filed under a slot, not the text, so an in-place edit silently credits a learner with a sentence they never heard.
         → docs/pods/pod-migration-protocol.md (adopted 2026-08-16, plate A-111)
  [ REQD] make-before-break: 1 audio link(s) change. Generate and VERIFY the replacement clip before the old one is touched — never the other way round. Deletion never precedes a verified replacement.
         → docs/architecture/AUDIO_PIPELINE_ARCHITECTURE.md §6b
  [ info] learner-cache-invalidated: course_practice_phrases carries touch_course_content_stamp, so this edit bumps courses.content_stamp (currently Mon Aug 17 2026 12:37:35 GMT+0000 (Coordinated Universal Time)). Learner script caches for eng_for_sin invalidate on the next read.
  [ info] learner-reach: courses.new_app_status = "beta". This course is reachable by learners — the blast radius is live.


══════════════════════════════════════════════════════════════════════════════
  eng_for_sin  ·  course_practice_phrases  ·  phrase eng_for_sin:S0415L03C02  [proposed]
══════════════════════════════════════════════════════════════════════════════
  known_text:
      was  "ලවා ඇහුවොත්"
      now  "ඇහුවොත්"

┌─ DECISION ─────────────────────────────────────────────────────────────────
│  PROCEED-WITH-REPAIRS
│  Safe to apply, but it is not finished when the text lands — 5 follow-up action(s) are required to leave the course whole.
│
│  because:
│    · known_audio_id becomes NULL — a silent slot until new audio is rendered.
│    · 3 intro clip(s) embed the old text and will keep speaking it.
│    · 1 other row(s) carry the identical old text and share its clip. Editing one leaves the course inconsistent.
│
│  before this edit is finished, you must:
│    1. Queue an audio pass so known_audio_id is refilled: node tools/course-optimization/queue-audio-pass.cjs eng_for_sin --reason "<pass>". Do NOT run TTS directly.
│    2. Re-compose 3 presentation clip(s) (POST /regenerate-presentations, which self-scopes to missing), then queue the audio pass.
│    3. Decide explicitly whether the other 1 row(s) should change too — include them in this proposal, or say why not.
│    4. If this text appears in a pod, migrate learner progress FIRST — docs/pods/pod-migration-protocol.md. Never edit a live pod in place.
│    5. ≈4 clip(s) will need rendering. TTS costs money — show a plan and get approval; end the pass by QUEUEING, never by running TTS.
└────────────────────────────────────────────────────────────────────────────

── VERDICT ──────────────────────────────────────────────────────────────────
  [ WARN] SILENT SLOT (known_audio_id): no existing clip matches the new text, so the trigger sets this to NULL. The slot is silent until Generate Missing Audio runs.
  [ WARN] SAME TEXT ELSEWHERE: 1 other row(s) in this course carry the identical old text. Editing one and not the others leaves the course inconsistent — and they share the same clip.
  [DANGER] PRESENTATIONS STALE: 3 intro clip(s) embed the OLD text and will keep speaking it. Presentations are per-LEGO and must be re-composed; the presentation text is built at render time, so this tool reports the clips, not the replacement wording.

── TRIGGER ──────────────────────────────────────────────────────────────────
  relink trigger: trg_null_phrase_audio_on_text_change
  trg_null_phrase_audio_on_text_change fires BEFORE UPDATE and REPLACES the audio link with audio_id_for_text(course, <new text>, role) — which is NULL when nothing matches (silent slot) and an EXISTING clip when something does (silent re-point, possibly to another voice).

── AUDIO LINKS ──────────────────────────────────────────────────────────────
  known_audio_id         nulled-silent
      now:  cef6e489-5284-4950-92e6-289a70778684  [azure_si-LK-SameeraNeural] "ලවා ඇහුවොත්"
      after: NULL (silent)  

── DERIVED ──────────────────────────────────────────────────────────────────
  note: A phrase has no derived rows; its blast radius is its own audio plus the course-wide checks below.

── COURSE-WIDE ──────────────────────────────────────────────────────────────
  tiling: not at risk — No vocab unit removed — nothing can stop tiling.
  same known text elsewhere (1): phrase 415/eng_for_

── PRESENTATIONS ────────────────────────────────────────────────────────────
  3 intro clip(s) embed the old text:
      a04fee0e-12c8-4a7f-b1dd-02b8db73ff68 lego=S0415L02 [azure_si-LK-SameeraNeural] "ඉංග්‍රීසිෙන්. 'ඒ ප්‍රශ්නයක් නෙමෙයි'. 'මාව ලවා ඇහුවොත් ඒ "
      27eca252-4af1-4219-a875-c7d74c713e3a lego=S0415L03 [azure_si-LK-SameeraNeural] "ඉංග්‍රීසිෙන්. 'මාව ලවා ඇහුවොත්'. 'මාව ලවා ඇහුවොත් ඒ ප්‍ර"
      1e2ef882-99ea-456f-867b-d02866dccd79 (component) [azure_si-LK-SameeraNeural] "ඉංග්‍රීසිෙන්. 'ලවා ඇහුවොත්'. '' ඉතින්. :"

── TTS ──────────────────────────────────────────────────────────────────────
  ≈ 4 clip(s) would need rendering (1 row + 3 presentation). NOTHING WAS RENDERED.
  ESTIMATE ONLY. This tool never renders. Content passes end by QUEUEING an audio pass (tools/course-optimization/queue-audio-pass.cjs), never by running TTS.

── DOCTRINE ─────────────────────────────────────────────────────────────────
  [CHECK] pod-content-migration: eng_for_sin has 1 pod(s). If this edit touches text that a pod sentence carries, learner progress MUST be migrated — progress is filed under a slot, not the text, so an in-place edit silently credits a learner with a sentence they never heard.
         → docs/pods/pod-migration-protocol.md (adopted 2026-08-16, plate A-111)
  [ REQD] make-before-break: 1 audio link(s) change. Generate and VERIFY the replacement clip before the old one is touched — never the other way round. Deletion never precedes a verified replacement.
         → docs/architecture/AUDIO_PIPELINE_ARCHITECTURE.md §6b
  [ info] learner-cache-invalidated: course_practice_phrases carries touch_course_content_stamp, so this edit bumps courses.content_stamp (currently Mon Aug 17 2026 12:37:35 GMT+0000 (Coordinated Universal Time)). Learner script caches for eng_for_sin invalidate on the next read.
  [ info] learner-reach: courses.new_app_status = "beta". This course is reachable by learners — the blast radius is live.


══════════════════════════════════════════════════════════════════════════════
  eng_for_sin  ·  course_practice_phrases  ·  phrase eng_for_sin:S0415L03B01  [proposed]
══════════════════════════════════════════════════════════════════════════════
  known_text:
      was  "මාව ලවා ඇහුවොත්"
      now  "මගෙන් ඇහුවොත්"

┌─ DECISION ─────────────────────────────────────────────────────────────────
│  PROCEED-WITH-REPAIRS
│  Safe to apply, but it is not finished when the text lands — 5 follow-up action(s) are required to leave the course whole.
│
│  because:
│    · known_audio_id becomes NULL — a silent slot until new audio is rendered.
│    · 2 intro clip(s) embed the old text and will keep speaking it.
│    · 2 other row(s) carry the identical old text and share its clip. Editing one leaves the course inconsistent.
│
│  before this edit is finished, you must:
│    1. Queue an audio pass so known_audio_id is refilled: node tools/course-optimization/queue-audio-pass.cjs eng_for_sin --reason "<pass>". Do NOT run TTS directly.
│    2. Re-compose 2 presentation clip(s) (POST /regenerate-presentations, which self-scopes to missing), then queue the audio pass.
│    3. Decide explicitly whether the other 2 row(s) should change too — include them in this proposal, or say why not.
│    4. If this text appears in a pod, migrate learner progress FIRST — docs/pods/pod-migration-protocol.md. Never edit a live pod in place.
│    5. ≈3 clip(s) will need rendering. TTS costs money — show a plan and get approval; end the pass by QUEUEING, never by running TTS.
└────────────────────────────────────────────────────────────────────────────

── VERDICT ──────────────────────────────────────────────────────────────────
  [ WARN] SILENT SLOT (known_audio_id): no existing clip matches the new text, so the trigger sets this to NULL. The slot is silent until Generate Missing Audio runs.
  [ WARN] TAUGHT LATE, USED EARLY: this edit introduces 1 word-form(s) that the course uses before (or without ever) teaching them. This is the shape that generalises past the edited row.
  [ WARN] SAME TEXT ELSEWHERE: 2 other row(s) in this course carry the identical old text. Editing one and not the others leaves the course inconsistent — and they share the same clip.
  [DANGER] PRESENTATIONS STALE: 2 intro clip(s) embed the OLD text and will keep speaking it. Presentations are per-LEGO and must be re-composed; the presentation text is built at render time, so this tool reports the clips, not the replacement wording.

── TRIGGER ──────────────────────────────────────────────────────────────────
  relink trigger: trg_null_phrase_audio_on_text_change
  trg_null_phrase_audio_on_text_change fires BEFORE UPDATE and REPLACES the audio link with audio_id_for_text(course, <new text>, role) — which is NULL when nothing matches (silent slot) and an EXISTING clip when something does (silent re-point, possibly to another voice).

── AUDIO LINKS ──────────────────────────────────────────────────────────────
  known_audio_id         nulled-silent
      now:  ca58f18c-6681-44e0-9edd-efa4466530f3  [azure_si-LK-SameeraNeural] "මාව ලවා ඇහුවොත්"
      after: NULL (silent)  

── DERIVED ──────────────────────────────────────────────────────────────────
  note: A phrase has no derived rows; its blast radius is its own audio plus the course-wide checks below.

── COURSE-WIDE ──────────────────────────────────────────────────────────────
  tiling: not at risk — No vocab unit removed — nothing can stop tiling.
  taught late / used early:
      [known] "මගෙන්" — taught at seed NEVER, used 0× from seed null
  same known text elsewhere (2): lego 415:3, phrase 415/eng_for_

── PRESENTATIONS ────────────────────────────────────────────────────────────
  2 intro clip(s) embed the old text:
      a04fee0e-12c8-4a7f-b1dd-02b8db73ff68 lego=S0415L02 [azure_si-LK-SameeraNeural] "ඉංග්‍රීසිෙන්. 'ඒ ප්‍රශ්නයක් නෙමෙයි'. 'මාව ලවා ඇහුවොත් ඒ "
      27eca252-4af1-4219-a875-c7d74c713e3a lego=S0415L03 [azure_si-LK-SameeraNeural] "ඉංග්‍රීසිෙන්. 'මාව ලවා ඇහුවොත්'. 'මාව ලවා ඇහුවොත් ඒ ප්‍ර"

── TTS ──────────────────────────────────────────────────────────────────────
  ≈ 3 clip(s) would need rendering (1 row + 2 presentation). NOTHING WAS RENDERED.
  ESTIMATE ONLY. This tool never renders. Content passes end by QUEUEING an audio pass (tools/course-optimization/queue-audio-pass.cjs), never by running TTS.

── DOCTRINE ─────────────────────────────────────────────────────────────────
  [CHECK] pod-content-migration: eng_for_sin has 1 pod(s). If this edit touches text that a pod sentence carries, learner progress MUST be migrated — progress is filed under a slot, not the text, so an in-place edit silently credits a learner with a sentence they never heard.
         → docs/pods/pod-migration-protocol.md (adopted 2026-08-16, plate A-111)
  [ REQD] make-before-break: 1 audio link(s) change. Generate and VERIFY the replacement clip before the old one is touched — never the other way round. Deletion never precedes a verified replacement.
         → docs/architecture/AUDIO_PIPELINE_ARCHITECTURE.md §6b
  [ info] learner-cache-invalidated: course_practice_phrases carries touch_course_content_stamp, so this edit bumps courses.content_stamp (currently Mon Aug 17 2026 12:37:35 GMT+0000 (Coordinated Universal Time)). Learner script caches for eng_for_sin invalidate on the next read.
  [ info] learner-reach: courses.new_app_status = "beta". This course is reachable by learners — the blast radius is live.


══════════════════════════════════════════════════════════════════════════════
  eng_for_sin  ·  course_practice_phrases  ·  phrase eng_for_sin:S0415L03B02  [proposed]
══════════════════════════════════════════════════════════════════════════════
  known_text:
      was  "මාව ලවා ඇහුවොත් අද"
      now  "මගෙන් ඇහුවොත් අද"

┌─ DECISION ─────────────────────────────────────────────────────────────────
│  PROCEED-WITH-REPAIRS
│  Safe to apply, but it is not finished when the text lands — 4 follow-up action(s) are required to leave the course whole.
│
│  because:
│    · known_audio_id becomes NULL — a silent slot until new audio is rendered.
│    · 1 other row(s) carry the identical old text and share its clip. Editing one leaves the course inconsistent.
│
│  before this edit is finished, you must:
│    1. Queue an audio pass so known_audio_id is refilled: node tools/course-optimization/queue-audio-pass.cjs eng_for_sin --reason "<pass>". Do NOT run TTS directly.
│    2. Decide explicitly whether the other 1 row(s) should change too — include them in this proposal, or say why not.
│    3. If this text appears in a pod, migrate learner progress FIRST — docs/pods/pod-migration-protocol.md. Never edit a live pod in place.
│    4. ≈1 clip(s) will need rendering. TTS costs money — show a plan and get approval; end the pass by QUEUEING, never by running TTS.
└────────────────────────────────────────────────────────────────────────────

── VERDICT ──────────────────────────────────────────────────────────────────
  [ WARN] SILENT SLOT (known_audio_id): no existing clip matches the new text, so the trigger sets this to NULL. The slot is silent until Generate Missing Audio runs.
  [ WARN] TAUGHT LATE, USED EARLY: this edit introduces 1 word-form(s) that the course uses before (or without ever) teaching them. This is the shape that generalises past the edited row.
  [ WARN] SAME TEXT ELSEWHERE: 1 other row(s) in this course carry the identical old text. Editing one and not the others leaves the course inconsistent — and they share the same clip.

── TRIGGER ──────────────────────────────────────────────────────────────────
  relink trigger: trg_null_phrase_audio_on_text_change
  trg_null_phrase_audio_on_text_change fires BEFORE UPDATE and REPLACES the audio link with audio_id_for_text(course, <new text>, role) — which is NULL when nothing matches (silent slot) and an EXISTING clip when something does (silent re-point, possibly to another voice).

── AUDIO LINKS ──────────────────────────────────────────────────────────────
  known_audio_id         nulled-silent
      now:  c4bd47a5-6e17-40b8-b8cd-84ed511c7b92  [azure_si-LK-SameeraNeural] "මාව ලවා ඇහුවොත් අද"
      after: NULL (silent)  

── DERIVED ──────────────────────────────────────────────────────────────────
  note: A phrase has no derived rows; its blast radius is its own audio plus the course-wide checks below.

── COURSE-WIDE ──────────────────────────────────────────────────────────────
  tiling: not at risk — No vocab unit removed — nothing can stop tiling.
  taught late / used early:
      [known] "මගෙන්" — taught at seed NEVER, used 0× from seed null
  same known text elsewhere (1): phrase 415/eng_for_

── TTS ──────────────────────────────────────────────────────────────────────
  ≈ 1 clip(s) would need rendering (1 row + 0 presentation). NOTHING WAS RENDERED.
  ESTIMATE ONLY. This tool never renders. Content passes end by QUEUEING an audio pass (tools/course-optimization/queue-audio-pass.cjs), never by running TTS.

── DOCTRINE ─────────────────────────────────────────────────────────────────
  [CHECK] pod-content-migration: eng_for_sin has 1 pod(s). If this edit touches text that a pod sentence carries, learner progress MUST be migrated — progress is filed under a slot, not the text, so an in-place edit silently credits a learner with a sentence they never heard.
         → docs/pods/pod-migration-protocol.md (adopted 2026-08-16, plate A-111)
  [ REQD] make-before-break: 1 audio link(s) change. Generate and VERIFY the replacement clip before the old one is touched — never the other way round. Deletion never precedes a verified replacement.
         → docs/architecture/AUDIO_PIPELINE_ARCHITECTURE.md §6b
  [ info] learner-cache-invalidated: course_practice_phrases carries touch_course_content_stamp, so this edit bumps courses.content_stamp (currently Mon Aug 17 2026 12:37:35 GMT+0000 (Coordinated Universal Time)). Learner script caches for eng_for_sin invalidate on the next read.
  [ info] learner-reach: courses.new_app_status = "beta". This course is reachable by learners — the blast radius is live.


══════════════════════════════════════════════════════════════════════════════
  eng_for_sin  ·  course_practice_phrases  ·  phrase eng_for_sin:S0415L03B03  [proposed]
══════════════════════════════════════════════════════════════════════════════
  known_text:
      was  "මාව ලවා ඇහුවොත් ගොඩ"
      now  "මගෙන් ඇහුවොත් ගොඩ"

┌─ DECISION ─────────────────────────────────────────────────────────────────
│  PROCEED-WITH-REPAIRS
│  Safe to apply, but it is not finished when the text lands — 4 follow-up action(s) are required to leave the course whole.
│
│  because:
│    · known_audio_id becomes NULL — a silent slot until new audio is rendered.
│    · 1 other row(s) carry the identical old text and share its clip. Editing one leaves the course inconsistent.
│
│  before this edit is finished, you must:
│    1. Queue an audio pass so known_audio_id is refilled: node tools/course-optimization/queue-audio-pass.cjs eng_for_sin --reason "<pass>". Do NOT run TTS directly.
│    2. Decide explicitly whether the other 1 row(s) should change too — include them in this proposal, or say why not.
│    3. If this text appears in a pod, migrate learner progress FIRST — docs/pods/pod-migration-protocol.md. Never edit a live pod in place.
│    4. ≈1 clip(s) will need rendering. TTS costs money — show a plan and get approval; end the pass by QUEUEING, never by running TTS.
└────────────────────────────────────────────────────────────────────────────

── VERDICT ──────────────────────────────────────────────────────────────────
  [ WARN] SILENT SLOT (known_audio_id): no existing clip matches the new text, so the trigger sets this to NULL. The slot is silent until Generate Missing Audio runs.
  [ WARN] TAUGHT LATE, USED EARLY: this edit introduces 1 word-form(s) that the course uses before (or without ever) teaching them. This is the shape that generalises past the edited row.
  [ WARN] SAME TEXT ELSEWHERE: 1 other row(s) in this course carry the identical old text. Editing one and not the others leaves the course inconsistent — and they share the same clip.

── TRIGGER ──────────────────────────────────────────────────────────────────
  relink trigger: trg_null_phrase_audio_on_text_change
  trg_null_phrase_audio_on_text_change fires BEFORE UPDATE and REPLACES the audio link with audio_id_for_text(course, <new text>, role) — which is NULL when nothing matches (silent slot) and an EXISTING clip when something does (silent re-point, possibly to another voice).

── AUDIO LINKS ──────────────────────────────────────────────────────────────
  known_audio_id         nulled-silent
      now:  e30d5d15-b155-437d-a129-db15126f91a1  [azure_si-LK-SameeraNeural] "මාව ලවා ඇහුවොත් ගොඩ"
      after: NULL (silent)  

── DERIVED ──────────────────────────────────────────────────────────────────
  note: A phrase has no derived rows; its blast radius is its own audio plus the course-wide checks below.

── COURSE-WIDE ──────────────────────────────────────────────────────────────
  tiling: not at risk — No vocab unit removed — nothing can stop tiling.
  taught late / used early:
      [known] "මගෙන්" — taught at seed NEVER, used 0× from seed null
  same known text elsewhere (1): phrase 415/eng_for_

── TTS ──────────────────────────────────────────────────────────────────────
  ≈ 1 clip(s) would need rendering (1 row + 0 presentation). NOTHING WAS RENDERED.
  ESTIMATE ONLY. This tool never renders. Content passes end by QUEUEING an audio pass (tools/course-optimization/queue-audio-pass.cjs), never by running TTS.

── DOCTRINE ─────────────────────────────────────────────────────────────────
  [CHECK] pod-content-migration: eng_for_sin has 1 pod(s). If this edit touches text that a pod sentence carries, learner progress MUST be migrated — progress is filed under a slot, not the text, so an in-place edit silently credits a learner with a sentence they never heard.
         → docs/pods/pod-migration-protocol.md (adopted 2026-08-16, plate A-111)
  [ REQD] make-before-break: 1 audio link(s) change. Generate and VERIFY the replacement clip before the old one is touched — never the other way round. Deletion never precedes a verified replacement.
         → docs/architecture/AUDIO_PIPELINE_ARCHITECTURE.md §6b
  [ info] learner-cache-invalidated: course_practice_phrases carries touch_course_content_stamp, so this edit bumps courses.content_stamp (currently Mon Aug 17 2026 12:37:35 GMT+0000 (Coordinated Universal Time)). Learner script caches for eng_for_sin invalidate on the next read.
  [ info] learner-reach: courses.new_app_status = "beta". This course is reachable by learners — the blast radius is live.


══════════════════════════════════════════════════════════════════════════════
  eng_for_sin  ·  course_practice_phrases  ·  phrase eng_for_sin:S0415L03U01  [proposed]
══════════════════════════════════════════════════════════════════════════════
  known_text:
      was  "මාව ලවා ඇහුවොත් ඒ ප්‍රශ්නයක් නෙමෙයි"
      now  "මගෙන් ඇහුවොත් ඒ ප්‍රශ්නයක් නෙමෙයි"

┌─ DECISION ─────────────────────────────────────────────────────────────────
│  PROCEED-WITH-REPAIRS
│  Safe to apply, but it is not finished when the text lands — 5 follow-up action(s) are required to leave the course whole.
│
│  because:
│    · known_audio_id becomes NULL — a silent slot until new audio is rendered.
│    · 2 intro clip(s) embed the old text and will keep speaking it.
│    · 2 other row(s) carry the identical old text and share its clip. Editing one leaves the course inconsistent.
│
│  before this edit is finished, you must:
│    1. Queue an audio pass so known_audio_id is refilled: node tools/course-optimization/queue-audio-pass.cjs eng_for_sin --reason "<pass>". Do NOT run TTS directly.
│    2. Re-compose 2 presentation clip(s) (POST /regenerate-presentations, which self-scopes to missing), then queue the audio pass.
│    3. Decide explicitly whether the other 2 row(s) should change too — include them in this proposal, or say why not.
│    4. If this text appears in a pod, migrate learner progress FIRST — docs/pods/pod-migration-protocol.md. Never edit a live pod in place.
│    5. ≈3 clip(s) will need rendering. TTS costs money — show a plan and get approval; end the pass by QUEUEING, never by running TTS.
└────────────────────────────────────────────────────────────────────────────

── VERDICT ──────────────────────────────────────────────────────────────────
  [ WARN] SILENT SLOT (known_audio_id): no existing clip matches the new text, so the trigger sets this to NULL. The slot is silent until Generate Missing Audio runs.
  [ WARN] TAUGHT LATE, USED EARLY: this edit introduces 1 word-form(s) that the course uses before (or without ever) teaching them. This is the shape that generalises past the edited row.
  [ WARN] SAME TEXT ELSEWHERE: 2 other row(s) in this course carry the identical old text. Editing one and not the others leaves the course inconsistent — and they share the same clip.
  [DANGER] PRESENTATIONS STALE: 2 intro clip(s) embed the OLD text and will keep speaking it. Presentations are per-LEGO and must be re-composed; the presentation text is built at render time, so this tool reports the clips, not the replacement wording.

── TRIGGER ──────────────────────────────────────────────────────────────────
  relink trigger: trg_null_phrase_audio_on_text_change
  trg_null_phrase_audio_on_text_change fires BEFORE UPDATE and REPLACES the audio link with audio_id_for_text(course, <new text>, role) — which is NULL when nothing matches (silent slot) and an EXISTING clip when something does (silent re-point, possibly to another voice).

── AUDIO LINKS ──────────────────────────────────────────────────────────────
  known_audio_id         nulled-silent
      now:  1d4d22a8-e913-4484-8ec2-3c1c8e244aec  [azure_si-LK-SameeraNeural] "මාව ලවා ඇහුවොත් ඒ ප්‍රශ්නයක් නෙමෙයි."
      after: NULL (silent)  

── DERIVED ──────────────────────────────────────────────────────────────────
  note: A phrase has no derived rows; its blast radius is its own audio plus the course-wide checks below.

── COURSE-WIDE ──────────────────────────────────────────────────────────────
  tiling: not at risk — No vocab unit removed — nothing can stop tiling.
  taught late / used early:
      [known] "මගෙන්" — taught at seed NEVER, used 0× from seed null
  same known text elsewhere (2): seed 415, phrase 415/eng_for_

── PRESENTATIONS ────────────────────────────────────────────────────────────
  2 intro clip(s) embed the old text:
      a04fee0e-12c8-4a7f-b1dd-02b8db73ff68 lego=S0415L02 [azure_si-LK-SameeraNeural] "ඉංග්‍රීසිෙන්. 'ඒ ප්‍රශ්නයක් නෙමෙයි'. 'මාව ලවා ඇහුවොත් ඒ "
      27eca252-4af1-4219-a875-c7d74c713e3a lego=S0415L03 [azure_si-LK-SameeraNeural] "ඉංග්‍රීසිෙන්. 'මාව ලවා ඇහුවොත්'. 'මාව ලවා ඇහුවොත් ඒ ප්‍ර"

── TTS ──────────────────────────────────────────────────────────────────────
  ≈ 3 clip(s) would need rendering (1 row + 2 presentation). NOTHING WAS RENDERED.
  ESTIMATE ONLY. This tool never renders. Content passes end by QUEUEING an audio pass (tools/course-optimization/queue-audio-pass.cjs), never by running TTS.

── DOCTRINE ─────────────────────────────────────────────────────────────────
  [CHECK] pod-content-migration: eng_for_sin has 1 pod(s). If this edit touches text that a pod sentence carries, learner progress MUST be migrated — progress is filed under a slot, not the text, so an in-place edit silently credits a learner with a sentence they never heard.
         → docs/pods/pod-migration-protocol.md (adopted 2026-08-16, plate A-111)
  [ REQD] make-before-break: 1 audio link(s) change. Generate and VERIFY the replacement clip before the old one is touched — never the other way round. Deletion never precedes a verified replacement.
         → docs/architecture/AUDIO_PIPELINE_ARCHITECTURE.md §6b
  [ info] learner-cache-invalidated: course_practice_phrases carries touch_course_content_stamp, so this edit bumps courses.content_stamp (currently Mon Aug 17 2026 12:37:35 GMT+0000 (Coordinated Universal Time)). Learner script caches for eng_for_sin invalidate on the next read.
  [ info] learner-reach: courses.new_app_status = "beta". This course is reachable by learners — the blast radius is live.


══════════════════════════════════════════════════════════════════════════════
  eng_for_sin  ·  course_practice_phrases  ·  phrase eng_for_sin:S0415L03U02  [proposed]
══════════════════════════════════════════════════════════════════════════════
  known_text:
      was  "ඔහු කිව්ව මාව ලවා ඇහුවොත්"
      now  "ඔහු කිව්ව මගෙන් ඇහුවොත්"

┌─ DECISION ─────────────────────────────────────────────────────────────────
│  PROCEED-WITH-REPAIRS
│  Safe to apply, but it is not finished when the text lands — 4 follow-up action(s) are required to leave the course whole.
│
│  because:
│    · known_audio_id becomes NULL — a silent slot until new audio is rendered.
│    · 1 other row(s) carry the identical old text and share its clip. Editing one leaves the course inconsistent.
│
│  before this edit is finished, you must:
│    1. Queue an audio pass so known_audio_id is refilled: node tools/course-optimization/queue-audio-pass.cjs eng_for_sin --reason "<pass>". Do NOT run TTS directly.
│    2. Decide explicitly whether the other 1 row(s) should change too — include them in this proposal, or say why not.
│    3. If this text appears in a pod, migrate learner progress FIRST — docs/pods/pod-migration-protocol.md. Never edit a live pod in place.
│    4. ≈1 clip(s) will need rendering. TTS costs money — show a plan and get approval; end the pass by QUEUEING, never by running TTS.
└────────────────────────────────────────────────────────────────────────────

── VERDICT ──────────────────────────────────────────────────────────────────
  [ WARN] SILENT SLOT (known_audio_id): no existing clip matches the new text, so the trigger sets this to NULL. The slot is silent until Generate Missing Audio runs.
  [ WARN] TAUGHT LATE, USED EARLY: this edit introduces 1 word-form(s) that the course uses before (or without ever) teaching them. This is the shape that generalises past the edited row.
  [ WARN] SAME TEXT ELSEWHERE: 1 other row(s) in this course carry the identical old text. Editing one and not the others leaves the course inconsistent — and they share the same clip.

── TRIGGER ──────────────────────────────────────────────────────────────────
  relink trigger: trg_null_phrase_audio_on_text_change
  trg_null_phrase_audio_on_text_change fires BEFORE UPDATE and REPLACES the audio link with audio_id_for_text(course, <new text>, role) — which is NULL when nothing matches (silent slot) and an EXISTING clip when something does (silent re-point, possibly to another voice).

── AUDIO LINKS ──────────────────────────────────────────────────────────────
  known_audio_id         nulled-silent
      now:  86a9e1f2-28fe-4cac-a6d9-fdae6d0f679f  [azure_si-LK-SameeraNeural] "ඔහු කිව්ව මාව ලවා ඇහුවොත්"
      after: NULL (silent)  

── DERIVED ──────────────────────────────────────────────────────────────────
  note: A phrase has no derived rows; its blast radius is its own audio plus the course-wide checks below.

── COURSE-WIDE ──────────────────────────────────────────────────────────────
  tiling: not at risk — No vocab unit removed — nothing can stop tiling.
  taught late / used early:
      [known] "මගෙන්" — taught at seed NEVER, used 0× from seed null
  same known text elsewhere (1): phrase 415/eng_for_

── TTS ──────────────────────────────────────────────────────────────────────
  ≈ 1 clip(s) would need rendering (1 row + 0 presentation). NOTHING WAS RENDERED.
  ESTIMATE ONLY. This tool never renders. Content passes end by QUEUEING an audio pass (tools/course-optimization/queue-audio-pass.cjs), never by running TTS.

── DOCTRINE ─────────────────────────────────────────────────────────────────
  [CHECK] pod-content-migration: eng_for_sin has 1 pod(s). If this edit touches text that a pod sentence carries, learner progress MUST be migrated — progress is filed under a slot, not the text, so an in-place edit silently credits a learner with a sentence they never heard.
         → docs/pods/pod-migration-protocol.md (adopted 2026-08-16, plate A-111)
  [ REQD] make-before-break: 1 audio link(s) change. Generate and VERIFY the replacement clip before the old one is touched — never the other way round. Deletion never precedes a verified replacement.
         → docs/architecture/AUDIO_PIPELINE_ARCHITECTURE.md §6b
  [ info] learner-cache-invalidated: course_practice_phrases carries touch_course_content_stamp, so this edit bumps courses.content_stamp (currently Mon Aug 17 2026 12:37:35 GMT+0000 (Coordinated Universal Time)). Learner script caches for eng_for_sin invalidate on the next read.
  [ info] learner-reach: courses.new_app_status = "beta". This course is reachable by learners — the blast radius is live.


══════════════════════════════════════════════════════════════════════════════
  eng_for_sin  ·  course_practice_phrases  ·  phrase eng_for_sin:S0415L03U03  [proposed]
══════════════════════════════════════════════════════════════════════════════
  known_text:
      was  "මම හිතනවා මාව ලවා ඇහුවොත්"
      now  "මම හිතනවා මගෙන් ඇහුවොත්"

┌─ DECISION ─────────────────────────────────────────────────────────────────
│  PROCEED-WITH-REPAIRS
│  Safe to apply, but it is not finished when the text lands — 4 follow-up action(s) are required to leave the course whole.
│
│  because:
│    · known_audio_id becomes NULL — a silent slot until new audio is rendered.
│    · 1 other row(s) carry the identical old text and share its clip. Editing one leaves the course inconsistent.
│
│  before this edit is finished, you must:
│    1. Queue an audio pass so known_audio_id is refilled: node tools/course-optimization/queue-audio-pass.cjs eng_for_sin --reason "<pass>". Do NOT run TTS directly.
│    2. Decide explicitly whether the other 1 row(s) should change too — include them in this proposal, or say why not.
│    3. If this text appears in a pod, migrate learner progress FIRST — docs/pods/pod-migration-protocol.md. Never edit a live pod in place.
│    4. ≈1 clip(s) will need rendering. TTS costs money — show a plan and get approval; end the pass by QUEUEING, never by running TTS.
└────────────────────────────────────────────────────────────────────────────

── VERDICT ──────────────────────────────────────────────────────────────────
  [ WARN] SILENT SLOT (known_audio_id): no existing clip matches the new text, so the trigger sets this to NULL. The slot is silent until Generate Missing Audio runs.
  [ WARN] TAUGHT LATE, USED EARLY: this edit introduces 1 word-form(s) that the course uses before (or without ever) teaching them. This is the shape that generalises past the edited row.
  [ WARN] SAME TEXT ELSEWHERE: 1 other row(s) in this course carry the identical old text. Editing one and not the others leaves the course inconsistent — and they share the same clip.

── TRIGGER ──────────────────────────────────────────────────────────────────
  relink trigger: trg_null_phrase_audio_on_text_change
  trg_null_phrase_audio_on_text_change fires BEFORE UPDATE and REPLACES the audio link with audio_id_for_text(course, <new text>, role) — which is NULL when nothing matches (silent slot) and an EXISTING clip when something does (silent re-point, possibly to another voice).

── AUDIO LINKS ──────────────────────────────────────────────────────────────
  known_audio_id         nulled-silent
      now:  6aa8140a-8013-4c0e-9bdc-33d60e7358ca  [azure_si-LK-SameeraNeural] "මම හිතනවා මාව ලවා ඇහුවොත්"
      after: NULL (silent)  

── DERIVED ──────────────────────────────────────────────────────────────────
  note: A phrase has no derived rows; its blast radius is its own audio plus the course-wide checks below.

── COURSE-WIDE ──────────────────────────────────────────────────────────────
  tiling: not at risk — No vocab unit removed — nothing can stop tiling.
  taught late / used early:
      [known] "මගෙන්" — taught at seed NEVER, used 0× from seed null
  same known text elsewhere (1): phrase 415/eng_for_

── TTS ──────────────────────────────────────────────────────────────────────
  ≈ 1 clip(s) would need rendering (1 row + 0 presentation). NOTHING WAS RENDERED.
  ESTIMATE ONLY. This tool never renders. Content passes end by QUEUEING an audio pass (tools/course-optimization/queue-audio-pass.cjs), never by running TTS.

── DOCTRINE ─────────────────────────────────────────────────────────────────
  [CHECK] pod-content-migration: eng_for_sin has 1 pod(s). If this edit touches text that a pod sentence carries, learner progress MUST be migrated — progress is filed under a slot, not the text, so an in-place edit silently credits a learner with a sentence they never heard.
         → docs/pods/pod-migration-protocol.md (adopted 2026-08-16, plate A-111)
  [ REQD] make-before-break: 1 audio link(s) change. Generate and VERIFY the replacement clip before the old one is touched — never the other way round. Deletion never precedes a verified replacement.
         → docs/architecture/AUDIO_PIPELINE_ARCHITECTURE.md §6b
  [ info] learner-cache-invalidated: course_practice_phrases carries touch_course_content_stamp, so this edit bumps courses.content_stamp (currently Mon Aug 17 2026 12:37:35 GMT+0000 (Coordinated Universal Time)). Learner script caches for eng_for_sin invalidate on the next read.
  [ info] learner-reach: courses.new_app_status = "beta". This course is reachable by learners — the blast radius is live.


══════════════════════════════════════════════════════════════════════════════
  eng_for_sin  ·  course_practice_phrases  ·  phrase eng_for_sin:S0415L03U04  [proposed]
══════════════════════════════════════════════════════════════════════════════
  known_text:
      was  "මාව ලවා ඇහුවොත් ක්‍රීඩාංගනේ ළඟ"
      now  "මගෙන් ඇහුවොත් ක්‍රීඩාංගනේ ළඟ"

┌─ DECISION ─────────────────────────────────────────────────────────────────
│  PROCEED-WITH-REPAIRS
│  Safe to apply, but it is not finished when the text lands — 4 follow-up action(s) are required to leave the course whole.
│
│  because:
│    · known_audio_id becomes NULL — a silent slot until new audio is rendered.
│    · 1 other row(s) carry the identical old text and share its clip. Editing one leaves the course inconsistent.
│
│  before this edit is finished, you must:
│    1. Queue an audio pass so known_audio_id is refilled: node tools/course-optimization/queue-audio-pass.cjs eng_for_sin --reason "<pass>". Do NOT run TTS directly.
│    2. Decide explicitly whether the other 1 row(s) should change too — include them in this proposal, or say why not.
│    3. If this text appears in a pod, migrate learner progress FIRST — docs/pods/pod-migration-protocol.md. Never edit a live pod in place.
│    4. ≈1 clip(s) will need rendering. TTS costs money — show a plan and get approval; end the pass by QUEUEING, never by running TTS.
└────────────────────────────────────────────────────────────────────────────

── VERDICT ──────────────────────────────────────────────────────────────────
  [ WARN] SILENT SLOT (known_audio_id): no existing clip matches the new text, so the trigger sets this to NULL. The slot is silent until Generate Missing Audio runs.
  [ WARN] TAUGHT LATE, USED EARLY: this edit introduces 1 word-form(s) that the course uses before (or without ever) teaching them. This is the shape that generalises past the edited row.
  [ WARN] SAME TEXT ELSEWHERE: 1 other row(s) in this course carry the identical old text. Editing one and not the others leaves the course inconsistent — and they share the same clip.

── TRIGGER ──────────────────────────────────────────────────────────────────
  relink trigger: trg_null_phrase_audio_on_text_change
  trg_null_phrase_audio_on_text_change fires BEFORE UPDATE and REPLACES the audio link with audio_id_for_text(course, <new text>, role) — which is NULL when nothing matches (silent slot) and an EXISTING clip when something does (silent re-point, possibly to another voice).

── AUDIO LINKS ──────────────────────────────────────────────────────────────
  known_audio_id         nulled-silent
      now:  8facea03-fe8e-41ed-90c6-f5092b5ce12c  [azure_si-LK-SameeraNeural] "මාව ලවා ඇහුවොත් ක්‍රීඩාංගනේ ළඟ"
      after: NULL (silent)  

── DERIVED ──────────────────────────────────────────────────────────────────
  note: A phrase has no derived rows; its blast radius is its own audio plus the course-wide checks below.

── COURSE-WIDE ──────────────────────────────────────────────────────────────
  tiling: not at risk — No vocab unit removed — nothing can stop tiling.
  taught late / used early:
      [known] "මගෙන්" — taught at seed NEVER, used 0× from seed null
  same known text elsewhere (1): phrase 415/eng_for_

── TTS ──────────────────────────────────────────────────────────────────────
  ≈ 1 clip(s) would need rendering (1 row + 0 presentation). NOTHING WAS RENDERED.
  ESTIMATE ONLY. This tool never renders. Content passes end by QUEUEING an audio pass (tools/course-optimization/queue-audio-pass.cjs), never by running TTS.

── DOCTRINE ─────────────────────────────────────────────────────────────────
  [CHECK] pod-content-migration: eng_for_sin has 1 pod(s). If this edit touches text that a pod sentence carries, learner progress MUST be migrated — progress is filed under a slot, not the text, so an in-place edit silently credits a learner with a sentence they never heard.
         → docs/pods/pod-migration-protocol.md (adopted 2026-08-16, plate A-111)
  [ REQD] make-before-break: 1 audio link(s) change. Generate and VERIFY the replacement clip before the old one is touched — never the other way round. Deletion never precedes a verified replacement.
         → docs/architecture/AUDIO_PIPELINE_ARCHITECTURE.md §6b
  [ info] learner-cache-invalidated: course_practice_phrases carries touch_course_content_stamp, so this edit bumps courses.content_stamp (currently Mon Aug 17 2026 12:37:35 GMT+0000 (Coordinated Universal Time)). Learner script caches for eng_for_sin invalidate on the next read.
  [ info] learner-reach: courses.new_app_status = "beta". This course is reachable by learners — the blast radius is live.


══════════════════════════════════════════════════════════════════════════════
  eng_for_sin  ·  course_practice_phrases  ·  phrase eng_for_sin:S0415L03U05  [proposed]
══════════════════════════════════════════════════════════════════════════════
  known_text:
      was  "මාව ලවා ඇහුවොත් ඉහළ ළඟ"
      now  "මගෙන් ඇහුවොත් ඉහළ ළඟ"

┌─ DECISION ─────────────────────────────────────────────────────────────────
│  PROCEED-WITH-REPAIRS
│  Safe to apply, but it is not finished when the text lands — 4 follow-up action(s) are required to leave the course whole.
│
│  because:
│    · known_audio_id becomes NULL — a silent slot until new audio is rendered.
│    · 1 other row(s) carry the identical old text and share its clip. Editing one leaves the course inconsistent.
│
│  before this edit is finished, you must:
│    1. Queue an audio pass so known_audio_id is refilled: node tools/course-optimization/queue-audio-pass.cjs eng_for_sin --reason "<pass>". Do NOT run TTS directly.
│    2. Decide explicitly whether the other 1 row(s) should change too — include them in this proposal, or say why not.
│    3. If this text appears in a pod, migrate learner progress FIRST — docs/pods/pod-migration-protocol.md. Never edit a live pod in place.
│    4. ≈1 clip(s) will need rendering. TTS costs money — show a plan and get approval; end the pass by QUEUEING, never by running TTS.
└────────────────────────────────────────────────────────────────────────────

── VERDICT ──────────────────────────────────────────────────────────────────
  [ WARN] SILENT SLOT (known_audio_id): no existing clip matches the new text, so the trigger sets this to NULL. The slot is silent until Generate Missing Audio runs.
  [ WARN] TAUGHT LATE, USED EARLY: this edit introduces 1 word-form(s) that the course uses before (or without ever) teaching them. This is the shape that generalises past the edited row.
  [ WARN] SAME TEXT ELSEWHERE: 1 other row(s) in this course carry the identical old text. Editing one and not the others leaves the course inconsistent — and they share the same clip.

── TRIGGER ──────────────────────────────────────────────────────────────────
  relink trigger: trg_null_phrase_audio_on_text_change
  trg_null_phrase_audio_on_text_change fires BEFORE UPDATE and REPLACES the audio link with audio_id_for_text(course, <new text>, role) — which is NULL when nothing matches (silent slot) and an EXISTING clip when something does (silent re-point, possibly to another voice).

── AUDIO LINKS ──────────────────────────────────────────────────────────────
  known_audio_id         nulled-silent
      now:  374bba6b-8bf1-42c0-958c-491678355d5a  [azure_si-LK-SameeraNeural] "මාව ලවා ඇහුවොත් ඉහළ ළඟ"
      after: NULL (silent)  

── DERIVED ──────────────────────────────────────────────────────────────────
  note: A phrase has no derived rows; its blast radius is its own audio plus the course-wide checks below.

── COURSE-WIDE ──────────────────────────────────────────────────────────────
  tiling: not at risk — No vocab unit removed — nothing can stop tiling.
  taught late / used early:
      [known] "මගෙන්" — taught at seed NEVER, used 0× from seed null
  same known text elsewhere (1): phrase 415/eng_for_

── TTS ──────────────────────────────────────────────────────────────────────
  ≈ 1 clip(s) would need rendering (1 row + 0 presentation). NOTHING WAS RENDERED.
  ESTIMATE ONLY. This tool never renders. Content passes end by QUEUEING an audio pass (tools/course-optimization/queue-audio-pass.cjs), never by running TTS.

── DOCTRINE ─────────────────────────────────────────────────────────────────
  [CHECK] pod-content-migration: eng_for_sin has 1 pod(s). If this edit touches text that a pod sentence carries, learner progress MUST be migrated — progress is filed under a slot, not the text, so an in-place edit silently credits a learner with a sentence they never heard.
         → docs/pods/pod-migration-protocol.md (adopted 2026-08-16, plate A-111)
  [ REQD] make-before-break: 1 audio link(s) change. Generate and VERIFY the replacement clip before the old one is touched — never the other way round. Deletion never precedes a verified replacement.
         → docs/architecture/AUDIO_PIPELINE_ARCHITECTURE.md §6b
  [ info] learner-cache-invalidated: course_practice_phrases carries touch_course_content_stamp, so this edit bumps courses.content_stamp (currently Mon Aug 17 2026 12:37:35 GMT+0000 (Coordinated Universal Time)). Learner script caches for eng_for_sin invalidate on the next read.
  [ info] learner-reach: courses.new_app_status = "beta". This course is reachable by learners — the blast radius is live.


  36 edit(s) checked · DECISION: RECONSIDER · 39 danger · 124 warn · ≈150 clip(s) would need rendering · 0 rendered · 0 rows written

  JSON report → /home/tomcassidy/SSi/ssi-dashboard-v7-clean/docs/a134-sin-ask-hear/impact.json
IMPACT EXIT 20
