# eng_for_sin ask/hear — apply log

*2026-08-17. Gate lifted by #932 (seed + phrase triggers live). **Applied to production Supabase.***

## Gate verified independently before writing

Queried `pg_trigger` directly rather than taking the report on trust:

| table | trigger | enabled | drop log | voice-guarded |
|---|---|---|---|---|
| `course_seeds` | `trg_null_seed_audio_on_text_change` | ✓ | **✓** | **✓** |
| `course_practice_phrases` | `trg_null_phrase_audio_on_text_change` | ✓ | **✓** | **✓** |
| `course_legos` | `trg_null_lego_audio_on_text_change` | ✓ | **✗** | **✗** |

`course_legos` is still on the pre-2026-08-06 rule — no drop log, no voice guard — exactly as Tom
warned. Every lego link was therefore verified by hand rather than trusted to the trigger.

Also checked before writing: no other agent live on `eng_for_sin` (only this session), and a
182-row before-image snapshot taken for rollback (`.a74-scratch/sin-apply/before-image.json`).

## What was applied — 36 edits, one transaction

Seeds 380, 381, 382, 415, 465 — 5 seed rows, 4 legos, 27 phrases. `ON_ERROR_STOP=1`, all
`UPDATE 1`, committed.

| seed | class | known side | English |
|---|---|---|---|
| 380 | collision | `මම ඇයගෙන් ඇහුවා` | "I asked **her** what" |
| 381 | contradiction (markable) | `මම ඔහුගෙන් ඇහුවේ නෑ` | "I didn't ask **him** if" |
| 382 | contradiction (markable) | `ඔයා ඔහුගෙන් ඇහුවාද` | "did you ask **him**" |
| 415 | naturalness | `මගෙන් ඇහුවොත්` (was `මාව ලවා`) | unchanged |
| 465 | naturalness | `ඇගෙන්` added | unchanged |

**Held back** pending #941's adversarial verdict: seeds 420 and 432 (the involuntary-form class) and
the 420 one-field seed-English referent fix.

## Audio duty — 72 links dropped, ZERO silent re-points

The new triggers logged every drop to `content_audio_link_drops`:

| table | column | n | reason |
|---|---|---|---|
| `course_seeds` | known_audio_id | 5 | `nulled-no-same-voice-clip-for-new-text` |
| `course_seeds` | target1_audio_id | 3 | same |
| `course_seeds` | target2_audio_id | 3 | same |
| `course_practice_phrases` | known_audio_id | 27 | same |
| `course_practice_phrases` | target1_audio_id | 17 | same |
| `course_practice_phrases` | target2_audio_id | 17 | same |

**72 drops across 32 rows, every one a NULL.** Not a single silent re-point to another voice — the
hazard the guard was built for did not fire once.

The **seed** rows nulling is itself the proof #932's work landed: before today a seed edit did
nothing at all and left the link pointing at a recording of the old sentence, invisibly.

### Legos, verified by hand (no log exists)

| lego | known | target1 | presentation |
|---|---|---|---|
| `S0380L03` | NULL | NULL | NULL |
| `S0381L03` | NULL | NULL | NULL |
| `S0382L04` | NULL | NULL | NULL |
| `S0415L03` | NULL | **kept `xai_bedd6226`** | NULL |

`S0415L03` keeping its target1 is **correct** — its English never changed, so its English clip is
still valid. No lego link moved to a different voice.

Phrase-level check on the exact 27 edited ids: 27/27 known nulled, 17 target1 nulled, **10 target1
correctly kept** (the 415 family, English unchanged).

### Pass queued, not rendered

`eng_for_sin` already had a **pending, Tom-approved pod-0 request**. `queue-audio-pass` has since
been fixed to **append** rather than overwrite (`existing.reason || new`), so the pod-0 text survives
verbatim and the A-134 pass is appended after it, with its fulfiller named (`POST /generate`, *not*
`/generate-pods`) so the two cannot be confused in the one row they now share. **No TTS was run.**

`courses.content_stamp` bumped to `2026-08-17 14:37:48+00`, so learner script caches invalidate on
next read. `audio_stamp` deliberately **not** bumped — nothing relinked, everything nulled; the pass
will move it.

## Production verification — and its honest limit

- `course_round_index` carries **structure only** (`course_code, round_index, lego_id, seed_number,
  lego_index`) — **no text** — so a text edit cannot stale it. No refresh needed. Production serves
  `version 1164`, 1240 rounds, `S0380L03` present.
- **The deep cards cannot be eyeballed through the ungated endpoint.** `api/courses/eng_for_sin/cycles`
  returns `{"error":"Subscription required","reason":"preview_only"}` for seed 380+, which is far past
  the preview cap. So the served-text assertion rests on the **direct DB read**, not on a production
  response. Stated as a limit rather than glossed.

## The finding that reframes the original defect

**`S0382L04` was never on the learner path.**

| `is_new` | legos | in `course_round_index` | absent |
|---|---|---|---|
| `true` | 1240 | **1240** | 0 |
| `false` | 60 | **0** | **60** |

A clean split with no exceptions. `S0382L04` is `is_new=false`, so **no learner ever heard "did you
hear" from it** — the card the whole ruling was named after was a data defect, silent in delivery.

Of the four contradictions, the three `is_new=true` ones — `S0381L03` (now fixed), `S0420L03` and
`S0432L01` (both still pending #941) — were the genuinely learner-facing ones, along with the 380
collision. Worth separating severity honestly: one headline defect was invisible, and two of the
live ones were found only after the census regex was corrected.

### Open decision this raises

`S0382L04`'s text now differs from `S0366L03`, the card it was a repeat of. It is arguably no longer
a repeat. Flipping `is_new` to true would put it **on** the learner path and it would then need
practice phrases (it has none, which is correct for a repeat). That is a deliberate decision, not a
side effect, so it has **not** been touched.

## Still open

- **#941** — adversarial verdict on the 420/432 re-authored wordings. 420 and 432 are unapplied.
- Presentations: `POST /regenerate-presentations` (self-scopes to missing) before the pass renders.
- `මගෙන්` taught nowhere — Kai's ruling outstanding; it is now **live** in seed 415.
- `අහන්නේ` taught-late/used-early at 420 (inherited, not created).
- Speaker list: dative-vs-ablative addressee split; `මම විමසන්නවා` malformed; `වගේ ඇහෙනවා` vs bare;
  the elliptical `ඔහු කොච්චර කාලද` glossed "how long he stayed".

Rollback: `.a74-scratch/sin-apply/before-image.json`, 182 rows with all audio ids.

---

# Render — completed 2026-08-17

Kai's Sinhala audio ruling covers this; Tom released it explicitly.

## Pre-flight

`GET /needs/eng_for_sin`: **82 missing slots** (36 known, 23 target1, 23 target2, 0 presentation),
**66 TTS jobs**, **2 copies from spares**, **0 ungeneratable**, `readyForGenerate: true`.

Running build verified before firing: phase8 on `:3465`, from the **`-prod` checkout on `main`** at
`8bc7e652`. Confirmed `667a6e09` (the compressor-free chain) **is an ancestor** of it, and confirmed
the source calls `normalizeAudioClean` with `PRE_COMPRESS` removed. Voices taken from
`courses.voice_config`, not guessed: known `azure si-LK-SameeraNeural`, target1 `xai bedd6226`
(Olivia), target2 `xai gfzdpspr5fdp`.

## I did NOT run /regenerate-presentations — it would have made things worse

Its dry run scopes to **12 legos, none of them mine** (`S0380L03`, `S0381L03`, `S0382L04`,
`S0415L03` are all absent), and **every one of the 12 would be minted with an empty quote slot**:

```
ඉංග්‍රීසිෙන්. 'ෆිල්ම් එකක්'. '' ඉතින්. :      ← S0221L02, seed field empty
ඉංග්‍රීසිෙන්. 'ඒ අයට ... ඕනේ'. '' ඉතින්. :   ← S0426L02
```

All 12 report `context_source: none`, `fromSeed: 0`. Running it live would have created **12 corrupt
presentation clips speaking an empty quotation** and fixed **none** of the four this plate nulled.
That is the known corrupt-presentation-text defect, and it is a **pre-existing course defect outside
this plate's scope** — reported, not touched.

The four nulled lego presentations were *not* seen as missing by phase8
(`presentationStatus.totalMissing: 0`), so `/generate` did not refill them either. **The four lego
presentation links remain NULL** — recorded below as the one outstanding audio item.

## Render result

Three `/generate` passes (it skips linked clips, so each pass picked up only what remained):

| pass | total | success | failed |
|---|---|---|---|
| 1 | 66 | **64** | 2 (Azure timeout) |
| 2 | 2 | **1** | 1 (same clip) |
| 3 | 1 | 0 | 1 (same clip) |

Veracity gate on pass 1: **checked 5, passed 5, failed 0**, 0 quarantined, 0 re-rendered.

**67 new clips**, voices exactly as configured — no substitution anywhere:

| role | voice | clips | word_boundaries |
|---|---|---|---|
| known | `azure_si-LK-SameeraNeural` | 29 | **29** |
| target1 | `xai_bedd6226` | 19 | 0 (xAI emits none) |
| target2 | `xai_gfzdpspr5fdp` | 19 | 0 |

### Final link state

| scope | rows | still NULL |
|---|---|---|
| seeds 380/381/382/415/465 | 5 | **0** |
| legos (4) | 4 | **0** |
| phrases (27) | 27 | **1** |

## Production-endpoint byte verification — PASSED

Fetched through the live learner route, not from S3 directly:

| clip | lego | HTTP | bytes | duration |
|---|---|---|---|---|
| `bb4aecaa` | S0380L03 known | 200 | 32,256 | 2.63s |
| `22bf0dfb` | S0380L03 target1 | 200 | 24,192 | 1.97s |
| `3ff66ec8` | S0381L03 known | 200 | 28,800 | 2.34s |
| `0453672e` | S0382L04 known | 200 | 25,344 | 2.05s |
| `f660a00a` | S0415L03 known | 200 | 22,752 | 1.84s |

All `audio/mpeg`, all real durations.

### And the ablative is genuinely spoken

`word_boundaries` is the only field that proves what TTS actually voiced, and it agrees with the new
text word-for-word — **including the ablative marks that are the entire point of the fix**:

| clip | stored text | words |
|---|---|---|
| `0453672e` | ඔයා **ඔහුගෙන්** ඇහුවාද | 3 |
| `bb4aecaa` | මොකක්ද කියලා මම **ඇයගෙන්** ඇහුවා | 5 |
| `f660a00a` | **මගෙන්** ඇහුවොත් | 2 |

## Honest gaps

1. **One slot is still silent: `eng_for_sin:S0380L03B02`, known role**, text
   `මොකක්ද කියලා මම ඇයගෙන් ඇහුවා අද`. Azure timed out at 120s on **four** consecutive attempts — a
   deterministic failure, not a transient. Its sibling `B01` (the same string minus `අද`) rendered
   fine. No spare exists (`course_audio` has 0 rows for that normalized text) and
   `/regenerate-single` needs an existing clip uuid, so `/generate` is the only path. I did **not**
   alter the content text to dodge a TTS bug. **1 of 82 slots.**
2. **The four lego presentation links remain NULL** — phase8 does not consider them missing, and the
   only endpoint that would touch presentations is the one that mints corrupt text. Needs a decision.
3. **`audio_stamp` not bumped** — nothing relinked; every fill was a fresh render into a NULL.
