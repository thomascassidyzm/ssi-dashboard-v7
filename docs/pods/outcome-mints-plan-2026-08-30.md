# The four minted outcome shapes — the plan, committed before a line of dialogue

**2026-08-30.** One page. If the framing here is wrong it is wrong cheaply. The scenes follow in
`outcome-mints-2026-08-30.md`; the proposal in `services/shared/metagraph/proposed/outcome-mints-2026-08-30.json`.
Nothing here executes. POD-1 is append-only: every arm hangs off a fork POD-1 already has.

## Position 1 is O1 — a ruling, and a discrepancy to name

Tom's ruling (U1 = YES: asking presupposes surviving a plain no) puts **O1 at position 1**. The
store's `outcome-shapes.json` still carries `sequence: [O3, O1, …]` with O3 at `sequence_position: 1`
and O1 at 2. The store is **not edited**; the discrepancy is recorded here and in the document.

## Which four, which registers, which fork, which recovery

| | Symmetric arm (equals) | Asymmetric arm (the other party holds the authority / instrument / record and can end the encounter) | Recovery the learner must own |
|---|---|---|---|
| **O1** Non-delivery with a reason | **The taxi** — W14 step 3, the P2 grant at `g139` gets a second arm: the driver's "No. The machine's not working." Learner: `g161` + `g163` — absorb, ask what is possible. | **The bus** — the drill run W15 (`g147`, `g159`, `g160`, `g161`) given its partner and its consequence: the driver holds, the learner asks (`g163`), the doors close. Same ask (pay by card), same position (P2.3). | Absorb the no; re-select, or ask what is possible — **in one turn**, because in the asymmetric arm nobody waits. |
| **O3** The native does not understand you | **The café** — W1 step 2, `g11`. The barista's flat "Sorry?" (F7 arrives). Learner cuts the order to its load-bearing words and tags a check; the barista's read-back confirms and the walk resumes into the option-choice pair and `g18`. | **The ticket window** — `g148`. No F7 arrives: the clerk *acts* on a mishearing ("Two singles"). The learner must **notice** the wrong uptake, reformulate, check, and take the corrected read-back. | Reformulate your own turn — shorter, slower — and check it landed. Asymmetric adds: *detect* it, because the other party will not tell you. |
| **O6** Trouble that is your own fault | **Late for the friend** — W4's arrangement (`g22` "let's talk on Saturday") given its Saturday: the friend holds the consequence ("I nearly went home") and does not absolve. Learner admits, supplies, accepts. Draws on scene 19's `g190`–`g193`. | **The lost ticket** — `g182`/`g183` given the inspector. The consequence is priced and not explained; the learner supplies what they hold (own ticket, time, place) and pays. | Admit it, supply what you can, accept the consequence. |
| **O7** The native discloses worry or difficulty | **The acquaintance** — W5 step 6, `g30` "I've been here for two years" continues into "I miss home, some days." Learner acknowledges without fixing; the discloser closes the aside; the walk resumes on `g35`. | **The chemist's** — W12 after `g115`: the pharmacist steps out of frame ("we're one down and I've been on since seven"). Learner acknowledges, does **not** withdraw and does **not** fix, and continues on `g116`/`g117`. | Acknowledge without fixing, then continue. |

Eight scenes. No register was found not to apply — but two shapes could not keep the store's sited
mask in both registers, and the reason is the finding, not a convenience: **the chemist's and the
ticket are asymmetric by construction** (a counter with a record; a gatekeeper's object), so their
symmetric arms sit on the nearest fork POD-1 has between equals, with the identical position sequence.
That is a register change, not a mask fan.

## What rests on health attestation and what is minted from nothing

- **Health attests the asymmetric shapes with the learner in the POWER seat** (the HW is the L2
  speaker). O1: 1.3 f3, 2.6 f3, 2.3 f3 (the learner absorbs a no from below), 2.7 f2, 2.10 f2 (the
  learner refuses from above and the patient absorbs). O3: 2.5 f2, 2.2 f2, 1.4 f2 (the learner
  reformulates for a native who did not follow). O6: 2.7 f3 (admit → hold → accept, patient below).
  O7: eight flows, patient discloses, professional — usually — fixes.
- **The arms minted here put the learner UNDER the power.** The position sequence is the same with
  the seats swapped; what changes is which turns the learner must produce and who owns the clock.
  So: O1-asym, O3-asym and O6-asym rest on health for the *shape* and are minted for the *seat*;
  O7-asym is minted from nothing in this direction (health's eight are the other direction, and they
  need no mint — they need the health pod). All four symmetric arms are minted from nothing in the
  producing register; the Method Pod holds sibling **mints** (not attestations) of each in its own
  listening register (Scenes 24, 17, 39, 36).

## What gets minted, in the store's register, from 201 up

Nodes `N201`–`N204` (one position sequence per outcome shape), moves `F201`–`F205`, composition
edges `C201`+, survivability edges `S201`+ (each stated as A/B, on the failure branch only), walks
`W201`–`W208`. `O1`/`O3`/`O6`/`O7` keep their ids as the things being filled. Store byte-identical.

## The second axis — the test, declared before the result

Redemption latency sequences the four; the candidate axis weights by the cost of failing to recover.
The test: rank the eight scenes on each axis and see where they disagree. Prediction, written down
now: cost will not reorder the shapes, but it will be the thing that separates the two registers of
one shape. Result in the document, honest either way.
