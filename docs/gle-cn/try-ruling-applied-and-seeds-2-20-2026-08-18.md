# Connemara Irish — your "try" ruling is applied, and seeds 1–20 are banked

> ## ADDENDUM, after your correction: "just ag iarracht"
>
> **Applied.** `iarracht a dhéanamh` and `ag déanamh iarracht` are now **0** across the whole
> course, alongside `ag triail` which is also **0**. Seeds 46 and 47 now read
> `Tá mé ag iarracht foghlaim` and `Tá mé ag iarracht cuimhneamh ar fhocal`.
>
> **Nothing banked had to be un-banked.** Seeds 1–20 contain no "try" at all — that is what
> the delay bought — so the correction touched only the two undecomposed translations at 46
> and 47. `ag iarracht` occurrences in seeds 1–20: **0**.
>
> **Separation: 45 seeds.** "want" debuts at seed 1, "try" at seed 46. You asked me to push
> it further if my range would put `ag iarracht` near `ag iarraidh` — it does not, so I have
> not. The stage-1 measurement that justifies 45: `ag iarraidh` already carries **189 rows**
> across **19 of the 20** banked seeds, in six person-forms.
>
> **Your drilling requirement is written down and binding**, with a per-seed quota for
> 46–70 and six same-phrase pairings that are provably buildable from banked vocabulary
> today — including the minimal pair `Níl mé ag iarraidh stopadh` / `Níl mé ag iarracht
> stopadh`. It is a separate document because the seeds that carry it are not built yet:
> **`docs/gle-cn/try-want-contrast-plan-2026-08-18.md`**, published alongside this.
>
> **One thing is blocked and I did not paper over it.** `ag iarracht` is a progressive.
> **14 of the 22 "try" seeds in the corpus fit it; 8 need a non-progressive "to try" that you
> have not ruled** — and two of those eight, seeds **48** and **49**, sit inside the
> introduction block. `Tá mé ag iarraidh ag iarracht…` ("I want to try") and
> `Tá mé chun ag iarracht…` ("I'm going to try") are both ungrammatical. Their translations
> are now **blank**: they carried the form you rejected, and inventing a replacement is your
> call, not mine. Two ways forward are set out at the end of the plan document. Seed 48 is
> the sharp one — it is the *ideal* contrast sentence, carrying both concepts natively, and
> the one the sanctioned form cannot express.


`gle_cn_for_eng`. **Audio spend: £0.00. Zero TTS was generated, no clip rendered, nothing
deleted.** Your live `gle_for_eng` was not touched — its last content edit is still
2026-07-28, and its 668 seeds / 943 legos / 5,975 phrases are byte-for-byte as they were.
The new course is still `status=draft`, `new_app_status=not_available`.

---

## 1. What your ruling changed

**`ag triail` is gone from the course. Not reduced — gone.**

| | before | after |
|---|---|---|
| `ag triail` in seed translations | 2 (seeds 2, 6) | **0** |
| `ag triail` in legos | 1 (`I'm trying → tá mé ag triail`) | **0** |
| `ag triail` in practice phrases | 15 (seeds 2, 3, 4) | **0** |
| First seed teaching "try" | **seed 2** | **seed 46** |

**How I implemented it: I moved the seeds, which is the option you named as acceptable and
it is also the cheapest.** The four "try"-bearing seeds in the opening — 2 *(I'm trying to
learn)*, 6 *(I'm trying to remember a word)*, 7 *(I want to try as hard as I can today)*,
8 *(I'm going to try to explain what I mean)* — were lifted out and reinserted at 46, 47,
48, 49. Everything between slid up by one to four places; **seed 1 and every seed from 50
onward keep their original number.** 48 seed rows were permuted, 620 were not.

I chose 46–49 rather than an arbitrary "few tens later" because **seed 50 in the English
corpus is already `I'm not trying to finish as quickly as possible`.** The concept now
debuts four seeds before the corpus's own next demand for it, instead of 48 seeds before
it. **The seed moved 44 places (2 → 46), which puts 45 seeds between "want" at seed 1 and
"try" at seed 46.**

**Why all four moved, not just the first.** Your ruling was that `ag triail` is out of the
early course and that the *concept* is delayed. Seeds 7 and 8 did not use `ag triail` at
all, but leaving them at 7 and 8 would have taught "try" in the opening stretch anyway — which is the thing you ruled against. So the concept leaves as
a block and returns as a block.

**The two seeds that still said `ag triail` were retranslated on the spot**, so no landmine
is left sitting in the draft:

| seed | known | now |
|---|---|---|
| 46 | I'm trying to learn | `Tá mé ag iarracht foghlaim` |
| 47 | I'm trying to remember a word | `Tá mé ag iarracht cuimhneamh ar fhocal` |
| 48 | I want to try as hard as I can today | **blank — blocked on your ruling** |
| 49 | I'm going to try to explain what I mean | **blank — blocked on your ruling** |

`ag iarracht` is the form, as you corrected. **Seeds 46–49 are deliberately NOT decomposed** —
the reintroduction is a later run, and the gloss decision belongs with it. 48 and 49 are
blank because the progressive cannot express "I want to try" or "I'm going to try"; see the
addendum at the top.

**On your `ag iarraidh` / `ag iarracht` concern — it is real and it got sharper, not
softer, with your correction.** Both forms are now `ag` + noun, one syllable apart, so
there is no structural cue and **the 45-seed gap is the only thing protecting
predictability**. The gloss lock is `want → ag iarraidh` (seed 1) against
`trying → ag iarracht` (seed 46), each with the `-ing` kept on, so one known prompt maps to
one target in both directions and ZUT holds. Gap plus gloss is the two-part handling you
specified. The drilling that has to follow it is in
`docs/gle-cn/try-want-contrast-plan-2026-08-18.md`.

**The cost of the ruling, stated plainly.** Old seed 2 was the debut of `foghlaim` (to
learn). Removing it took "learn" out of the opening, so the rebuilt seeds 2 and 3 lost every
phrase that leaned on it and had to be rebuilt from seed 1's four legos alone. "Learn"
now debuts at seed 16. That is a thinner opening, not a richer one, and it compounds the
F3 problem the build report already flagged.

---

## 2. Seeds banked

**20 seeds banked. 0 refused. 0 left unfinished in the 1–20 range.**

| | |
|---|---|
| Seeds decomposed and banked | **20** (1–20) |
| Newly built this run | **19** (seeds 2–20; seed 1 was untouched) |
| LEGOs | **65** |
| Practice phrases | **535** |
| Irish strings through the Connemara dialect gate | **624 checked, 0 violations** |
| ZUT conflicts remaining, lego level | **0** |
| ZUT conflicts remaining, component level | **0** |
| Audio rows created | **0** |

Every one passed the course-builder's atomic gate — tiling, ZUT, target vocabulary,
known-side controlled language, phrase counts, anti-template. **The gate rejected 9
submissions before they landed**, which is the gate doing its job, not a tally of failures:
untaught `faoi`/`about`, untaught `anseo`/`here`, untaught `liom`/`me` and `dom` two seeds
before they were introduced, `foghlaim` used bare when only `a fhoghlaim` had been taught,
a duplicate phrase, a word-containment failure on `bualadh leat` against the lego `bualadh
le`, an anti-template strike, and one real ZUT conflict (below).

Per-seed decompositions are committed as markdown at `docs/gle-cn/decompositions/`.

### The two things I found and fixed rather than shipped

**1. `to remember` was glued to its preposition, and seed 20 exposed it.** Seed 6 taught
`to remember → cuimhneamh ar`. Seed 20 *(I'm not going to be able to remember easily)* has
no object, so its Irish is bare `cuimhneamh` — and the gate correctly refused a second
target for the same English prompt. A learner taught `cuimhneamh ar` would say
`cuimhneamh ar go héasca`, which is wrong. I snapshotted and rebuilt seed 6 so the
preposition is its own glossed piece (`to remember → cuimhneamh`, `about the whole sentence
→ ar an abairt ar fad`). **This is the pilot's own F1 rule — never chunk a function word
that will recombine — applied where the pilot had not applied it.**

**2. Two English words each had two Irish targets at component level**, which the gate
does not check: `to` → `a` (in `a rá`) and `to` → `le` (in `cén chaoi le`); `you` → `duit`
(in `tar éis duit`) and `you` → `tá tú`. Both are now single unsplit legos, so the
conflicting glosses no longer exist.

### Rules carried forward, as instructed

- **Function words are never chunked into a lexical lego if they recombine** — applied at
  seeds 6, 13 and 18, and it is what forced the seed 6 rebuild.
- **Verbal-noun legos are taught object-fronted** — `Gaeilge a labhairt`, `labhairt a
  chleachtadh`, `a ainm a fhoghlaim`, `a hainm a fhoghlaim`.

---

## 3. The translation memory — and a correction you need

I consulted the native base for every seed. Her choice was the default and carried
seeds 9 *(labhraíonn tú Gaeilge go han-mhaith)*, 15 *(ach níl mé ag iarraidh stopadh ag
caint)*, and the legos `ar ball`, `an freagra`, `ar a sé a chlog`, `go tapa`, `bualadh le
chéile`, `daoine a labhraíonn Gaeilge`, `beagán Gaeilge`, `tomhas`, `duine éigin eile`,
`in ann`, `agus`, `liom`. Seeds 17–20 had no translation at all and I translated them
fresh, three of the four straight off her attested patterns.

**The correction. "The native base course" in the build report is not your live Irish
course. It is a legacy JSON export at `scripts/en-ga-compare/en-ga.json`, and the two
disagree on nearly every marker the report rests on.** I measured both:

| form | legacy export (15,904 items) | live `gle_for_eng` (7,586 items) |
|---|---|---|
| `céard` | **644** | **0** |
| `cad` | **0** | **522** |
| `in ann` | **646** | **12** |
| `ábalta` | **0** | **192** |
| `tá muid` | **0** | **90** |
| `táimid` | **22** | **0** |
| `labharófá` | **46** | **0** |
| `ar ball` | present (lego, seed 276) | **0** |
| `conas` | 279 | 271 |

Both are real Irish assets. The report's evidence is sound *about the legacy export* — I
re-ran its own census script and reproduced its numbers exactly. But three claims in it do
not describe your live course: the practice layer is **5,975 phrases, not 13,455**; the
`labharófá` defect is **0 occurrences live**, not 46; and `níl fhios agam` is **0 live** —
the live course already writes the correct `níl a fhios agam` 33 times. **Report item 8
("mine her practice layer, 13,320 clean native phrases") is about a JSON file, not about
anything a learner can reach.** I could not establish the provenance of that export and
have not tried to rule which asset is the real ancestor — that is a question for you.

I stayed with the legacy export as the translation memory because that is what the pilot
used and what your ruling's context assumes. Where the two disagreed on a live decision I
took the legacy one and said so, except once (§4).

None of the six named defects were inherited: **`conas` 0, `táimid` 0, `teastaíonn` 0,
`níl fhios agam` 0, `labharófá` 0, `nó` for "No" 0** across all 624 Irish strings.

---

## 4. Awaiting your word

**Your six open taste items — I did not decide any of them.**

| your item | where it now sits | what I banked |
|---|---|---|
| 1. `cén chaoi le` + verbal noun | seeds 2, 3 | banked as the pilot had it, undecided |
| 3. `chomh dian` | now seed **48** | not banked — outside this run |
| 4. `ar feadh an lae` | now seed **10** | **I departed — see below** |
| 5. `a ainm a fhoghlaim` | seed 16 | banked as the pilot had it, undecided |
| 6. clause order | now seed **49** | not banked — outside this run |
| 7. `éigin` vs `eicínt` | seeds 3, 4, 18 | banked as standard `éigin`, undecided |

**Item 4 is the one I moved, and you should overrule me if you disagree.** You were shown
`ar feadh an lae` versus `i rith an lae`. There is a third option neither of us had:
**`an lá ar fad`**, which is what the live Irish course uses for this exact seed (46
occurrences), which is not the bounded-stretch framing you objected to, and which reuses
`ar fad` already taught at seed 6. Both of the options you were shown have **zero** support
in either corpus. I banked `An labhraíonn tú Gaeilge an lá ar fad?`. Reverting it is one
seed's rebuild.

**Five decisions of mine that are not on your list and probably should be:**

1. **`to practise` → `a chleachtadh`, not her `cleachtadh a dhéanamh`** (seed 4). Her form
   cannot be split into honest legos here: `cleachtadh a dhéanamh ar an gcaint` needs a
   lego glossed "speaking → ar an gcaint", which is not true. `Tá mé chun labhairt a
   chleachtadh le duine éigin eile` tiles cleanly, is object-fronted, and reuses `labhairt`.
2. **`céard atá` and `céard é` are both "what is"** — seed 8 teaches `what's → céard atá`,
   seed 13 teaches `what the answer is → céard é an freagra`. The rule is real (`céard atá`
   before a verb, `céard é` before a noun) but a learner meets the two three seeds apart
   and the English glosses look identical. This is the same shape of hazard `try` was.
3. **`to meet` → `bualadh le chéile` (seed 14) versus `to meet with` → `bualadh le`
   (seed 18)**, distinguished by gloss so ZUT holds. Same caveat as 2.
4. **`in ann` kept over `ábalta`** (seeds 7, 20). The legacy export says `in ann` 646 /
   `ábalta` 0; the live course says the opposite. I went with the legacy export and with
   Connacht speech.
5. **Seeds 46 and 47's new translations are mine and unreviewed by anyone.**

---

## 5. Gaps — what I could not settle

- **No native Connemara speaker has seen any of this.** Every naturalness call, including
  all four fresh translations for seeds 17–20, rests on my reading plus the corpora. This
  is unchanged from the pilot and it is the largest gap.
- **The two corpora disagree and I did not resolve which is the real native base.** I
  reported both rather than picking one silently. The provenance of
  `scripts/en-ga-compare/en-ga.json` is outside anything I could verify from the code.
- **Seeds 1–3 are still thin, and the ruling made them thinner.** They carry 11, 18 and 17
  phrases against a mature-course floor of ~7 per lego; seed 1's first lego has none at all.
  This is F3 from the build report, unresolved, and losing `foghlaim` from the opening
  worsened it. It cannot be fixed by backfilling with later vocabulary — that would break
  the very rule the gate enforces — so it needs deliberate extra seed-1-vocab phrases.
- **Seeds 46–49 are translated but not decomposed**, and 648 seeds are still untranslated.
- **No external corpus, dictionary or web access.** "this evening" (`tráthnóna inniu`,
  seed 14) has zero support in either corpus and is my call.
- **TTS behaviour on `labhrófá`, `chuile dhuine`, `cén chaoi`, `go héasca` is still
  unverified** — no audio exists, so the orthography-protects-pronunciation claim remains a
  reason, not a measurement.

---

## 6. Spend

**£0.00.** No TTS. No renders. No deletions of any generated asset. The ~56,000-clip
projection in the build report is untouched and uncosted; nothing will be generated until
you have seen a real per-clip figure.

---

Everything destroyed in this run was snapshotted first and is restorable: a file
before-image at `docs/gle-cn/snapshots/pre-try-ruling-2026-08-18.json`, the permutation map
at `docs/gle-cn/snapshots/renumber-2026-08-18.json`, and six rows in `seed_redo_snapshots`
(batch `58f6e0c7-df51-4014-ac8e-65d693b05790` for seeds 1–4, plus one each for the seed 6
and seed 20 rebuilds).

---

**LANDING LINE:** the commits are on branch **`docs/gle-cn-try-ruling-2026-08-18`**; they
are **not merged** — nothing has gone to `main`; and they are **not deployed anywhere** —
the course content itself is live in Supabase as `gle_cn_for_eng` at `status=draft`,
`new_app_status=not_available`, which I verified by direct query, so **no learner can reach
any of it**.
