# Register additions — worker D4b2, seeds 123–127

Course `gle_cn_for_eng`. Decomposition band 123–127, 21 August 2026.
**Per §G of the addendum this is my own file. The shared register was not edited.**

These are the recurring English→Irish pairs I banked as teaching units that the shared register
does not already carry a row for. Everything else in the band routed through an existing register
row or an already-live chunk.

| English (known) | Irish (target) | first at | authority | confidence |
|---|---|---|---|---|
| very good | `an-mhaith` | S125 | register #153 (`very` → `an-` + lenition) applied to `maith` (#258); matches the live `an-suimiúil` at S112 | confident |
| that's a good idea / that was a good idea | `gur smaoineamh maith é sin` | S123 / S124 | the register's own worked example under §3 #17; `smaoineamh` = idea, #20/#173 | confident |
| a good idea | `smaoineamh maith` | S123 | as above | confident |
| that your idea was | `go raibh do smaoineamh` | S125 | `go` complementiser #87 + past of `bí`; `do` = your, #75 | confident |
| your idea | `do smaoineamh` | S125 (component) | #75 + #173 | confident |
| I thought | `bhí mé ag cheapadh` | S124 | register §3 #17 exactly | confident |
| changing | `ag athrú` | S126 | #223 `athrú`, progressive on the `tá … ag …` backbone the course has used since S1 | confident |
| this work is | `tá an obair seo` | S126 | #188 `obair`; `seo` demonstrative as at S92 `an ruda seo` | confident |
| **the shape of my brain** | **`cruth m'inchinne`** | S126 | FGB `cruth` = shape/form; `inchinn` #176, genitive `inchinne`; `m'` before a vowel, #74 | **best attempt** |
| that isn't why | `ní hé sin an fáth` | S127 | the negative copula of #98 / S105 `sin é an fáth`; `ní hé` before a vowel-initial pronoun | confident |
| that I wanted | `go raibh mé ag iarraidh` | S127 | `go` #87 over the S30 past `bhí mé ag iarraidh` | confident |

## Notes for the coordinator

- **`cruth m'inchinne` is the one lexical call in the band that is mine.** `cruth` is not in the
  shared register and `shape` appears in no other seed I can see, so I have not proposed it as a
  recurring row — it is here so a ZUT sweep can find it if `shape` recurs later in 128–300.
- **`gur smaoineamh maith é sin` is deliberately the target for two different English prompts** —
  *that's a good idea* (S123) and *that was a good idea* (S124). ZUT constrains known→target, and
  each known prompt still has exactly one target, so this is intact. It is also the teaching point
  of S124: English marks the tense on the reported clause, Irish marks it on the matrix verb only.
- **`an-mhaith` does not touch S13.** R7/A7 rule *very good Irish* as one unit and S13 deliberately
  banked `Gaeilge mhaith` without `an-`. I did not write `Gaeilge an-mhaith` anywhere.
