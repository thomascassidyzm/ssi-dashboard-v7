# Morning brief — 2026-06-16 (overnight autonomous run)

*What I did, what I'm >90% confident of, and the prioritised next actions. Everything is text/analysis/contract/code — NO TTS, NO learner-facing content was mass-mutated. All on `main` via `methodology-encoding`.*

## TL;DR
- **The gate-and-fix template travels to a second, unrelated language (French) — confirmed.** The English known-side scaffold + the 7 tense constructions are **free reuse** across every `_for_eng` course (known=English). I copied them verbatim into a French contract.
- **The big finding: the gates encode Chinese-*analytic* typology assumptions.** Chinese doesn't inflect, so ZUT/frame were clean. French conjugates + agrees + has clitics, so:
  - **ZUT over-flags inflected targets**: of 1,513 French ZUT flags, **~65% (990) are morphology** (clitics 665, conjugation 282, gender 43), only **523 true collisions**.
  - **known-side over-flags without the target's construction layer**: of 4,245 flags, **89% (3,773) are construction-licensing gaps** (going-to / negation) because the French contract's `constructions` is empty — they collapse once defined (the "flags author the contract" loop).
- So porting to a new pair = **template + per-pair contract (flags author it) + a target-morphology layer in the ZUT gate for inflected languages.** That's the real shape of the ~69-course program.

## What's done + on main
1. **Frame-coverage made script-aware** (`validation.cjs`) — was CJK-only (read 0 on French); now collapses Latin-script pronouns too. Verified on French + zho. *Limitation:* French verb agreement masks some subject-swaps — full frame-diversity for inflected targets needs the 39-slot audit-tool, not this lens.
2. **First-pass French contract** (`docs/pair-contracts/fra_for_eng.contract.cjs`) — English scaffold reused verbatim; French target layer empty (the flags below fill it).
3. **Generic toolchain**: `scripts/lint-any.cjs <course>` (any course), `fra-analysis.cjs`, `reclassify-known-side.cjs`. The whole lint→classify loop is now course-agnostic.
4. **zho S351–668 fix-queue** (`upper-half-fix-queue.md`) + the tense correction in the contract (`knownTenseConstructions`) — from earlier in the session.

## French actionable fix-queue (after stripping morphology + construction-gaps)
- **Define French constructions** (resolves 3,773 known-side + reduces ZUT): `going-to` carrier (`aller`+inf), negation carrier (`ne…pas`), question-forms (es-tu / est-ce que / inversion), relative `ce qui`/`ce que`, clitics (me/moi…). These are contract entries, not per-item fixes.
- **~523 true ZUT collisions** (real, like zho's 帮/帮助) → consolidate.
- **~174 untaught-content re-glosses** + **227 used-before-introduced** (reorder/defer).
- **A target-morphology layer for the ZUT gate** so conjugation/agreement (990 flags) stop reading as collisions — see Next Actions.

## zho tense demos — DESIGN (for your approval; not authored yet)
The 617 zho tense flags collapse to **~7 demonstrations**, each introduced once at its first occurrence, showing "English changes the verb, Chinese keeps it + moves tense to an adverb/aspect marker":
| Construction | English carrier (first use) | Chinese rendering to demonstrate |
|---|---|---|
| past | "I tried / I went" | verb unchanged + 了 / time-adverb (昨天) |
| progressive | "I'm trying" | 在/正在 + verb |
| perfect | "I have tried" | 了 (done) / 过 (experiential) |
| future | "I will / going to" | 会/要 + verb / 明天 |
| 3sg | "she wants" | verb unchanged (no agreement) |
| comparative | "harder / better" | 更/最 + adjective |
| plural | "words" | noun unchanged / 些 / measure word |
Each is a tiny convergence basket (1 demo phrase + a note), authored at the seed where that English form first appears. **Build step:** wire these into the gate (license at carrier seed, retire the blanket `stemStrip`).

## PRIORITISED NEXT ACTIONS
1. **[gate] Target-morphology-aware ZUT** — the highest-leverage fix for scaling. Compare target *lemmas* (or accept a per-pair "agreement expected" flag) so conjugation/gender/clitics stop reading as ZUT violations. Without it, every inflected course (French/Spanish/Italian/German/Portuguese — most of the BIG-10) drowns in false ZUT. *This is the gate change that makes the program actually travel.*
2. **[zho] Author the ~7 tense demos** (design above) + wire `knownTenseConstructions` into the gate. Highest learner value on zho; banks the known-side model.
3. **[zho] Work the S351–668 fix-queue** — 90 ZUT consolidations first (learner-facing), then the 68 untaught re-glosses + 19 metadata.
4. **[French] Ratify the French contract** from the flags — define the constructions (going-to/negation/question-forms/relative/clitics), then re-lint to confirm the known-side flags collapse. Proves the full loop closes on French.
5. **[frame] (low)** port the 39-slot audit tool for inflected-target frame-diversity (the lens is a coarse warn-gate).

## Open decisions for you
- **#1 (morphology-aware ZUT)**: lemma-comparison (needs a target lemmatiser per language) vs a simpler per-pair "agreement classes are exempt" contract flag? I lean **per-pair exempt-flag** (BSC: simpler, no lemmatiser, the contract already exists) — but it's your call.
- **Tense demos**: approve the 7 above / adjust the set?
- **Sequencing**: do #1 (gate) before touching more courses, or push zho (#2–3) to learner value first? I lean **#1 then #2–3** (the gate fix unblocks every inflected course; zho is already mostly mapped).

## Confidence
**>90% on the program**: gate-and-fix existing courses, per-pair contracts the flags author, English known-side reused across `_for_eng`. The one genuine gap the French test exposed — target-morphology-aware ZUT — is well-understood and scoped (#1). That's the thing to build before pointing this at the other inflected courses.
