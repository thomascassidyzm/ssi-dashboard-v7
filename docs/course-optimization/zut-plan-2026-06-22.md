# ZUT Resolution Plan — 9 main `_for_eng` courses (2026-06-22)

Detected on **paren+slash-stripped** known_text (the strip reveals hidden conflicts, per Kai). NO DB writes yet — this is the plan. Source: `*.zut.json`.

## Totals by resolution type

| type | count | resolution |
|---|---|---|
| `punct_artifact` | 1 | NOT a real ZUT — resolves when the missing-?/punctuation fix runs. Verify after. |
| `true_synonym` | 113 | Pick different known gloss for one, OR set is_new=false if covered elsewhere, OR merge. Per-item. |
| `tense_aspect` | 19 | Tense/aspect demo: introduce-once-then-free, OR expand with a time/aspect cue so the English prompt is deterministic. |
| `case_number_register` | 99 | Expand the LEGO to encode the grammatical role via context (M-type w/ governing prep/verb), OR distinguish on the known side. Needs methodology judgment (known-side control). |
| `mixed_grammar` | 14 | Case + tense both — full expansion/redecomposition. Heaviest; per-item. |
| `slash_only` | 87 | Driven by slash resolution — DECIDE THE SLASH FIRST (he/she→seed; synonym→first; word+trailing→keep). May be a false collision from first-option heuristic. |

**Grand total real ZUT: 333** (gender pairs excluded).

## Per-course breakdown

| course | total | punct_artifact | true_synonym | tense_aspect | case/num/reg | mixed | slash_only |
|---|---|---|---|---|---|---|---|
| spa | 15 | 0 | 6 | 6 | 2 | 0 | 1 |
| fra | 31 | 0 | 21 | 1 | 7 | 0 | 2 |
| ita | 14 | 0 | 6 | 0 | 5 | 0 | 3 |
| deu | 77 | 0 | 11 | 1 | 41 | 10 | 14 |
| zho | 57 | 0 | 13 | 1 | 3 | 0 | 40 |
| jpn | 10 | 0 | 10 | 0 | 0 | 0 | 0 |
| por | 19 | 0 | 7 | 1 | 3 | 0 | 8 |
| kor | 77 | 1 | 25 | 5 | 33 | 0 | 13 |
| ara | 33 | 0 | 14 | 4 | 5 | 4 | 6 |

## spa_for_eng

### true_synonym (6) — Pick different known gloss for one, OR set is_new=false if covered elsewhere, OR merge. Per-item.

- **"it was"** → `era` vs `fue`
    - S0373L02 raw="it was (ser)" → `era` (seed: "it was beautiful")
    - S0125L02 raw="it was" → `fue` (seed: "I believe that your idea was very good")
- **"to leave"** → `irse` vs `irme`
    - S0345L01 raw="to leave (oneself)/go away" → `irse` (seed: "who said that he's not ready to leave yet")
    - S0588L02 raw="to leave (go away)" → `irme` (seed: "so I wanted to grow up and leave home")
- **"to wake up"** → `despertarnos` vs `despertarse`
    - S0108L01 raw="to wake up" → `despertarnos` (seed: "we didn't hope to wake in the middle of the night")
    - S0584L01 raw="to wake up (oneself)" → `despertarse` (seed: "what's it like to wake up every morning and see the mountains?")
- **"to feel"** → `sentirme` vs `sentirse`
    - S0041L04 raw="to feel" → `sentirme` (seed: "I feel okay, but I'm starting to feel tired")
    - S0122L02 raw="to feel" → `sentirse` (seed: "It's starting to feel easier and I'm excited about how it's going")
- **"instead of"** → `en lugar de` vs `en vez de`
    - S0523L01 raw="instead of" → `en lugar de` (seed: "instead of giving an excuse")
    - S0502L03 raw="instead of" → `en vez de` (seed: "She almost got lost because she turned left instead of right")
- **"we are"** → `estamos` vs `somos`
    - S0102L01 raw="we are" → `estamos` (seed: "we're trying to say that it's not like that")
    - S0110L01 raw="we are" → `somos` (seed: "we're friends, and after we finish I'd like to relax")

### tense_aspect (6) — Tense/aspect demo: introduce-once-then-free, OR expand with a time/aspect cue so the English prompt is deterministic.

- **"she could"** → `podría` vs `pueda`
    - S0310L01 raw="she could" → `podría` (seed: "She could write a story about that man")
    - S0318L01 raw="she/he could (subjunctive)" → `pueda` (seed: "no I don't think she could this time")
- **"they had"** → `tenían` vs `lo habían`
    - S0356L01 raw="they had" → `tenían` (seed: "yes they had something important to discuss")
    - S0622L01 raw="they had (past perfect)" → `lo habían` (seed: "I knew that the children had broken it")
- **"you were"** → `estuviste` vs `estabas`
    - S0385L01 raw="you were/agreed (preterite)" → `estuviste` (seed: "did you agree with her?")
    - S0107L03 raw="you were" → `estabas` (seed: "we hoped to see what you were doing")
- **"they could"** → `podrían` vs `pudieron`
    - S0432L02 raw="they could" → `podrían` (seed: "they could mean that they want you to ask")
    - S0433L03 raw="they could (preterite)" → `pudieron` (seed: "they couldn't find out when the film started")
- **"we could"** → `podríamos` vs `podíamos`
    - S0413L01 raw="we could" → `podríamos` (seed: "we could fall if we go too close to the edge")
    - S0412L04 raw="we could (imperfect)" → `podíamos` (seed: "we couldn't allow them to win everything")
- **"likes"** → `le gusta` vs `aprecie`
    - S0239L02 raw="likes" → `le gusta` (seed: "my mother likes to read")
    - S0419L02 raw="likes/appreciates (subjunctive)" → `aprecie` (seed: "if they want people to like them")

### case_number_register (2) — Expand the LEGO to encode the grammatical role via context (M-type w/ governing prep/verb), OR distinguish on the known side. Needs methodology judgment (known-side control).

- **"enough"** → `suficientes` vs `suficiente`
    - S0058L03 raw="enough" → `suficientes` (seed: "It's interesting when you understand enough words")
    - S0378L01 raw="enough (singular)" → `suficiente` (seed: "no I didn't have enough money for holidays")
- **"you're doing it"** → `lo estás haciendo` vs `lo está haciendo`
    - S0072L02 raw="you're doing it" → `lo estás haciendo` (seed: "I think that you're doing very well")
    - S0655L01 raw="you're doing it (formal)" → `lo está haciendo` (seed: "I think that you're doing very well, madam")

### slash_only (1) — Driven by slash resolution — DECIDE THE SLASH FIRST (he/she→seed; synonym→first; word+trailing→keep). May be a false collision from first-option heuristic.

- **"over there"** → `de allí` vs `allí`
    - S0389L01 raw="over there/from there" → `de allí` (seed: "that person over there")
    - S0498L03 raw="over there" → `allí` (seed: "he's standing alone over there inside the entrance")

## fra_for_eng

### true_synonym (21) — Pick different known gloss for one, OR set is_new=false if covered elsewhere, OR merge. Per-item.

- **"to stop"** → `s'arrêter` vs `t'arrêter`
    - S0402L02 raw="to stop (oneself)" → `s'arrêter` (seed: "yes it would be nice to stop somewhere for food")
    - S0067L02 raw="to stop" → `t'arrêter` (seed: "Why do you want to stop?")
- **"possible"** → `possibles` vs `possible`
    - S0325L04 raw="possible" → `possibles` (seed: "I think that he needs to consider ten possible problems")
    - S0430L02 raw="possible (sing.)" → `possible` (seed: "no that wouldn't be possible")
- **"next"** → `prochain` vs `ensuite` vs `prochaine`
    - S0395L04 raw="next (masc)" → `prochain` (seed: "we need to turn left at the next corner")
    - S0493L02 raw="next (after)" → `ensuite` (seed: "what's going to come next?")
    - S0059L03 raw="next" → `prochaine` (seed: "I know how to do what I need to do next week")
- **"we don't know"** → `on ne connaît pas` vs `nous ne savons pas`
    - S0472L01 raw="we don't know (facts)" → `on ne connaît pas` (seed: "part of the problem is that we don't know the facts")
    - S0213L01 raw="we don't know" → `nous ne savons pas` (seed: "we don't know what they're trying to achieve")
- **"interesting"** → `intéressant` vs `intéressantes`
    - S0492L01 raw="interesting (masc.)" → `intéressant` (seed: "which of those places do you think is the most interesting?")
    - S0051L02 raw="interesting" → `intéressantes` (seed: "I enjoy doing interesting things with my friends")
- **"only"** → `seul` vs `seulement`
    - S0481L01 raw="only (adj.)" → `seul` (seed: "It's the only real hope we have left")
    - S0280L01 raw="only" → `seulement` (seed: "no I only had to do the most important job")
- **"them"** → `eux` vs `les`
    - S0429L03 raw="them (masc.)" → `eux` (seed: "yes that would be perfect for them")
    - S0183L01 raw="them" → `les` (seed: "no I'm afraid I haven't seen them")
- **"guess"** → `devine` vs `deviner`
    - S0527L04 raw="guess (imperative)" → `devine` (seed: "guess who told me that funny story")
    - S0012L02 raw="guess" → `deviner` (seed: "I wouldn't like to guess what's going to happen tomorrow")
- **"last night"** → `la nuit dernière` vs `hier soir`
    - S0042L02 raw="last night" → `la nuit dernière` (seed: "I was starting to feel better than last night")
    - S0453L03 raw="last night (evening)" → `hier soir` (seed: "did they say who they saw last night?")
- **"kind"** → `gentille` vs `gentil`
    - S0147L02 raw="kind" → `gentille` (seed: "she was very kind when she saw me feeling nervous")
    - S0478L03 raw="kind (masc.)" → `gentil` (seed: "she has such a kind heart")
- **"to move"** → `bouger` vs `déménager`
    - S0513L01 raw="to move (body)" → `bouger` (seed: "it hurts most when I move my head up and down")
    - S0319L02 raw="to move" → `déménager` (seed: "she needs to move to a different country")
- **"I heard that"** → `j'ai entendu dire` vs `j'ai entendu dire que`
    - S0364L04 raw="I heard that" → `j'ai entendu dire` (seed: "I heard that he didn't like that place")
    - S0509L02 raw="I heard that (you...)" → `j'ai entendu dire que` (seed: "I heard that you're going to pay for a new bed")
- **"anywhere"** → `nulle part` vs `quelque part`
    - S0603L03 raw="anywhere" → `nulle part` (seed: "we didn't have anywhere to live at the time")
    - S0182L03 raw="anywhere" → `quelque part` (seed: "have you seen my keys anywhere?")
- **"holidays"** → `vacances` vs `les vacances`
    - S0378L01 raw="holidays" → `vacances` (seed: "no I didn't have enough money for holidays")
    - S0573L01 raw="holidays" → `les vacances` (seed: "It's the kind of thing that makes the holidays very special")
- **"that"** → `ce` vs `cela` vs `que`
    - S0393L01 raw="that (masc before consonant)" → `ce` (seed: "that boy with the green shirt")
    - S0644L01 raw="that" → `cela` (seed: "could you say that sir?")
    - S0121L02 raw="that" → `que` (seed: "It's unusual that you don't like to use your car")
- **"how"** → `à quel point` vs `comment`
    - S0600L03 raw="how" → `à quel point` (seed: "I'd have driven if you'd told me how tired you were")
    - S0003L01 raw="how" → `comment` (seed: "how to speak as often as possible")
- **"thought"** → `pensais` vs `trouvé`
    - S0616L01 raw="thought" → `pensais` (seed: "you were very brave to say you thought that")
    - S0615L03 raw="thought" → `trouvé` (seed: "I thought you were very brave to say that")
    - S0124L01 raw="thought" → `pensais` (seed: "I thought that was a good idea")
- **"to leave"** → `partir` vs `quitter`
    - S0345L02 raw="to leave" → `partir` (seed: "who said that he's not ready to leave yet")
    - S0139L02 raw="to leave" → `partir` (seed: "I'm sorry that I need to leave so early")
    - S0588L01 raw="to leave" → `quitter` (seed: "so I wanted to grow up and leave home")
- **"wanted"** → `voulu` vs `voulait`
    - S0580L03 raw="wanted" → `voulu` (seed: "we've often wanted to take the children somewhere a little warmer")
    - S0052L01 raw="wanted" → `voulait` (seed: "he wanted to write a letter to his friend last week")
- **"work"** → `travail` vs `fonctionne`
    - S0610L01 raw="work" → `travail` (seed: "he needs to look for work")
    - S0094L03 raw="work" → `fonctionne` (seed: "this is the only way it will work")
- **"her"** → `son` vs `sa`
    - S0637L01 raw="her" → `son` (seed: "where is her bag?")
    - S0053L02 raw="her" → `sa` (seed: "she wanted to put his letter in her bag")

### tense_aspect (1) — Tense/aspect demo: introduce-once-then-free, OR expand with a time/aspect cue so the English prompt is deterministic.

- **"it is"** → `c'est` vs `ce soit`
    - S0049L01 raw="it is" → `c'est` (seed: "It's like this, if you know what I mean")
    - S0330L01 raw="it is (subjunctive)" → `ce soit` (seed: "no I don't think it's very important")

### case_number_register (7) — Expand the LEGO to encode the grammatical role via context (M-type w/ governing prep/verb), OR distinguish on the known side. Needs methodology judgment (known-side control).

- **"ready"** → `prêt` vs `prêts`
    - S0345L01 raw="ready" → `prêt` (seed: "who said that he's not ready to leave yet")
    - S0431L03 raw="ready (pl.)" → `prêts` (seed: "they're not ready yet but they'll be ready soon")
- **"beautiful"** → `beau` vs `beaux`
    - S0373L01 raw="beautiful" → `beau` (seed: "it was beautiful")
    - S0486L01 raw="beautiful (pl.)" → `beaux` (seed: "I think your eyes are beautiful")
- **"happy"** → `content` vs `heureuse` vs `contents`
    - S0599L02 raw="happy" → `content` (seed: "I'd have been happy to drive if you'd told me")
    - S0344L01 raw="happy" → `content` (seed: "who said that he's happy to help you")
    - S0408L04 raw="happy (fem)" → `heureuse` (seed: "yes that's the best way to make a happy family")
    - S0448L02 raw="happy (pl.)" → `contents` (seed: "yes they'll be happy to help")
- **"please"** → `s'il te plaît` vs `s'il vous plaît`
    - S0624L02 raw="please" → `s'il te plaît` (seed: "yes, that would be great, please")
    - S0414L06 raw="please (formal)" → `s'il vous plaît` (seed: "could we have a bottle of red wine please?")
- **"what you said"** → `ce que tu as dit` vs `ce que vous avez dit`
    - S0078L02 raw="what you said" → `ce que tu as dit` (seed: "I don't understand what you said")
    - S0648L01 raw="what you said (formal)" → `ce que vous avez dit` (seed: "what you said madam")
- **"young"** → `jeune` vs `jeunes`
    - S0069L02 raw="young" → `jeune` (seed: "he didn't want to look after the young dog all afternoon")
    - S0440L03 raw="young (pl.)" → `jeunes` (seed: "they want to travel while they're still young")
- **"different"** → `différents` vs `différentes`
    - S0060L02 raw="different" → `différents` (seed: "I don't know how to say enough different words yet")
    - S0457L04 raw="different (fem. pl.)" → `différentes` (seed: "part of the problem is the number of different areas")

### slash_only (2) — Driven by slash resolution — DECIDE THE SLASH FIRST (he/she→seed; synonym→first; word+trailing→keep). May be a false collision from first-option heuristic.

- **"to ask"** → `demander` vs `poser`
    - S0405L02 raw="to ask" → `demander` (seed: "should we ask if we have to book?")
    - S0423L02 raw="to ask/pose" → `poser` (seed: "do they need to ask such an obvious question?")
- **"there"** → `là-bas` vs `là`
    - S0562L02 raw="there / over there" → `là-bas` (seed: "I just want to get there safely")
    - S0157L02 raw="there" → `là` (seed: "I won't be able to be there next month")

## ita_for_eng

### true_synonym (6) — Pick different known gloss for one, OR set is_new=false if covered elsewhere, OR merge. Per-item.

- **"to play"** → `suonare` vs `giocare`
    - S0098L03 raw="to play" → `suonare` (seed: "I should consider playing something else")
    - S0337L01 raw="to play (a game)" → `giocare` (seed: "do you think that he can continue to play?")
    - S0567L03 raw="to play (games)" → `giocare` (seed: "it's been lovely spending time outdoors watching the children play")
- **"to eat"** → `da mangiare` vs `mangiare`
    - S0243L02 raw="to eat" → `da mangiare` (seed: "I'm going to ask for the same thing to eat")
    - S0400L01 raw="to eat" → `mangiare` (seed: "do we want to eat something later on?")
- **"to wake up"** → `svegliarmi` vs `svegliarsi`
    - S0055L01 raw="to wake up" → `svegliarmi` (seed: "I don't enjoy waking up when I didn't sleep very well")
    - S0584L02 raw="to wake up" → `svegliarsi` (seed: "what's it like to wake up every morning and see the mountains?")
- **"a friend"** → `un'amica` vs `un amico`
    - S0308L01 raw="a friend" → `un'amica` (seed: "yes she's a friend of my mother")
    - S0265L01 raw="a friend" → `un amico` (seed: "A friend")
- **"I'm afraid"** → `temo` vs `temo di`
    - S0521L01 raw="I'm afraid" → `temo` (seed: "I'm afraid that you'll forget why we needed to stay here")
    - S0183L01 raw="I'm afraid" → `temo di` (seed: "no I'm afraid I haven't seen them")
- **"to answer"** → `rispondere` vs `per rispondere`
    - S0043L02 raw="to answer" → `rispondere` (seed: "I wasn't thinking about how to answer")
    - S0027L03 raw="to answer" → `per rispondere` (seed: "I don't like taking too much time to answer")

### case_number_register (5) — Expand the LEGO to encode the grammatical role via context (M-type w/ governing prep/verb), OR distinguish on the known side. Needs methodology judgment (known-side control).

- **"you're doing"** → `tu stia andando` vs `sta facendo`
    - S0072L02 raw="you're doing" → `tu stia andando` (seed: "I think that you're doing very well")
    - S0646L01 raw="you're doing (formal)" → `sta facendo` (seed: "you're doing something sir")
- **"could you"** → `potresti` vs `potrebbe`
    - S0061L01 raw="could you" → `potresti` (seed: "could you say that again a little more slowly?")
    - S0644L01 raw="could you (formal)" → `potrebbe` (seed: "could you say that sir?")
- **"you speak"** → `parli` vs `lei lo parla`
    - S0013L01 raw="you speak" → `parli` (seed: "you speak Italian very well")
    - S0647L01 raw="you speak (formal)" → `lei lo parla` (seed: "you speak it madam")
- **"help you"** → `aiutarti` vs `aiutarla`
    - S0062L01 raw="help you" → `aiutarti` (seed: "I'm not sure if I can help you at the same time")
    - S0645L01 raw="help you (formal)" → `aiutarla` (seed: "I can help you madam")
- **"what you said"** → `quello che hai detto` vs `quello che ha detto`
    - S0078L02 raw="what you said" → `quello che hai detto` (seed: "I don't understand what you said")
    - S0648L01 raw="what you said (formal)" → `quello che ha detto` (seed: "what you said madam")

### slash_only (3) — Driven by slash resolution — DECIDE THE SLASH FIRST (he/she→seed; synonym→first; word+trailing→keep). May be a false collision from first-option heuristic.

- **"anywhere"** → `da qualche parte` vs `da nessuna parte`
    - S0182L02 raw="anywhere" → `da qualche parte` (seed: "have you seen my keys anywhere?")
    - S0376L01 raw="anywhere/nowhere" → `da nessuna parte` (seed: "I didn't go anywhere last month")
- **"quiet"** → `zitto` vs `tranquillo` vs `in silenzio`
    - S0549L02 raw="quiet" → `zitto` (seed: "Because I have got to be quiet")
    - S0361L01 raw="quiet/calm" → `tranquillo` (seed: "he was quiet")
    - S0403L03 raw="quiet/silent" → `in silenzio` (seed: "we should remain quiet for as long as possible")
- **"her"** → `le` vs `lei`
    - S0177L01 raw="her" → `le` (seed: "I'll ask her where she wants to go")
    - S0385L01 raw="her/she" → `lei` (seed: "did you agree with her?")

## deu_for_eng

### true_synonym (11) — Pick different known gloss for one, OR set is_new=false if covered elsewhere, OR merge. Per-item.

- **"to know"** → `wissen` vs `kennen`
    - S0581L02 raw="to know (have knowledge)" → `wissen` (seed: "he'd like to know a little more about what it's like")
    - S0045L02 raw="to know" → `wissen` (seed: "I don't need to know everything")
    - S0472L02 raw="to know (facts)" → `kennen` (seed: "part of the problem is that we don't know the facts")
- **"at the"** → `beim` vs `am`
    - S0117L03 raw="at the" → `beim` (seed: "I'm definitely doing better than I was last time we talked to each other")
    - S0552L01 raw="at the (an+dem)" → `am` (seed: "the church at the other end of the village is ugly")
- **"so"** → `also` vs `um` vs `so`
    - S0056L01 raw="so" → `also` (seed: "so I can remember how to say a few words")
    - S0471L01 raw="so (conj.)" → `um` (seed: "do you want to stop for a rest?")
    - S0538L01 raw="so/thus" → `so` (seed: "I don't want to seem as though I don't care")
- **"after"** → `nachdem` vs `nach`
    - S0110L02 raw="after" → `nachdem` (seed: "we're friends, and after we finish I'd like to relax")
    - S0447L01 raw="after/to (place)" → `nach` (seed: "will they be able to drive us home after the meal?")
- **"to stop"** → `aufhören` vs `anzuhalten`
    - S0019L02 raw="to stop" → `aufhören` (seed: "but I don't want to stop talking")
    - S0402L03 raw="to stop (car)" → `anzuhalten` (seed: "yes it would be nice to stop somewhere for food")
- **"one"** → `man` vs `eines`
    - S0056L03 raw="one" → `man` (seed: "so I can remember how to say a few words")
    - S0634L02 raw="one (neut.)" → `eines` (seed: "I'd like one of those")
- **"nowhere"** → `nirgendwohin` vs `nirgendwo`
    - S0376L02 raw="nowhere/anywhere" → `nirgendwohin` (seed: "I didn't go anywhere last month")
    - S0603L03 raw="nowhere (static)" → `nirgendwo` (seed: "we didn't have anywhere to live at the time")
- **"before"** → `vor` vs `bevor`
    - S0404L02 raw="before" → `vor` (seed: "we shouldn't expect to finish before Thursday")
    - S0470L04 raw="before (conj.)" → `bevor` (seed: "how high do you want to climb before we stop?")
- **"good"** → `gutes` vs `gute`
    - S0407L02 raw="good" → `gutes` (seed: "shouldn't we try to set a good example?")
    - S0478L02 raw="good (neut.)" → `gutes` (seed: "she has such a kind heart")
    - S0541L04 raw="good (adj.)" → `gute` (seed: "it's a good idea to try and breathe slowly")
- **"only"** → `einzige` vs `nur`
    - S0481L01 raw="only (adj.)" → `einzige` (seed: "it's the only real hope we have left")
    - S0562L01 raw="only/just" → `nur` (seed: "I just want to get there safely")
- **"to write"** → `schreiben` vs `einen Brief`
    - S0052L01 raw="to write" → `schreiben` (seed: "he wanted to write a letter to his friend last week")
    - S0052L02 raw="to write" → `einen Brief` (seed: "he wanted to write a letter to his friend last week")

### tense_aspect (1) — Tense/aspect demo: introduce-once-then-free, OR expand with a time/aspect cue so the English prompt is deterministic.

- **"when"** → `wenn` vs `wann`
    - S0435L01 raw="when (conditional)" → `wenn` (seed: "yes they could if they wanted to")
    - S0433L02 raw="when" → `wann` (seed: "they couldn't find out when the film started")
    - S0540L03 raw="when/if" → `wenn` (seed: "I don't mind if you want to leave without the car")

### case_number_register (41) — Expand the LEGO to encode the grammatical role via context (M-type w/ governing prep/verb), OR distinguish on the known side. Needs methodology judgment (known-side control).

- **"children"** → `kinder` vs `kindern`
    - S0580L01 raw="children (acc. pl.)" → `kinder` (seed: "we've often wanted to take the children somewhere a little warmer")
    - S0455L03 raw="children" → `kinder` (seed: "we had to leave because the children were tired")
    - S0567L04 raw="children (dative pl.)" → `kindern` (seed: "it's been lovely spending time outdoors watching the children play")
- **"to take"** → `hinzubringen` vs `nehmen`
    - S0580L03 raw="to take/bring there (zu-inf.)" → `hinzubringen` (seed: "we've often wanted to take the children somewhere a little warmer")
    - S0450L03 raw="to take" → `nehmen` (seed: "no they have to catch the train themselves")
- **"things"** → `sachen` vs `dinge`
    - S0528L02 raw="things" → `sachen` (seed: "I'm probably supposed to keep those things in another room")
    - S0607L01 raw="things (pl.)" → `dinge` (seed: "if I'd known I'd have done things differently")
- **"can"** → `könnt` vs `kannst` vs `können` vs `kann`
    - S0529L05 raw="can (2pl können)" → `könnt` (seed: "can you all put your hands up?")
    - S0090L01 raw="can" → `kannst` (seed: "if you can speak more slowly that would be great")
    - S0469L03 raw="can (modal)" → `können` (seed: "but that doesn't mean we can't change it")
    - S0630L01 raw="can (1sg of können)" → `kann` (seed: "can I have something to drink please?")
- **"you"** → `ihr` vs `ihnen` vs `dich`
    - S0529L03 raw="you (plural nom.)" → `ihr` (seed: "can you all put your hands up?")
    - S0639L01 raw="you (formal, dative)" → `ihnen` (seed: "with you sir")
    - S0524L01 raw="you (accusative)" → `dich` (seed: "I'll call you back in three or four minutes")
- **"your"** → `eure` vs `deine` vs `dein`
    - S0529L04 raw="your (plural poss.)" → `eure` (seed: "can you all put your hands up?")
    - S0614L03 raw="your (informal fem. nom.)" → `deine` (seed: "it's near where your family live")
    - S0121L02 raw="your" → `dein` (seed: "it's unusual that you don't like to use your car")
    - S0627L01 raw="your (masc./neut. nom.)" → `dein` (seed: "your coffee is ready")
    - S0564L01 raw="your (informal sg. feminine)" → `deine` (seed: "I wouldn't have been able to without your help")
- **"have"** → `hast` vs `habe` vs `habt`
    - S0616L01 raw="have (2sg)" → `hast` (seed: "you were very brave to say you thought that")
    - S0037L01 raw="have" → `habe` (seed: "I started to think about it carefully last month")
    - S0663L01 raw="have (2pl of haben)" → `habt` (seed: "what you all said")
- **"my"** → `meinem` vs `meine` vs `meiner`
    - S0585L01 raw="my (dative)" → `meinem` (seed: "I can see the mountains out of my bedroom window")
    - S0463L01 raw="my (fem.)" → `meine` (seed: "my room number")
    - S0594L01 raw="my (acc. fem./neut.)" → `meine` (seed: "I had to share my toys with my sister")
    - S0594L03 raw="my (dat.)" → `meiner` (seed: "I had to share my toys with my sister")
    - S0566L02 raw="my (feminine)" → `meine` (seed: "I'd have thought more if it had been my choice")
- **"would like"** → `möchte` vs `möchten` vs `möchtest`
    - S0581L01 raw="would like (3sg)" → `möchte` (seed: "he'd like to know a little more about what it's like")
    - S0411L01 raw="would like" → `möchten` (seed: "we would like to reserve a table for four tonight")
    - S0634L01 raw="would like (1sg of möchten)" → `möchte` (seed: "I'd like one of those")
    - S0631L01 raw="would like (2sg of möchten)" → `möchtest` (seed: "what would you like?")
- **"to grow up"** → `aufzuwachsen` vs `aufwachsen`
    - S0582L01 raw="to grow up (zu-inf.)" → `aufzuwachsen` (seed: "what's it like to grow up here?")
    - S0588L03 raw="to grow up (inf.)" → `aufwachsen` (seed: "so I wanted to grow up and leave home")
- **"many"** → `viel` vs `viele`
    - S0103L01 raw="many" → `viel` (seed: "we're not trying to hear many more words")
    - S0475L01 raw="many (pl.)" → `viele` (seed: "there are many reasons to consider waiting")
- **"you work"** → `arbeitet` vs `arbeitest`
    - S0133L04 raw="you work" → `arbeitet` (seed: "you get to know someone very well when you work together")
    - S0388L03 raw="you work (singular)" → `arbeitest` (seed: "that person you work with")
- **"new"** → `neue` vs `neues` vs `neuen`
    - S0109L02 raw="new" → `neue` (seed: "we must work hard to learn a lot of new words")
    - S0555L02 raw="new (neut. acc.)" → `neues` (seed: "and I'm too tired to fetch a new one")
    - S0442L01 raw="new (acc.)" → `neuen` (seed: "did they want to develop a new approach?")
    - S0515L03 raw="new (genitive adj.)" → `neuen` (seed: "on the first day of the new year")
    - S0519L02 raw="new (neuter nom.)" → `neues` (seed: "I haven't seen their new baby yet")
- **"different"** → `verschiedene` vs `verschiedenen`
    - S0060L02 raw="different" → `verschiedene` (seed: "I don't know how to say enough different words yet")
    - S0457L04 raw="different (pl.)" → `verschiedenen` (seed: "part of the problem is the number of different areas")
- **"you feel"** → `du fühlst dich` vs `fühlst`
    - S0040L01 raw="you feel" → `du fühlst dich` (seed: "how do you feel at the moment?")
    - S0542L03 raw="you feel (2sg fühlen)" → `fühlst` (seed: "whenever you feel angry")
- **"her"** → `sie` vs `ihre` vs `ihr`
    - S0177L01 raw="her" → `sie` (seed: "I'll ask her where she wants to go")
    - S0637L02 raw="her (fem. nom.)" → `ihre` (seed: "where is her bag?")
    - S0604L03 raw="her (dative)" → `ihr` (seed: "she offered to let us stay with her")
- **"myself"** → `mich` vs `mir`
    - S0056L02 raw="myself" → `mich` (seed: "so I can remember how to say a few words")
    - S0654L01 raw="myself/me (1sg dative)" → `mir` (seed: "I'm not sure if I can help you, sir")
    - S0595L01 raw="myself/me (acc.)" → `mich` (seed: "I need to lie down in the garden")
- **"this"** → `diesem` vs `diese` vs `diesen` vs `dieses`
    - S0583L01 raw="this (dative)" → `diesem` (seed: "what's it like in this part of the world?")
    - S0388L02 raw="this/that (fem.)" → `diese` (seed: "that person you work with")
    - S0445L01 raw="this/that (acc.)" → `diesen` (seed: "they will be able to carry that suitcase")
    - S0527L04 raw="this (fem. acc.)" → `diese` (seed: "guess who told me that funny story")
    - S0534L03 raw="this (masc/neut dat.)" → `diesem` (seed: "let's not go outside in this dreadful weather")
    - S0554L01 raw="this (neut. nom.)" → `dieses` (seed: "this towel isn't dry")
- **"help"** → `helfe` vs `helfen` vs `hilfe`
    - S0171L02 raw="help" → `helfe` (seed: "do you want me to help you look for it?")
    - S0660L01 raw="help (inf.)" → `helfen` (seed: "I can help you all")
    - S0605L01 raw="help (noun)" → `hilfe` (seed: "because we needed help")
- **"me"** → `mir` vs `mich`
    - S0025L03 raw="me" → `mir` (seed: "are you going to help me before I have to go?")
    - S0589L01 raw="me (dative)" → `mir` (seed: "she told me she'd just seen the last bus")
    - S0518L04 raw="me/myself (dative)" → `mir` (seed: "I couldn't imagine myself having exactly the same problem")
    - S0548L03 raw="me (accusative)" → `mich` (seed: "I am feeling sad at the moment")
- **"I feel"** → `ich fühle mich` vs `fühle`
    - S0041L01 raw="I feel" → `ich fühle mich` (seed: "I feel okay, but I'm starting to feel tired")
    - S0548L04 raw="I feel (1sg fühlen)" → `fühle` (seed: "I am feeling sad at the moment")
- **"I think"** → `ich denke` vs `finde`
    - S0047L01 raw="I think" → `ich denke` (seed: "because I think that it's a good thing to make mistakes")
    - S0553L03 raw="I think/find (1sg finden)" → `finde` (seed: "I think the small church is very ugly")
- **"to live"** → `leben` vs `wohnen`
    - S0302L02 raw="to live" → `leben` (seed: "she said that she doesn't want to live in a city")
    - S0603L04 raw="to live/reside (inf.)" → `wohnen` (seed: "we didn't have anywhere to live at the time")
- **"are"** → `sind` vs `seid`
    - S0396L04 raw="are" → `sind` (seed: "we don't need to stand until everybody else is ready")
    - S0649L01 raw="are (3pl/formal of sein)" → `sind` (seed: "are you ready sir?")
    - S0664L01 raw="are (2pl of sein)" → `seid` (seed: "are you all ready?")
- **"all"** → `alle` vs `allen`
    - S0396L03 raw="all/everyone" → `alle` (seed: "we don't need to stand until everybody else is ready")
    - S0656L02 raw="all (dative)" → `allen` (seed: "with you all")
    - S0657L02 raw="all (nom./acc.)" → `alle` (seed: "how do you all feel?")
- **"home"** → `nach Hause` vs `hause`
    - S0401L04 raw="home" → `nach Hause` (seed: "no it would be better to go straight home")
    - S0447L03 raw="home (dat.)" → `hause` (seed: "will they be able to drive us home after the meal?")
- **"to do"** → `machen` vs `tun`
    - S0409L03 raw="to do/make" → `machen` (seed: "no it doesn't matter what we do")
    - S0479L02 raw="to do" → `tun` (seed: "it's the least I could do")
    - S0608L03 raw="to do (inf.)" → `tun` (seed: "it would have been the sensible thing to do")
- **"goes"** → `geht` vs `führt`
    - S0391L02 raw="goes/walks" → `geht` (seed: "the one who is walking towards the bus")
    - S0560L04 raw="goes/leads (3sg führen)" → `führt` (seed: "it goes down to the beach")
- **"hope"** → `hoffnung` vs `hoffe`
    - S0399L01 raw="hope" → `hoffnung` (seed: "we don't want to lose hope")
    - S0668L01 raw="hope (1sg of hoffen)" → `hoffe` (seed: "I hope you'll all be able to go")
- **"everyone"** → `allen` vs `alle` vs `jeden` vs `jeder`
    - S0424L03 raw="everyone (gen.)" → `allen` (seed: "no they're wasting everybody's time")
    - S0511L04 raw="everyone/all" → `alle` (seed: "the news took several hours to reach everyone in the office")
    - S0574L01 raw="everyone/each" → `jeden` (seed: "the holidays are very special for everyone")
    - S0531L03 raw="everyone/anyone" → `jeder` (seed: "it isn't true that anyone can win the game")
- **"problem"** → `problem` vs `problems`
    - S0415L02 raw="problem" → `problem` (seed: "that wouldn't be a problem if you ask me")
    - S0457L02 raw="problem (gen.)" → `problems` (seed: "part of the problem is the number of different areas")
- **"wrong"** → `falschen` vs `unrecht`
    - S0535L03 raw="wrong (adj. acc.)" → `falschen` (seed: "he made a promise that he wouldn't choose the wrong job")
    - S0537L02 raw="wrong/injustice" → `unrecht` (seed: "but I was wrong")
- **"speak"** → `sprechen` vs `sprecht`
    - S0647L01 raw="speak (inf./pl. of sprechen)" → `sprechen` (seed: "you speak it madam")
    - S0662L01 raw="speak (2pl of sprechen)" → `sprecht` (seed: "you all speak it")
- **"want"** → `wollen` vs `willst` vs `wollt`
    - S0643L01 raw="want (inf./pl. of wollen)" → `wollen` (seed: "do you want sir")
    - S0470L02 raw="want (2sg)" → `willst` (seed: "how high do you want to climb before we stop?")
    - S0658L01 raw="want (2pl of wollen)" → `wollt` (seed: "do you all want?")
- **"think"** → `denken` vs `denke` vs `denkt`
    - S0651L01 raw="think (inf./pl. of denken)" → `denken` (seed: "what do you think madam?")
    - S0636L01 raw="think (1sg of denken)" → `denke` (seed: "I think that that is Jane's bag")
    - S0666L01 raw="think (2pl/3sg of denken)" → `denkt` (seed: "what do you all think?")
- **"you all"** → `euch` vs `ihr`
    - S0656L01 raw="you all (2pl dative)" → `euch` (seed: "with you all")
    - S0657L01 raw="you all (2pl nom.)" → `ihr` (seed: "how do you all feel?")
- **"like"** → `mag` vs `magst`
    - S0629L01 raw="like (1sg of mögen)" → `mag` (seed: "I like it with milk but without sugar")
    - S0628L01 raw="like (2sg of mögen)" → `magst` (seed: "do you like tea?")
- **"last"** → `letzten` vs `letzte`
    - S0589L04 raw="last (acc. masc.)" → `letzten` (seed: "she told me she'd just seen the last bus")
    - S0572L03 raw="last (adj.)" → `letzte` (seed: "have you forgotten what happened the last time they visited us?")
- **"I find"** → `ich finde` vs `finde`
    - S0486L01 raw="I find/think" → `ich finde` (seed: "I think your eyes are beautiful")
    - S0526L03 raw="I find (1sg)" → `finde` (seed: "I'm finding it hard to believe that you can't guess")
- **"safe"** → `sicheren` vs `sicher`
    - S0510L01 raw="safe (accusative adj.)" → `sicheren` (seed: "she's gone to look for somewhere safe to park the car")
    - S0562L02 raw="safe/surely" → `sicher` (seed: "I just want to get there safely")
  …and 1 more

### mixed_grammar (10) — Case + tense both — full expansion/redecomposition. Heaviest; per-item.

- **"would have"** → `hätten` vs `hätte`
    - S0612L01 raw="would have (1pl Konj. II)" → `hätten` (seed: "we would have bought something")
    - S0589L02 raw="would have (Konj. II of haben)" → `hätte` (seed: "she told me she'd just seen the last bus")
    - S0563L03 raw="would have (Konj. II plusq.)" → `hätte` (seed: "I wouldn't have been able to keep going any longer")
- **"should"** → `sollte` vs `soll` vs `sollten`
    - S0528L06 raw="should (Konjunktiv II 1sg)" → `sollte` (seed: "I'm probably supposed to keep those things in another room")
    - S0043L02 raw="should" → `soll` (seed: "I wasn't thinking about how to answer")
    - S0438L04 raw="should (3sg past)" → `sollte` (seed: "they didn't want to decide what he should do")
    - S0499L02 raw="should (1pl)" → `sollten` (seed: "maybe we should open the door and close the window")
- **"wanted"** → `gewollt` vs `wollte` vs `wollten`
    - S0580L04 raw="wanted (past part.)" → `gewollt` (seed: "we've often wanted to take the children somewhere a little warmer")
    - S0030L01 raw="wanted" → `wollte` (seed: "I wanted to ask you something yesterday")
    - S0435L02 raw="wanted (past)" → `wollten` (seed: "yes they could if they wanted to")
    - S0588L02 raw="wanted (past 1sg/3sg)" → `wollte` (seed: "so I wanted to grow up and leave home")
- **"thought"** → `gedacht` vs `dachte` vs `fand` vs `dachten`
    - S0616L02 raw="thought (past part.)" → `gedacht` (seed: "you were very brave to say you thought that")
    - S0615L01 raw="thought (1sg past)" → `dachte` (seed: "I thought you were very brave to say that")
    - S0374L01 raw="thought/found" → `fand` (seed: "yes I thought it was very beautiful")
    - S0387L01 raw="thought" → `dachte` (seed: "no I didn't think she was right")
    - S0444L01 raw="thought (3pl past)" → `dachten` (seed: "yes they thought it could be done more efficiently")
    - S0536L03 raw="thought (past 1sg)" → `dachte` (seed: "I used to think that being crazy was bad")
- **"could"** → `konnte` vs `könnten` vs `könntet` vs `könnte` vs `konntest`
    - S0148L02 raw="could" → `konnte` (seed: "he wasn't very patient when I couldn't answer")
    - S0644L01 raw="could (Konj. II of können)" → `könnten` (seed: "could you say that sir?")
    - S0479L03 raw="could (past)" → `konnte` (seed: "it's the least I could do")
    - S0659L01 raw="could (2pl Konj. II of können)" → `könntet` (seed: "could you all say that")
    - S0501L05 raw="could (konjunktiv)" → `könnte` (seed: "if only I could trust you to play together without arguing")
    - S0525L04 raw="could (past 2sg)" → `konntest` (seed: "to check if you've been able to finish")
- **"perfect"** → `perfekt` vs `perfekte`
    - S0137L02 raw="perfect" → `perfekt` (seed: "it's more important to talk often than to be perfect")
    - S0514L01 raw="perfect (acc.)" → `perfekte` (seed: "I found the perfect house on the first day")
- **"had to"** → `musste` vs `mussten`
    - S0593L03 raw="had to (past 1sg)" → `musste` (seed: "however much I argued I still had to share")
    - S0353L01 raw="had to" → `musste` (seed: "she needed to run around the field")
    - S0455L01 raw="had to (past)" → `mussten` (seed: "we had to leave because the children were tired")
    - S0521L05 raw="had to (1pl past)" → `mussten` (seed: "I'm afraid that you'll forget why we needed to stay here")
- **"had"** → `hatte` vs `hatten`
    - S0081L01 raw="had" → `hatte` (seed: "when do you want to start?")
    - S0587L04 raw="had (past 1pl)" → `hatten` (seed: "she used to insist that we had eggs for supper")
    - S0537L01 raw="had (past 1sg)" → `hatte` (seed: "but I was wrong")
- **"will"** → `wird` vs `werdet` vs `werde` vs `wirst`
    - S0094L03 raw="will" → `wird` (seed: "this is the only way it will work")
    - S0668L02 raw="will (2pl of werden)" → `werdet` (seed: "I hope you'll all be able to go")
    - S0465L02 raw="will (future 1sg)" → `werde` (seed: "next time I will ask her what her name is")
    - S0521L04 raw="will (2sg future)" → `wirst` (seed: "I'm afraid that you'll forget why we needed to stay here")
- **"would"** → `würden` vs `würde`
    - S0557L02 raw="would (3pl Konjunktiv II)" → `würden` (seed: "I wish they wouldn't play such loud music")
    - S0485L02 raw="would (conditional)" → `würde` (seed: "nothing would make me happier than to get away")
    - S0535L04 raw="would (Konjunktiv II 3sg)" → `würde` (seed: "he made a promise that he wouldn't choose the wrong job")

### slash_only (14) — Driven by slash resolution — DECIDE THE SLASH FIRST (he/she→seed; synonym→first; word+trailing→keep). May be a false collision from first-option heuristic.

- **"a while"** → `eine Weile` vs `weile`
    - S0092L01 raw="a while" → `eine Weile` (seed: "I'd like to keep on doing this for a while")
    - S0596L02 raw="a while/moment" → `weile` (seed: "and I need to close my eyes for a while")
- **"to pay"** → `zu bezahlen` vs `zahlen`
    - S0299L02 raw="to pay" → `zu bezahlen` (seed: "he wants to pay half")
    - S0569L04 raw="to pay/settle" → `zahlen` (seed: "have you decided how much you are willing to pay?")
- **"thing"** → `Ding` vs `sache`
    - S0257L02 raw="thing" → `Ding` (seed: "I like that blue thing")
    - S0573L03 raw="thing/matter" → `sache` (seed: "it's the kind of thing that makes the holidays very special")
- **"time"** → `Zeit` vs `mal`
    - S0027L03 raw="time" → `Zeit` (seed: "I don't like taking too much time to answer")
    - S0568L03 raw="time/occasion" → `mal` (seed: "I'm expecting to see an improvement next time we meet")
- **"happy"** → `zufrieden` vs `glücklich`
    - S0245L01 raw="happy" → `zufrieden` (seed: "I'm happy with how much I've done in a short time")
    - S0599L02 raw="happy/lucky" → `glücklich` (seed: "I'd have been happy to drive if you'd told me")
- **"anywhere"** → `irgendwo` vs `irgendwohin`
    - S0182L01 raw="anywhere" → `irgendwo` (seed: "have you seen my keys anywhere?")
    - S0377L02 raw="anywhere/somewhere" → `irgendwohin` (seed: "did you go anywhere in the world last year?")
- **"to put"** → `stecken` vs `hinstellen`
    - S0053L02 raw="to put" → `stecken` (seed: "she wanted to put his letter in her bag")
    - S0382L02 raw="to put/place" → `hinstellen` (seed: "did you ask where he wanted to put it?")
- **"to watch"** → `schauen` vs `zuzusehen`
    - S0313L01 raw="to watch" → `schauen` (seed: "he said that he couldn't watch all five games")
    - S0567L06 raw="to watch/look on" → `zuzusehen` (seed: "it's been lovely spending time outdoors watching the children play")
- **"place"** → `ort` vs `platz`
    - S0364L02 raw="place" → `ort` (seed: "I heard that he didn't like that place")
    - S0510L02 raw="place/spot" → `platz` (seed: "she's gone to look for somewhere safe to park the car")
- **"if"** → `ob` vs `wenn`
    - S0381L01 raw="if" → `ob` (seed: "I didn't ask if he wanted to follow us")
    - S0413L03 raw="if/when" → `wenn` (seed: "we could fall if we go too close to the edge")
- **"way"** → `weg` vs `weise`
    - S0408L02 raw="way" → `weg` (seed: "yes that's the best way to make a happy family")
    - S0443L04 raw="way/manner" → `weise` (seed: "no they wanted to keep doing it the same way")
- **"still"** → `immer noch` vs `noch`
    - S0410L02 raw="still" → `immer noch` (seed: "they still fight with each other")
    - S0593L04 raw="still/yet" → `noch` (seed: "however much I argued I still had to share")
- **"nearby"** → `nähe` vs `in der Nähe`
    - S0390L01 raw="nearby/near" → `nähe` (seed: "the one who is standing near the entrance")
    - S0506L02 raw="nearby / around here" → `in der Nähe` (seed: "I used to live around here years ago before we moved")
- **"number"** → `zahl` vs `anzahl`
    - S0434L01 raw="number" → `zahl` (seed: "could they cut the number of students they take?")
    - S0457L03 raw="number/count" → `anzahl` (seed: "part of the problem is the number of different areas")

## zho_for_eng

### true_synonym (13) — Pick different known gloss for one, OR set is_new=false if covered elsewhere, OR merge. Per-item.

- **"but"** → `但是` vs `但`
    - S0019L01 raw="but" → `但是` (seed: "but I don't want to stop talking")
    - S0426L03 raw="but / however (in contrast)" → `但` (seed: "They would like to love each other but they're unhappy.")
    - S0537L01 raw="but / however" → `但` (seed: "But I was wrong.")
    - S0539L01 raw="but / however (full form)" → `但是` (seed: "But it's getting late.")
- **"time"** → `时间` vs `遍`
    - S0027L03 raw="time" → `时间` (seed: "I don't like taking too much time to answer")
    - S0644L02 raw="time / occurrence (measure word)" → `遍` (seed: "Could you say that sir?")
    - S0539L02 raw="time / duration" → `时间` (seed: "But it's getting late.")
- **"a bit"** → `有点` vs `点`
    - S0039L02 raw="a bit" → `有点` (seed: "but I'm a little tired this morning")
    - S0625L01 raw="a bit / some / a little (measure)" → `点` (seed: "Do you want something to drink?")
- **"how long"** → `多久了` vs `多久`
    - S0033L02 raw="how long (up to now)" → `多久了` (seed: "how long have you been learning Chinese?")
    - S0033L01 raw="how long" → `多久` (seed: "how long have you been learning Chinese?")
- **"almost"** → `几乎` vs `差点`
    - S0449L01 raw="almost / nearly" → `几乎` (seed: "They said that they were almost ready.")
    - S0502L01 raw="almost (colloquial) / just barely missed" → `差点` (seed: "She almost got lost because she turned left instead of right.")
- **"everyone"** → `大家` vs `每个人` vs `所有人`
    - S0424L01 raw="everyone / everybody (informal)" → `大家` (seed: "No they're wasting everybody's time.")
    - S0511L05 raw="everyone / each person" → `每个人` (seed: "The news took several hours to reach everyone in the office.")
    - S0396L01 raw="everyone / all people" → `所有人` (seed: "We don't need to stand until everybody else is ready.")
- **"okay"** → `还行` vs `好的`
    - S0041L01 raw="okay" → `还行` (seed: "I feel okay, but I'm starting to feel tired")
    - S0624L01 raw="okay / alright / yes (acceptance)" → `好的` (seed: "Yes, that would be great, please.")
- **"to think"** → `以为` vs `觉得`
    - S0536L01 raw="to think / assume / believe (mistakenly)" → `以为` (seed: "I used to think that being crazy was bad.")
    - S0553L01 raw="to think / feel / believe" → `觉得` (seed: "I think the small church is very ugly.")
    - S0041L02 raw="to think" → `觉得` (seed: "I feel okay, but I'm starting to feel tired")
- **"to speak"** → `新事` vs `说`
    - S0227L01 raw="to speak" → `新事` (seed: "that man is going to tell me something new")
    - S0001L02 raw="to speak" → `说` (seed: "I want to speak Chinese with you now")
- **"finish"** → `结束` vs `完成`
    - S0050L01 raw="finish" → `结束` (seed: "I'm not trying to finish as quickly as possible")
    - S0149L03 raw="finish" → `完成` (seed: "this isn't very difficult, so I hope you'll finish soon")
- **"if"** → `如果` vs `的话`
    - S0049L01 raw="if" → `如果` (seed: "It's like this, if you know what I mean")
    - S0229L02 raw="if" → `的话` (seed: "that woman would help you if she could")
- **"to know"** → `知道` vs `认识`
    - S0045L01 raw="to know" → `知道` (seed: "I don't need to know everything")
    - S0230L01 raw="to know" → `认识` (seed: "I know a young man who wants to work with you")
- **"to remember"** → `想起` vs `记住`
    - S0006L01 raw="to remember" → `想起` (seed: "I'm trying to remember a word")
    - S0010L04 raw="to remember" → `记住` (seed: "I'm not sure if I can remember the whole sentence")

### tense_aspect (1) — Tense/aspect demo: introduce-once-then-free, OR expand with a time/aspect cue so the English prompt is deterministic.

- **"to want"** → `试试` vs `想要` vs `要`
    - S0222L01 raw="to want" → `试试` (seed: "he's trying to tell me what he wants")
    - S0473L01 raw="to want / desire" → `想要` (seed: "I don't want it.")
    - S0631L01 raw="to want / to need / will (future)" → `要` (seed: "What would you like?")

### case_number_register (3) — Expand the LEGO to encode the grammatical role via context (M-type w/ governing prep/verb), OR distinguish on the known side. Needs methodology judgment (known-side control).

- **"you"** → `你` vs `您`
    - S0020L01 raw="you" → `你` (seed: "you want to learn his name quickly")
    - S0642L01 raw="you (polite/formal)" → `您` (seed: "how do you feel madam?")
- **"things"** → `事` vs `东西`
    - S0051L04 raw="things" → `事` (seed: "I enjoy doing interesting things with my friends")
    - S0612L02 raw="things / stuff / object" → `东西` (seed: "We would have bought something.")
- **"to turn left"** → `向左转` vs `左转`
    - S0359L01 raw="to turn left" → `向左转` (seed: "Your friend said that he could turn left.")
    - S0502L03 raw="to turn left (casual)" → `左转` (seed: "She almost got lost because she turned left instead of right.")

### slash_only (40) — Driven by slash resolution — DECIDE THE SLASH FIRST (he/she→seed; synonym→first; word+trailing→keep). May be a false collision from first-option heuristic.

- **"ready"** → `准备好` vs `好了` vs `准备好了`
    - S0088L01 raw="ready" → `准备好` (seed: "I'm not ready to talk to people I don't know yet")
    - S0627L01 raw="ready / done / finished" → `好了` (seed: "Your coffee is ready.")
    - S0095L01 raw="ready" → `准备好` (seed: "are you ready to go home on the next bus?")
    - S0649L01 raw="ready / all set / prepared" → `准备好了` (seed: "Are you ready sir?")
- **"kind"** → `友善` vs `善良`
    - S0147L02 raw="kind" → `友善` (seed: "she was very kind when she saw me feeling nervous")
    - S0478L03 raw="kind / good-natured / benevolent" → `善良` (seed: "She has such a kind heart.")
- **"can"** → `会` vs `可以`
    - S0009L01 raw="can" → `会` (seed: "I speak a little Chinese now")
    - S0630L01 raw="can / may / permitted to" → `可以` (seed: "Can I have something to drink please?")
    - S0359L02 raw="can / be allowed to" → `可以` (seed: "Your friend said that he could turn left.")
- **"towards"** → `对` vs `朝`
    - S0076L01 raw="towards" → `对` (seed: "I'm very happy with how much I've learnt already")
    - S0391L01 raw="towards / in the direction of" → `朝` (seed: "The one who is walking towards the bus.")
- **"to feel"** → `感到` vs `感觉`
    - S0343L02 raw="to feel" → `感到` (seed: "that person said she feels worried about the economy")
    - S0618L01 raw="to feel / feeling / sensation" → `感觉` (seed: "It doesn't feel like a long time.")
    - S0542L02 raw="to feel / sense / realize" → `感到` (seed: "Whenever you feel angry.")
- **"to help"** → `帮` vs `帮忙`
    - S0025L04 raw="to help" → `帮` (seed: "are you going to help me before I have to go?")
    - S0491L02 raw="to help / lend a hand" → `帮忙` (seed: "I love the way you try to help.")
    - S0645L01 raw="to help / to assist" → `帮` (seed: "I can help you madam")
- **"city"** → `市` vs `城市`
    - S0198L01 raw="city" → `市` (seed: "my daughter works for the council")
    - S0507L01 raw="city / urban area" → `城市` (seed: "We moved to the city.")
- **"next"** → `下一` vs `接下来`
    - S0095L04 raw="next" → `下一` (seed: "are you ready to go home on the next bus?")
    - S0493L01 raw="next / coming up / following" → `接下来` (seed: "What's going to come next?")
- **"at that time"** → `当时` vs `那时`
    - S0043L02 raw="at that time" → `当时` (seed: "I wasn't thinking about how to answer")
    - S0603L01 raw="at that time / then / back then" → `那时` (seed: "We didn't have anywhere to live at the time.")
- **"or"** → `或者` vs `还是`
    - S0044L01 raw="or" → `或者` (seed: "or if I need to improve")
    - S0623L03 raw="or / or else / still" → `还是` (seed: "Do you want a cup of coffee or tea?")
- **"meaning"** → `意思` vs `意义`
    - S0008L03 raw="meaning" → `意思` (seed: "I'm going to try to explain what I mean")
    - S0508L03 raw="meaning / point / significance" → `意义` (seed: "There's no point worrying about how you're going to pay.")
- **"to do"** → `做` vs `弄`
    - S0051L01 raw="to do" → `做` (seed: "I enjoy doing interesting things with my friends")
    - S0622L03 raw="to do / to make / to cause" → `弄` (seed: "I knew that the children had broken it.")
- **"more"** → `多` vs `更`
    - S0023L03 raw="more" → `多` (seed: "I'm going to start talking more soon")
    - S0363L01 raw="more / a lot / many" → `多` (seed: "Yes he felt like talking a lot.")
    - S0485L02 raw="more / even more / comparatively" → `更` (seed: "Nothing would make me happier than to get away.")
- **"people"** → `人` vs `人们`
    - S0022L03 raw="people" → `人` (seed: "because I want to meet people who speak Chinese")
    - S0419L01 raw="people / folks" → `人们` (seed: "If they want people to like them.")
- **"help"** → `帮忙` vs `帮助`
    - S0168L02 raw="help" → `帮忙` (seed: "and then I'll be able to come and help later on")
    - S0605L03 raw="help / to help" → `帮助` (seed: "Because we needed help.")
    - S0563L01 raw="help / assistance" → `帮助` (seed: "I wouldn't have been able to keep going any longer.")
- **"willing"** → `愿意` vs `乐意`
    - S0271L01 raw="willing" → `愿意` (seed: "would you like to come with us next month?")
    - S0569L02 raw="willing / want to / be happy to" → `愿意` (seed: "Have you decided how much you are willing to pay?")
    - S0448L01 raw="willing / happy to" → `乐意` (seed: "Yes they'll be happy to help.")
- **"where"** → `哪` vs `哪里`
    - S0070L02 raw="where" → `哪` (seed: "she didn't want to tell me where it was")
    - S0637L01 raw="where / which place" → `哪里` (seed: "Where is her bag?")
- **"all"** → `所有` vs `都`
    - S0331L02 raw="all" → `所有` (seed: "she can't provide all the answers")
    - S0668L02 raw="all / both / every one" → `都` (seed: "I hope you'll all be able to go")
- **"about"** → `大概` vs `关于`
    - S0038L02 raw="about" → `大概` (seed: "I've been learning for about a week")
    - S0597L03 raw="about / regarding / concerning" → `关于` (seed: "I suspect that he's heard a hundred stories about it.")
- **"interesting"** → `有趣` vs `意思`
    - S0051L03 raw="interesting" → `有趣` (seed: "I enjoy doing interesting things with my friends")
    - S0527L01 raw="interesting / fun / amusing" → `有趣` (seed: "Guess who told me that funny story.")
    - S0492L02 raw="interesting / meaning / fun" → `意思` (seed: "Which of those places do you think is the most interesting?")
- **"should"** → `应该` vs `该`
    - S0098L01 raw="should" → `应该` (seed: "I should consider playing something else")
    - S0545L01 raw="should / ought to / it's time for" → `该` (seed: "It's your turn to take the clean clothes upstairs.")
- **"to tell"** → `告诉` vs `讲`
    - S0222L02 raw="to tell" → `告诉` (seed: "he's trying to tell me what he wants")
    - S0527L03 raw="to tell / narrate / recount" → `讲` (seed: "Guess who told me that funny story.")
- **"serious"** → `严重` vs `认真`
    - S0339L03 raw="serious" → `严重` (seed: "no, I think he's quite seriously hurt")
    - S0482L01 raw="serious / earnest / conscientious" → `认真` (seed: "The only real hope is that they're not serious.")
- **"the same"** → `同一` vs `一样`
    - S0143L01 raw="the same" → `同一` (seed: "It's the same thing as we were talking about earlier")
    - S0518L05 raw="the same / identical / alike" → `一样` (seed: "I couldn't imagine myself having exactly the same problem.")
- **"reason"** → `原因` vs `理由`
    - S0105L01 raw="reason" → `原因` (seed: "that is why he didn't know the answer")
    - S0475L01 raw="reason / grounds" → `理由` (seed: "There are many reasons to consider waiting.")
- **"way"** → `办法` vs `方式`
    - S0094L01 raw="way" → `办法` (seed: "this is the only way it will work")
    - S0491L03 raw="way / manner / method" → `方式` (seed: "I love the way you try to help.")
- **"family"** → `家庭` vs `家人`
    - S0408L02 raw="family" → `家庭` (seed: "Yes that's the best way to make a happy family.")
    - S0614L02 raw="family / family members" → `家人` (seed: "It's near where your family live.")
    - S0520L02 raw="family / household" → `家庭` (seed: "It might have happened to the whole family.")
- **"measure word for buildings"** → `所` vs `座`
    - S0514L02 raw="measure word for buildings" → `所` (seed: "I found the perfect house on the first day.")
    - S0551L01 raw="measure word for buildings/mountains" → `座` (seed: "The church is ugly.")
- **"to lose"** → `输` vs `失去`
    - S0496L02 raw="to lose / be defeated" → `输` (seed: "I'm not planning to lose when it really matters.")
    - S0399L01 raw="to lose / give up" → `失去` (seed: "We don't want to lose hope.")
- **"to pay"** → `付钱` vs `付`
    - S0508L02 raw="to pay / pay money" → `付钱` (seed: "There's no point worrying about how you're going to pay.")
    - S0569L03 raw="to pay / hand over" → `付` (seed: "Have you decided how much you are willing to pay?")
- **"each other"** → `彼此` vs `互相`
    - S0426L01 raw="each other / one another" → `彼此` (seed: "They would like to love each other but they're unhappy.")
    - S0410L01 raw="each other / mutually" → `互相` (seed: "They still fight with each other.")
- **"to argue"** → `争吵` vs `争论`
    - S0410L02 raw="to argue / fight" → `争吵` (seed: "They still fight with each other.")
    - S0593L02 raw="to argue / debate / dispute" → `争论` (seed: "However much I argued I still had to share.")
- **"to stay"** → `留` vs `待着`
    - S0521L01 raw="to stay / remain / keep" → `留` (seed: "I'm afraid that you'll forget why we needed to stay here.")
    - S0351L01 raw="to stay / hang around" → `待着` (seed: "No he didn't want to leave me on my own.")
- **"to expect"** → `指望` vs `期待`
    - S0404L01 raw="to expect / count on / hope for" → `指望` (seed: "We shouldn't expect to finish before Thursday.")
    - S0568L01 raw="to expect / look forward to" → `期待` (seed: "I'm expecting to see an improvement next time we meet.")
- **"to forget"** → `忘` vs `忘了`
    - S0572L01 raw="to forget" → `忘` (seed: "Have you forgotten what happened the last time they visited us?")
    - S0464L01 raw="to forget / forgot" → `忘了` (seed: "I told her my room number once but she forgot it.")
- **"to play"** → `玩` vs `放`
    - S0501L01 raw="to play / have fun" → `玩` (seed: "If only I could trust you to play together without arguing.")
    - S0556L02 raw="to play / release / put" → `放` (seed: "They play music late at night.")
- **"just"** → `刚` vs `只是`
    - S0589L01 raw="just / just now / just barely" → `刚` (seed: "She told me she'd just seen the last bus.")
    - S0357L01 raw="just / only" → `只是` (seed: "No she just wanted to send her a message.")
- **"field"** → `场地` vs `领域`
    - S0353L01 raw="field / venue" → `场地` (seed: "She needed to run around the field.")
    - S0457L02 raw="field / area / domain" → `领域` (seed: "Part of the problem is the number of different areas.")
- **"to start"** → `开始` vs `会问`
    - S0023L02 raw="to start" → `开始` (seed: "I'm going to start talking more soon")
    - S0601L03 raw="to start / begin" → `开始` (seed: "We were talking about how it all started.")
    - S0223L01 raw="to start" → `会问` (seed: "he's going to ask you tomorrow")
- **"to try"** → `足球` vs `试试` vs `试着`
    - S0221L01 raw="to try" → `足球` (seed: "I watched the football and then I watched a film")
    - S0002L01 raw="to try" → `试试` (seed: "I'm trying to learn")
    - S0407L01 raw="to try / attempt" → `试着` (seed: "Shouldn't we try to set a good example?")

## jpn_for_eng

### true_synonym (10) — Pick different known gloss for one, OR set is_new=false if covered elsewhere, OR merge. Per-item.

- **"woman"** → `女の人` vs `女性`
    - S0305L01 raw="woman" → `女の人` (seed: "woman")
    - S0232L02 raw="woman" → `女性` (seed: "I know an old woman who can remember the answer")
- **"over there"** → `あそこ` vs `向こう`
    - S0258L01 raw="over there" → `あそこ` (seed: "what's that blue thing over there?")
    - S0466L02 raw="over there" → `向こう` (seed: "let me throw it over the wall")
- **"can do"** → `できる` vs `できます`
    - S0317L02 raw="can do" → `できる` (seed: "yes I think she could if she wanted to")
    - S0645L02 raw="can do" → `できます` (seed: "I can help you madam")
- **"three"** → `三つ` vs `三`
    - S0311L01 raw="three" → `三つ` (seed: "he couldn't believe the three most important facts")
    - S0524L01 raw="three" → `三` (seed: "I'll call you back in three or four minutes")
- **"that"** → `その` vs `それが`
    - S0336L01 raw="that" → `その` (seed: "I don't think that she can open the door")
    - S0127L01 raw="that" → `それが` (seed: "that isn't why I wanted to see you")
- **"can speak"** → `話せる` vs `話せます`
    - S0009L01 raw="can speak" → `話せる` (seed: "I speak a little Japanese now")
    - S0662L01 raw="can speak" → `話せます` (seed: "you all speak it")
- **"think"** → `思う` vs `思います`
    - S0486L04 raw="think" → `思う` (seed: "I think your eyes are beautiful")
    - S0666L01 raw="think" → `思います` (seed: "what do you all think?")
- **"don't mind"** → `気にしない` vs `気にしませんか`
    - S0540L03 raw="don't mind" → `気にしない` (seed: "I don't mind if you want to leave without the car")
    - S0667L01 raw="don't mind" → `気にしませんか` (seed: "do you all mind?")
- **"can help"** → `お手伝いできます` vs `お手伝いできる`
    - S0660L01 raw="can help" → `お手伝いできます` (seed: "I can help you all")
    - S0654L01 raw="can help" → `お手伝いできる` (seed: "I'm not sure if I can help you, sir")
- **"are doing"** → `おられます` vs `されています` vs `やってる`
    - S0655L03 raw="are doing" → `おられます` (seed: "I think that you're doing very well, madam")
    - S0661L01 raw="are doing" → `されています` (seed: "you're all doing something")
    - S0033L01 raw="are doing" → `やってる` (seed: "how long have you been learning Japanese?")

## por_for_eng

### true_synonym (7) — Pick different known gloss for one, OR set is_new=false if covered elsewhere, OR merge. Per-item.

- **"to be"** → `ficar` vs `ser`
    - S0340L01 raw="to be (okay)" → `ficar` (seed: "yes I'm sure he's going to be okay")
    - S0137L02 raw="to be" → `ser` (seed: "It's more important to talk often than to be perfect")
- **"I heard"** → `ouvi` vs `ouvi dizer que`
    - S0365L01 raw="I heard" → `ouvi` (seed: "I didn't hear what she said to him")
    - S0364L01 raw="I heard (that)" → `ouvi dizer que` (seed: "I heard that he didn't like that place")
- **"that"** → `essa` vs `isso`
    - S0408L02 raw="that (demonstrative)" → `essa` (seed: "yes that's the best way to make a happy family")
    - S0061L02 raw="that" → `isso` (seed: "could you say that again a little more slowly?")
    - S0497L01 raw="that / it" → `isso` (seed: "that sounds as though you need to get some sleep")
- **"them"** → `eles` vs `as`
    - S0134L03 raw="them" → `eles` (seed: "It's not a problem when you work at something difficult with them")
    - S0183L01 raw="them (e.g. the keys)" → `as` (seed: "no I'm afraid I haven't seen them")
- **"kind"** → `simpático` vs `simpática` vs `bondoso`
    - S0142L01 raw="kind" → `simpático` (seed: "that's very kind of you and I'm grateful to you for helping")
    - S0147L03 raw="kind (about a woman)" → `simpática` (seed: "she was very kind when she saw me feeling nervous")
    - S0478L01 raw="kind/good-natured" → `bondoso` (seed: "she has such a kind heart")
- **"when"** → `quando é que` vs `quando`
    - S0079L01 raw="when (for questions)" → `quando é que` (seed: "when did you start to learn?")
    - S0111L01 raw="when" → `quando` (seed: "when we learn something new it changes our brain")
- **"your"** → `tua` vs `teu`
    - S0125L02 raw="your" → `tua` (seed: "I believe that your idea was very good")
    - S0570L02 raw="your (masc)" → `teu` (seed: "how much are you willing to pay for your ticket?")

### tense_aspect (1) — Tense/aspect demo: introduce-once-then-free, OR expand with a time/aspect cue so the English prompt is deterministic.

- **"has"** → `tem` vs `tenha`
    - S0324L02 raw="has" → `tem` (seed: "that student has both of her hands up")
    - S0532L01 raw="has / have (subjunctive)" → `tenha` (seed: "unless they're lucky")

### case_number_register (3) — Expand the LEGO to encode the grammatical role via context (M-type w/ governing prep/verb), OR distinguish on the known side. Needs methodology judgment (known-side control).

- **"my"** → `as minhas` vs `minha`
    - S0182L02 raw="my (plural)" → `as minhas` (seed: "have you seen my keys anywhere?")
    - S0131L05 raw="my" → `minha` (seed: "there are too many ideas going around in my head")
- **"possible"** → `possíveis` vs `possível`
    - S0325L04 raw="possible (pl.)" → `possíveis` (seed: "I think that he needs to consider ten possible problems")
    - S0430L01 raw="possible" → `possível` (seed: "no that wouldn't be possible")
- **"patient"** → `pacientes` vs `paciente`
    - S0398L01 raw="patient (plural)" → `pacientes` (seed: "we want to become more patient with our children")
    - S0148L02 raw="patient" → `paciente` (seed: "he wasn't very patient when I couldn't answer")

### slash_only (8) — Driven by slash resolution — DECIDE THE SLASH FIRST (he/she→seed; synonym→first; word+trailing→keep). May be a false collision from first-option heuristic.

- **"two"** → `duas` vs `dois`
    - S0324L04 raw="two / both" → `duas` (seed: "that student has both of her hands up")
    - S0217L03 raw="two" → `dois` (seed: "I had a glass or two of water")
- **"place"** → `lugar` vs `sítio`
    - S0364L02 raw="place" → `lugar` (seed: "I heard that he didn't like that place")
    - S0402L01 raw="place/spot" → `sítio` (seed: "yes it would be nice to stop somewhere for food")
    - S0578L02 raw="place / spot" → `sítio` (seed: "somewhere warmer")
- **"children"** → `filhos` vs `crianças`
    - S0398L02 raw="children/sons" → `filhos` (seed: "we want to become more patient with our children")
    - S0455L01 raw="children" → `crianças` (seed: "we had to leave because the children were tired")
    - S0622L01 raw="children / kids" → `crianças` (seed: "I knew that the children had broken it")
- **"a little"** → `um pouco de` vs `um pouco`
    - S0009L02 raw="a little" → `um pouco de` (seed: "I speak a little Portuguese now")
    - S0497L04 raw="a little / some" → `um pouco` (seed: "that sounds as though you need to get some sleep")
- **"to be able to"** → `conseguir` vs `poder`
    - S0011L02 raw="to be able to" → `conseguir` (seed: "I'd like to be able to speak after you finish")
    - S0530L01 raw="to be able to / can" → `poder` (seed: "so I can count them")
- **"the way"** → `caminho` vs `a maneira`
    - S0416L01 raw="the way/path" → `caminho` (seed: "they want to lead the way next year")
    - S0491L01 raw="the way" → `a maneira` (seed: "I love the way you try to help")
- **"to raise"** → `angariar` vs `levantar`
    - S0437L02 raw="to raise/collect" → `angariar` (seed: "they wanted to raise as much as possible for charity")
    - S0529L01 raw="to raise / to lift" → `levantar` (seed: "can you all put your hands up?")
- **"wrong"** → `errado` vs `enganado`
    - S0535L05 raw="wrong" → `errado` (seed: "he made a promise that he wouldn't choose the wrong job")
    - S0537L01 raw="wrong / mistaken" → `enganado` (seed: "but I was wrong")

## kor_for_eng

### punct_artifact (1) — NOT a real ZUT — resolves when the missing-?/punctuation fix runs. Verify after.

- **"do you want?"** → `원하세요` vs `원하세요?`
    - S0643L02 raw="do you want? (honorific)" → `원하세요` (seed: "do you want sir")
    - S0658L02 raw="do you want? (polite)" → `원하세요?` (seed: "do you all want?")

### true_synonym (25) — Pick different known gloss for one, OR set is_new=false if covered elsewhere, OR merge. Per-item.

- **"I think"** → `것 같아요` vs `생각해요`
    - S0655L04 raw="I think (it seems)" → `것 같아요` (seed: "I think that you're doing very well, madam")
    - S0553L04 raw="I think" → `생각해요` (seed: "I think the small church is very ugly")
- **"time"** → `시간을` vs `번`
    - S0054L02 raw="time" → `시간을` (seed: "we wanted to give you a little more time")
    - S0464L01 raw="time / once (counter)" → `번` (seed: "I told her my room number once but she forgot it")
- **"happy"** → `기뻐요` vs `행복한`
    - S0076L01 raw="happy" → `기뻐요` (seed: "I'm very happy with how much I've learnt already")
    - S0408L03 raw="happy (adj. / contented)" → `행복한` (seed: "yes that's the best way to make a happy family")
- **"do"** → `해요` vs `해`
    - S0002L01 raw="do" → `해요` (seed: "I'm trying to learn")
    - S0485L01 raw="do / make (light verb)" → `해` (seed: "nothing would make me happier than to get away")
- **"two"** → `둘` vs `두`
    - S0516L01 raw="two / both" → `둘` (seed: "they both came on their own")
    - S0500L01 raw="two (numeral)" → `두` (seed: "Why don't you want to sit between the two girls?")
- **"doesn't want to"** → `싶지 않다고` vs `싶어하지 않는`
    - S0302L03 raw="doesn't want to (reported)" → `싶지 않다고` (seed: "she said that she doesn't want to live in a city")
    - S0304L03 raw="doesn't want to (3rd person participle)" → `싶어하지 않는` (seed: "I think that she doesn't want to work from home")
- **"about"** → `남자에 대해` vs `대한`
    - S0310L01 raw="about" → `남자에 대해` (seed: "she could write a story about that man")
    - S0598L04 raw="about (that matter)" → `대한` (seed: "he's heard a thousand stories about what they were doing")
- **"woman"** → `여자` vs `여자와`
    - S0305L01 raw="woman" → `여자` (seed: "woman")
    - S0355L01 raw="woman (with particle)" → `여자와` (seed: "did she need to talk to that woman you know?")
- **"to look for"** → `찾으러` vs `찾아야`
    - S0510L03 raw="to look for (purposive)" → `찾으러` (seed: "she's gone to look for somewhere safe to park the car")
    - S0610L02 raw="to look for" → `찾아야` (seed: "he needs to look for work")
- **"seeing"** → `봐` vs `보는 게`
    - S0521L01 raw="seeing / afraid of (modal particle)" → `봐` (seed: "I'm afraid that you'll forget why we needed to stay here")
    - S0584L04 raw="seeing" → `보는 게` (seed: "what's it like to wake up every morning and see the mountains?")
- **"to win"** → `이길` vs `이기게`
    - S0531L01 raw="to win (modifier form)" → `이길` (seed: "it isn't true that anyone can win the game")
    - S0412L01 raw="to win (causative)" → `이기게` (seed: "we couldn't allow them to win everything")
- **"possible"** → `가능한` vs `가능하지`
    - S0325L02 raw="possible" → `가능한` (seed: "I think that he needs to consider ten possible problems")
    - S0430L01 raw="possible (negation)" → `가능하지` (seed: "no that wouldn't be possible")
- **"go out"** → `나가고` vs `나가지`
    - S0349L02 raw="go out (connective form)" → `나가고` (seed: "did he want to go out on Friday night?")
    - S0534L03 raw="go out (negative connector)" → `나가지` (seed: "let's not go outside in this dreadful weather")
- **"go"** → `가고` vs `갈`
    - S0351L03 raw="go (connective form)" → `가고` (seed: "no he didn't want to leave me on my own")
    - S0378L01 raw="go (modifier form)" → `갈` (seed: "no I didn't have enough money for holidays")
- **"make"** → `만들` vs `만들어`
    - S0332L04 raw="make / build (potential form)" → `만들` (seed: "he can build a new life for his sister")
    - S0489L05 raw="make (and/stem)" → `만들어` (seed: "if you don't make me a strong cup of coffee right now")
- **"absolutely"** → `완전히` vs `절대`
    - S0544L03 raw="absolutely" → `완전히` (seed: "whoever said it would be difficult was absolutely right")
    - S0490L02 raw="absolutely / never (emphasis)" → `절대` (seed: "then I will never trust anyone ever again")
- **"holiday"** → `휴가` vs `휴가를` vs `휴가는`
    - S0378L02 raw="holiday / vacation" → `휴가` (seed: "no I didn't have enough money for holidays")
    - S0573L01 raw="holiday" → `휴가를` (seed: "it's the kind of thing that makes the holidays very special")
    - S0574L01 raw="holiday (topic)" → `휴가는` (seed: "the holidays are very special for everyone")
- **"near"** → `근처에` vs `근처에서` vs `근처예요`
    - S0390L03 raw="near / nearby" → `근처에` (seed: "the one who is standing near the entrance")
    - S0460L04 raw="near / nearby (locative)" → `근처에서` (seed: "I want to find a shop near the hotel")
    - S0614L03 raw="near/nearby" → `근처예요` (seed: "it's near where your family live")
- **"to be"** → `있을` vs `있어도`
    - S0396L01 raw="to be (potential form)" → `있을` (seed: "we don't need to stand until everybody else is ready")
    - S0604L02 raw="to be/stay (even if)" → `있어도` (seed: "she offered to let us stay with her")
- **"in the garden"** → `정원에` vs `정원에서`
    - S0383L01 raw="in the garden" → `정원에` (seed: "yes he said he wanted to put it in the garden")
    - S0595L01 raw="in the garden (activity)" → `정원에서` (seed: "I need to lie down in the garden")
- **"waiting"** → `기다리는 게` vs `기다리는`
    - S0576L01 raw="waiting" → `기다리는 게` (seed: "it has been very upsetting waiting")
    - S0475L03 raw="waiting (modifier)" → `기다리는` (seed: "there are many reasons to consider waiting")
- **"to visit"** → `방문하고` vs `놀러`
    - S0428L01 raw="to visit / visiting (and-form)" → `방문하고` (seed: "would they like to visit us on Tuesday?")
    - S0454L01 raw="to visit / to hang out" → `놀러` (seed: "our friends came round at about six o'clock")
- **"saw"** → `봤어요` vs `봤는지`
    - S0590L04 raw="saw/watched" → `봤어요` (seed: "I saw the last bus leaving the station")
    - S0453L01 raw="saw / who they saw (embedded)" → `봤는지` (seed: "did they say who they saw last night?")
- **"leaving"** → `떠나는 걸` vs `떠나는`
    - S0590L03 raw="leaving/departing (seeing)" → `떠나는 걸` (seed: "I saw the last bus leaving the station")
    - S0485L02 raw="leaving / getting away (modifier)" → `떠나는` (seed: "nothing would make me happier than to get away")
- **"to drink"** → `마시고` vs `마실`
    - S0625L01 raw="to drink (and)" → `마시고` (seed: "do you want something to drink?")
    - S0630L01 raw="to drink (attributive)" → `마실` (seed: "can I have something to drink please?")

### tense_aspect (5) — Tense/aspect demo: introduce-once-then-free, OR expand with a time/aspect cue so the English prompt is deterministic.

- **"didn't have"** → `없었던` vs `없었어요`
    - S0315L04 raw="didn't have (past attr.)" → `없었던` (seed: "I think that he couldn't afford the car that he wanted")
    - S0603L03 raw="didn't have/wasn't" → `없었어요` (seed: "we didn't have anywhere to live at the time")
- **"can't"** → `못` vs `수 없어요` vs `수 없는` vs `없다는`
    - S0526L01 raw="can't (negative ability)" → `못` (seed: "I'm finding it hard to believe that you can't guess")
    - S0331L04 raw="can't (present)" → `수 없어요` (seed: "she can't provide all the answers")
    - S0336L02 raw="can't (modifier/attributive form)" → `수 없는` (seed: "I don't think that she can open the door")
    - S0469L02 raw="can't (that) / couldn't (quotative)" → `없다는` (seed: "but that doesn't mean we can't change it")
- **"to leave"** → `떠나고` vs `떠날`
    - S0540L02 raw="to leave" → `떠나고` (seed: "I don't mind if you want to leave without the car")
    - S0345L01 raw="to leave (future potential form)" → `떠날` (seed: "who said that he's not ready to leave yet")
- **"meaning"** → `뜻일` vs `뜻은`
    - S0432L01 raw="meaning / implication (future/possible)" → `뜻일` (seed: "they could mean that they want you to ask")
    - S0469L01 raw="meaning (topic)" → `뜻은` (seed: "but that doesn't mean we can't change it")
- **"very brave"** → `아주 용감하다고` vs `아주 용감했어요`
    - S0615L03 raw="very brave" → `아주 용감하다고` (seed: "I thought you were very brave to say that")
    - S0616L02 raw="very brave (past)" → `아주 용감했어요` (seed: "you were very brave to say you thought that")

### case_number_register (33) — Expand the LEGO to encode the grammatical role via context (M-type w/ governing prep/verb), OR distinguish on the known side. Needs methodology judgment (known-side control).

- **"something"** → `뭔가를` vs `뭔가`
    - S0646L02 raw="something (object)" → `뭔가를` (seed: "you're doing something sir")
    - S0004L01 raw="something" → `뭔가를` (seed: "how to say something in Korean")
    - S0661L02 raw="something (bare)" → `뭔가` (seed: "you're all doing something")
    - S0612L02 raw="something/anything" → `뭔가를` (seed: "we would have bought something")
- **"sir"** → `선생님과` vs `선생님을` vs `선생님`
    - S0639L01 raw="sir/teacher (honorific)" → `선생님과` (seed: "with you sir")
    - S0654L01 raw="sir (object)" → `선생님을` (seed: "I'm not sure if I can help you, sir")
    - S0643L01 raw="sir/teacher" → `선생님` (seed: "do you want sir")
- **"that"** → `저` vs `그건` vs `그거` vs `그것이`
    - S0641L01 raw="that (over there)" → `저` (seed: "I think that it's the red one on that chair")
    - S0127L01 raw="that" → `그건` (seed: "that isn't why I wanted to see you")
    - S0644L02 raw="that/it (object)" → `그거` (seed: "could you say that sir?")
    - S0617L01 raw="that/it" → `그건` (seed: "you thought that it was a mistake")
    - S0621L01 raw="that/it (subject)" → `그것이` (seed: "I wouldn't have dared to tell her that it was broken")
- **"madam"** → `여사님` vs `여사님이` vs `여사님을`
    - S0647L01 raw="madam" → `여사님` (seed: "you speak it madam")
    - S0648L01 raw="madam (subject)" → `여사님이` (seed: "what you said madam")
    - S0642L01 raw="madam (honorific)" → `여사님` (seed: "how do you feel madam?")
    - S0645L01 raw="madam (object)" → `여사님을` (seed: "I can help you madam")
- **"do you think?"** → `생각하세요?` vs `생각해요?`
    - S0651L03 raw="do you think? (polite)" → `생각하세요?` (seed: "what do you think madam?")
    - S0666L03 raw="do you think? (casual)" → `생각해요?` (seed: "what do you all think?")
- **"very"** → `매우` vs `아주`
    - S0655L02 raw="very (formal)" → `매우` (seed: "I think that you're doing very well, madam")
    - S0013L02 raw="very" → `아주` (seed: "you speak Korean very well")
- **"what"** → `무엇이` vs `뭐`
    - S0652L02 raw="what (subject)" → `무엇이` (seed: "what do you need sir?")
    - S0631L01 raw="what" → `뭐` (seed: "what would you like?")
- **"please say"** → `말해` vs `말씀해`
    - S0659L03 raw="please say (polite)" → `말해` (seed: "could you all say that")
    - S0644L03 raw="please say (honorific)" → `말씀해` (seed: "could you say that sir?")
- **"you all"** → `여러분을` vs `여러분이` vs `여러분은` vs `여러분`
    - S0660L01 raw="you all (object)" → `여러분을` (seed: "I can help you all")
    - S0663L01 raw="you all (subject)" → `여러분이` (seed: "what you all said")
    - S0666L01 raw="you all (topic)" → `여러분은` (seed: "what do you all think?")
    - S0667L01 raw="you all (bare)" → `여러분` (seed: "do you all mind?")
    - S0657L01 raw="you all (plain)" → `여러분` (seed: "how do you all feel?")
- **"he"** → `그는` vs `그가`
    - S0016L01 raw="he" → `그는` (seed: "he wants to come back with everyone else later on")
    - S0598L01 raw="he (topic)" → `그는` (seed: "he's heard a thousand stories about what they were doing")
    - S0597L01 raw="he (subject)" → `그가` (seed: "I suspect that he's heard a hundred stories about it")
- **"bag"** → `가방은` vs `가방을` vs `가방이에요`
    - S0637L02 raw="bag (topic)" → `가방은` (seed: "where is her bag?")
    - S0445L02 raw="bag / suitcase (object)" → `가방을` (seed: "they will be able to carry that suitcase")
    - S0635L02 raw="bag" → `가방이에요` (seed: "that is Jane's bag")
- **"want to go"** → `가고 싶어요?` vs `가시고 싶으세요?`
    - S0665L02 raw="want to go" → `가고 싶어요?` (seed: "do you all want to go?")
    - S0650L02 raw="want to go (honorific)" → `가시고 싶으세요?` (seed: "do you want to go madam?")
- **"work"** → `일하고` vs `일을`
    - S0304L02 raw="work" → `일하고` (seed: "I think that she doesn't want to work from home")
    - S0535L01 raw="work / job (accusative)" → `일을` (seed: "he made a promise that he wouldn't choose the wrong job")
- **"news"** → `소식이` vs `소식을`
    - S0511L01 raw="news (subject)" → `소식이` (seed: "the news took several hours to reach everyone in the office")
    - S0577L02 raw="news" → `소식을` (seed: "we've been waiting for the news from the hospital")
- **"baby"** → `아기를` vs `새끼`
    - S0519L02 raw="baby (accusative)" → `아기를` (seed: "I haven't seen their new baby yet")
    - S0334L01 raw="baby / kitten (prefix)" → `새끼` (seed: "he said that he can let you hold the kitten")
- **"family"** → `가족` vs `가족을`
    - S0520L01 raw="family" → `가족` (seed: "it might have happened to the whole family")
    - S0408L01 raw="family (object)" → `가족을` (seed: "yes that's the best way to make a happy family")
- **"book"** → `책을` vs `책` vs `책이`
    - S0322L01 raw="book (acc.)" → `책을` (seed: "she said that she needs to read the same book")
    - S0321L01 raw="book" → `책` (seed: "a book")
    - S0346L01 raw="book (subject particle)" → `책이` (seed: "I wanted her to know that I liked her book")
- **"life"** → `삶을` vs `인생은`
    - S0332L03 raw="life (acc.)" → `삶을` (seed: "he can build a new life for his sister")
    - S0483L03 raw="life (topic)" → `인생은` (seed: "life isn't easy but it's not meant to be easy")
- **"words"** → `말을` vs `말` vs `말도`
    - S0533L02 raw="words / speech (accusative)" → `말을` (seed: "she won't listen to every word you say")
    - S0367L01 raw="words / say" → `말` (seed: "no nobody told me")
    - S0360L01 raw="words/also" → `말도` (seed: "did your friend say anything else?")
- **"friends"** → `친구들을` vs `친구들이`
    - S0350L01 raw="friends (accusative)" → `친구들을` (seed: "yes he wanted to see some old friends")
    - S0454L04 raw="friends (subject)" → `친구들이` (seed: "our friends came round at about six o'clock")
- **"where"** → `어디` vs `어디가`
    - S0377L02 raw="where / somewhere" → `어디` (seed: "did you go anywhere in the world last year?")
    - S0492L01 raw="where / which place (subject)" → `어디가` (seed: "which of those places do you think is the most interesting?")
- **"travel"** → `여행을` vs `여행`
    - S0379L01 raw="travel (accusative)" → `여행을` (seed: "yes I was lucky enough to travel to Africa")
    - S0377L03 raw="travel / trip" → `여행` (seed: "did you go anywhere in the world last year?")
- **"hope"** → `희망을` vs `희망은`
    - S0399L02 raw="hope (accusative)" → `희망을` (seed: "we don't want to lose hope")
    - S0482L02 raw="hope (topic)" → `희망은` (seed: "the only real hope is that they're not serious")
- **"question"** → `질문을` vs `질문`
    - S0423L03 raw="question (object)" → `질문을` (seed: "do they need to ask such an obvious question?")
    - S0422L01 raw="question" → `질문` (seed: "a question")
- **"approach"** → `접근법을` vs `접근법`
    - S0442L02 raw="approach (object)" → `접근법을` (seed: "did they want to develop a new approach?")
    - S0441L01 raw="approach / method" → `접근법` (seed: "an approach")
- **"themselves"** → `직접` vs `자기들을` vs `자기들이`
    - S0450L01 raw="themselves / directly" → `직접` (seed: "no they have to catch the train themselves")
    - S0419L01 raw="themselves (object)" → `자기들을` (seed: "if they want people to like them")
    - S0427L02 raw="themselves (subject)" → `자기들이` (seed: "they wouldn't like you to think that they're bored")
- **"eyes"** → `눈을` vs `눈이`
    - S0596L02 raw="eyes (object)" → `눈을` (seed: "and I need to close my eyes for a while")
    - S0486L01 raw="eyes (subject)" → `눈이` (seed: "I think your eyes are beautiful")
- **"egg"** → `달걀` vs `달걀을`
    - S0586L02 raw="egg(s)" → `달걀` (seed: "I want twenty boiled eggs")
    - S0587L03 raw="egg(s) (object)" → `달걀을` (seed: "she used to insist that we had eggs for supper")
- **"number"** → `번호를` vs `번호`
    - S0464L02 raw="number (object)" → `번호를` (seed: "I told her my room number once but she forgot it")
    - S0463L02 raw="number" → `번호` (seed: "my room number")
- **"children"** → `아이들을` vs `아이들이`
    - S0580L01 raw="children" → `아이들을` (seed: "we've often wanted to take the children somewhere a little warmer")
    - S0455L02 raw="children (subject)" → `아이들이` (seed: "we had to leave because the children were tired")
- **"shop"** → `가게를` vs `가게`
    - S0460L03 raw="shop / store (object)" → `가게를` (seed: "I want to find a shop near the hotel")
    - S0461L01 raw="shop (bare)" → `가게` (seed: "a shop where I can buy some postcards")
- **"mountains"** → `산이` vs `산을`
    - S0585L04 raw="mountains (subject)" → `산이` (seed: "I can see the mountains out of my bedroom window")
    - S0584L03 raw="mountains" → `산을` (seed: "what's it like to wake up every morning and see the mountains?")
- **"tea"** → `차를` vs `차`
    - S0628L01 raw="tea (object)" → `차를` (seed: "do you like tea?")
    - S0623L01 raw="tea" → `차` (seed: "do you want a cup of coffee or tea?")

### slash_only (13) — Driven by slash resolution — DECIDE THE SLASH FIRST (he/she→seed; synonym→first; word+trailing→keep). May be a false collision from first-option heuristic.

- **"about it"** → `그것에 대해` vs `그에 대해`
    - S0037L02 raw="about it" → `그것에 대해` (seed: "I started to think about it carefully last month")
    - S0597L02 raw="about it/him" → `그에 대해` (seed: "I suspect that he's heard a hundred stories about it")
- **"thank you"** → `고마워요` vs `감사합니다`
    - S0073L01 raw="thank you" → `고마워요` (seed: "thank you very much, but I've got more to learn")
    - S0624L03 raw="thank you/please" → `감사합니다` (seed: "yes, that would be great, please")
- **"a little"** → `조금` vs `좀`
    - S0009L02 raw="a little" → `조금` (seed: "I speak a little Korean now")
    - S0580L02 raw="a little/bit" → `좀` (seed: "we've often wanted to take the children somewhere a little warmer")
- **"together"** → `같이` vs `함께`
    - S0001L03 raw="together" → `같이` (seed: "I want to speak Korean with you now")
    - S0639L02 raw="together/with" → `함께` (seed: "with you sir")
- **"all"** → `다` vs `모든`
    - S0313L03 raw="all" → `다` (seed: "he said that he couldn't watch all five games")
    - S0331L01 raw="all / every" → `모든` (seed: "she can't provide all the answers")
- **"such"** → `그렇게` vs `그런`
    - S0557L02 raw="such" → `그렇게` (seed: "I wish they wouldn't play such loud music")
    - S0423L01 raw="such / that kind of" → `그런` (seed: "do they need to ask such an obvious question?")
- **"wants"** → `싶은 게` vs `원해요`
    - S0380L02 raw="wants / desired things" → `싶은 게` (seed: "I asked what she wanted to include")
    - S0586L05 raw="wants" → `원해요` (seed: "I want twenty boiled eggs")
- **"somewhere"** → `어딘가에` vs `곳에`
    - S0402L04 raw="somewhere / at some place" → `어딘가에` (seed: "yes it would be nice to stop somewhere for food")
    - S0578L02 raw="somewhere" → `곳에` (seed: "somewhere warmer")
- **"still"** → `아직도` vs `여전히`
    - S0410L01 raw="still / yet" → `아직도` (seed: "they still fight with each other")
    - S0593L03 raw="still/always" → `여전히` (seed: "however much I argued I still had to share")
- **"to ask"** → `물어볼` vs `물어보는`
    - S0420L01 raw="to ask / will ask" → `물어볼` (seed: "they don't need to ask how old he is")
    - S0609L01 raw="to ask/asking" → `물어보는` (seed: "the sensible thing to do would have been to ask")
- **"first"** → `처음에` vs `첫`
    - S0602L01 raw="first/initially" → `처음에` (seed: "how it started in the first place")
    - S0494L01 raw="first" → `첫` (seed: "what happens after the first part of the show?")
- **"must close"** → `감아야` vs `닫아야`
    - S0596L03 raw="must close" → `감아야` (seed: "and I need to close my eyes for a while")
    - S0499L03 raw="must close / have to close" → `닫아야` (seed: "maybe we should open the door and close the window")
- **"thought that"** → `생각한다고` vs `생각했어요`
    - S0616L01 raw="thought that/thinking that" → `생각한다고` (seed: "you were very brave to say you thought that")
    - S0617L03 raw="thought that" → `생각했어요` (seed: "you thought that it was a mistake")

## ara_for_eng

### true_synonym (14) — Pick different known gloss for one, OR set is_new=false if covered elsewhere, OR merge. Per-item.

- **"can I"** → `يُمْكِنُني` vs `يُمْكِنُني أَنْ`
    - S0630L01 raw="can I (it is possible for me)" → `يُمْكِنُني` (seed: "can I have something to drink please?")
    - S0119L01 raw="can I" → `يُمْكِنُني أَنْ` (seed: "can I ask you something before you leave?")
- **"to drink it"** → `لِأَشْرَبَهُ` vs `لِتَشْرَبَهُ`
    - S0630L03 raw="to drink it (for me)" → `لِأَشْرَبَهُ` (seed: "can I have something to drink please?")
    - S0625L01 raw="to drink it" → `لِتَشْرَبَهُ` (seed: "do you want something to drink?")
- **"helping you"** → `مُساعَدَتَكِ` vs `مُساعَدَتَكَ`
    - S0645L01 raw="helping you (feminine)" → `مُساعَدَتَكِ` (seed: "I can help you madam")
    - S0654L02 raw="helping you (masculine)" → `مُساعَدَتَكَ` (seed: "I'm not sure if I can help you, sir")
- **"you said"** → `قُلْتِ` vs `قُلْتَ`
    - S0648L01 raw="you said (feminine)" → `قُلْتِ` (seed: "what you said madam")
    - S0078L02 raw="you said" → `قُلْتَ` (seed: "I don't understand what you said")
- **"you do"** → `تَفْعَلُ` vs `تفعل`
    - S0646L01 raw="you do (with tashkeel)" → `تَفْعَلُ` (seed: "you're doing something sir")
    - S0167L02 raw="you do" → `تفعل` (seed: "what do you need to do tomorrow afternoon?")
- **"what"** → `ما` vs `ماذا`
    - S0451L01 raw="what (relative)" → `ما` (seed: "they said what they wanted to do")
    - S0068L01 raw="what (in questions)" → `ماذا` (seed: "what are you looking for?")
- **"right"** → `حَقٍّ` vs `اليَمينِ`
    - S0387L01 raw="right / correct" → `حَقٍّ` (seed: "no I didn't think she was right")
    - S0502L01 raw="right (direction)" → `اليَمينِ` (seed: "she almost got lost because she turned left instead of right")
- **"new"** → `جَديدَة` vs `جَديداً`
    - S0109L02 raw="new" → `جَديدَة` (seed: "we must work hard to learn a lot of new words")
    - S0111L01 raw="new (masc)" → `جَديداً` (seed: "when we learn something new it changes our brain")
- **"earlier"** → `أَبْكَرَ` vs `سابِقاً`
    - S0144L02 raw="earlier (comparative)" → `أَبْكَرَ` (seed: "I woke earlier than I wanted to this morning")
    - S0143L05 raw="earlier / previously" → `سابِقاً` (seed: "it's the same thing as we were talking about earlier")
- **"good"** → `جَيِّدَة` vs `جَيِّدٌ`
    - S0123L02 raw="good (fem)" → `جَيِّدَة` (seed: "I think that's a good idea")
    - S0047L01 raw="good" → `جَيِّدٌ` (seed: "because I think that it's a good thing to make mistakes")
- **"was"** → `كان` vs `كانَتْ`
    - S0070L02 raw="was" → `كان` (seed: "she didn't want to tell me where it was")
    - S0124L02 raw="was (fem)" → `كانَتْ` (seed: "I thought that was a good idea")
- **"that"** → `إِنَّ` vs `ذٰلِكَ` vs `تِلْكَ` vs `الَّذي`
    - S0102L03 raw="that (conjunction)" → `إِنَّ` (seed: "we're trying to say that it's not like that")
    - S0112L01 raw="that" → `ذٰلِكَ` (seed: "that was very interesting, and I wasn't expecting it")
    - S0124L01 raw="that (fem)" → `تِلْكَ` (seed: "I thought that was a good idea")
    - S0143L03 raw="that / which" → `الَّذي` (seed: "it's the same thing as we were talking about earlier")
- **"kind"** → `لَطيفَةً` vs `لَطيفٌ`
    - S0147L01 raw="kind (fem)" → `لَطيفَةً` (seed: "she was very kind when she saw me feeling nervous")
    - S0142L01 raw="kind" → `لَطيفٌ` (seed: "that's very kind of you and I'm grateful to you for helping")
- **"I'm afraid"** → `أَخْشى` vs `أخشى`
    - S0521L01 raw="I'm afraid" → `أَخْشى` (seed: "I'm afraid that you'll forget why we needed to stay here")
    - S0183L02 raw="I'm afraid" → `أخشى` (seed: "no I'm afraid I haven't seen them")

### tense_aspect (4) — Tense/aspect demo: introduce-once-then-free, OR expand with a time/aspect cue so the English prompt is deterministic.

- **"you can"** → `يُمْكِنُكَ` vs `تستطيع` vs `يُمْكِنُكَ أَنْ`
    - S0644L01 raw="you can (can you)" → `يُمْكِنُكَ` (seed: "could you say that sir?")
    - S0292L01 raw="you can (subjunctive)" → `تستطيع` (seed: "I hope you'll be able to come to the party")
    - S0136L02 raw="you can" → `يُمْكِنُكَ أَنْ` (seed: "of course you can ask her because she's my friend")
- **"you think"** → `تَظُنّينَ` vs `تَظُنَّ` vs `تُفَكِّرَ`
    - S0651L01 raw="you think (feminine)" → `تَظُنّينَ` (seed: "what do you think madam?")
    - S0427L02 raw="you think (subjunctive)" → `تَظُنَّ` (seed: "they wouldn't like you to think that they're bored")
    - S0091L02 raw="you think" → `تُفَكِّرَ` (seed: "it's difficult to think quickly enough to answer in time")
- **"if"** → `إِنْ` vs `لَوْ`
    - S0654L01 raw="if (present/future condition)" → `إِنْ` (seed: "I'm not sure if I can help you, sir")
    - S0152L03 raw="if (conditional)" → `لَوْ` (seed: "I would have done it differently if I had known what you wanted")
- **"I can"** → `أستطيع` vs `أَسْتَطيعُ`
    - S0291L01 raw="I can (subjunctive)" → `أستطيع` (seed: "I hope I'll be able to speak better soon")
    - S0007L03 raw="I can" → `أَسْتَطيعُ` (seed: "I want to try as hard as I can today")

### case_number_register (5) — Expand the LEGO to encode the grammatical role via context (M-type w/ governing prep/verb), OR distinguish on the known side. Needs methodology judgment (known-side control).

- **"long"** → `طَويلاً` vs `طَويلٌ`
    - S0618L01 raw="long" → `طَويلاً` (seed: "it doesn't feel like a long time")
    - S0619L01 raw="long (nominative)" → `طَويلٌ` (seed: "it's a long time since we saw each other")
- **"ready"** → `جاهِزَةٌ` vs `مُسْتَعِدّونَ` vs `مُسْتَعِدّينَ` vs `مُسْتَعِدٌّ`
    - S0627L01 raw="ready (done)" → `جاهِزَةٌ` (seed: "your coffee is ready")
    - S0664L01 raw="ready (plural nominative)" → `مُسْتَعِدّونَ` (seed: "are you all ready?")
    - S0396L02 raw="ready (plural)" → `مُسْتَعِدّينَ` (seed: "we don't need to stand until everybody else is ready")
    - S0026L02 raw="ready" → `مُسْتَعِدٌّ` (seed: "I like feeling as if I'm nearly ready to go")
- **"you feel"** → `تَشْعُرينَ` vs `تَشْعُرونَ`
    - S0642L01 raw="you feel (feminine)" → `تَشْعُرينَ` (seed: "how do you feel madam?")
    - S0657L01 raw="you (plural) feel" → `تَشْعُرونَ` (seed: "how do you all feel?")
- **"last night"** → `اللَّيْلَةَ الماضِيَةَ` vs `اللَّيْلَةِ الماضِيَة`
    - S0453L02 raw="last night (time/acc.)" → `اللَّيْلَةَ الماضِيَةَ` (seed: "did they say who they saw last night?")
    - S0042L02 raw="last night" → `اللَّيْلَةِ الماضِيَة` (seed: "I was starting to feel better than last night")
- **"quiet"** → `هادِئينَ` vs `هادئ`
    - S0403L01 raw="quiet (plural)" → `هادِئينَ` (seed: "we should remain quiet for as long as possible")
    - S0034L02 raw="quiet" → `هادئ` (seed: "he doesn't want to be quiet when other people are here")

### mixed_grammar (4) — Case + tense both — full expansion/redecomposition. Heaviest; per-item.

- **"you all can"** → `يُمْكِنُكُمْ` vs `تَسْتَطيعوا`
    - S0659L01 raw="you all can" → `يُمْكِنُكُمْ` (seed: "could you all say that")
    - S0668L01 raw="you all can (plural subjunctive)" → `تَسْتَطيعوا` (seed: "I hope you'll all be able to go")
- **"they wanted"** → `أَرادوا` vs `أرادوا`
    - S0451L02 raw="they wanted (past pl)" → `أَرادوا` (seed: "they said what they wanted to do")
    - S0419L01 raw="they wanted / they want (past)" → `أَرادوا` (seed: "if they want people to like them")
    - S0212L01 raw="they wanted" → `أرادوا` (seed: "they wanted to ask for help")
- **"they know"** → `يَعْرِفونَ` vs `يَعْرِفوا`
    - S0421L02 raw="they know (plural)" → `يَعْرِفونَ` (seed: "because they already know he's getting weak")
    - S0433L02 raw="they know/find out (plural subjunctive)" → `يَعْرِفوا` (seed: "they couldn't find out when the film started")
- **"they want"** → `يُريدوا` vs `يريدون`
    - S0438L01 raw="they want (plural subjunctive)" → `يُريدوا` (seed: "they didn't want to decide what he should do")
    - S0200L03 raw="they want" → `يريدون` (seed: "they say they want to make sure that we finish everything in time")

### slash_only (6) — Driven by slash resolution — DECIDE THE SLASH FIRST (he/she→seed; synonym→first; word+trailing→keep). May be a false collision from first-option heuristic.

- **"waiting"** → `الانْتِظارُ` vs `الانْتِظارَ`
    - S0576L01 raw="waiting" → `الانْتِظارُ` (seed: "it has been very upsetting waiting")
    - S0155L02 raw="waiting / the waiting" → `الانْتِظارَ` (seed: "I don't mind waiting for a few minutes tomorrow morning")
- **"each other"** → `بَعْضَنا` vs `بَعْضَهُمْ`
    - S0619L04 raw="each other" → `بَعْضَنا` (seed: "it's a long time since we saw each other")
    - S0426L03 raw="each other / one another" → `بَعْضَهُمْ` (seed: "they would like to love each other but they're unhappy")
- **"anyone"** → `أَحَداً` vs `أحد`
    - S0370L01 raw="anyone / someone" → `أَحَداً` (seed: "I didn't see anyone that I knew")
    - S0202L01 raw="anyone" → `أحد` (seed: "nobody was sure how to answer the question")
- **"I know him"** → `أَعْرِفُهُ` vs `أَعْرِفُه`
    - S0370L03 raw="I know him / I knew him" → `أَعْرِفُهُ` (seed: "I didn't see anyone that I knew")
    - S0128L02 raw="I know him" → `أَعْرِفُه` (seed: "you're like someone I used to know")
- **"everybody"** → `الجَميعُ` vs `الجَميعِ`
    - S0396L01 raw="everybody" → `الجَميعُ` (seed: "we don't need to stand until everybody else is ready")
    - S0424L01 raw="everybody / everyone" → `الجَميعِ` (seed: "no they're wasting everybody's time")
- **"we are"** → `نَكونَ` vs `نَحْنُ`
    - S0407L02 raw="we are / we be" → `نَكونَ` (seed: "shouldn't we try to set a good example?")
    - S0110L01 raw="we are" → `نَحْنُ` (seed: "we're friends, and after we finish I'd like to relax")

