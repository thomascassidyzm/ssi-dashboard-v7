# Generated at config load from the pod-recording speech clip, and rebuilt on
# every run — there is no reason to carry a WAV in git.
fixtures/
# Proof-of-run screenshots the spec writes as it goes.
*.png
